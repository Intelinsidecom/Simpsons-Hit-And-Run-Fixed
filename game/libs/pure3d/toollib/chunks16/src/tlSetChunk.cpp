// Automatically generated by Jeeves, don't edit.  Edit tlSetChunk.sc instead

#include "tlSetChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlSetChunk.sc


// tlSetChunk

int tlSetChunk::sortPriority = -1;

tlSetChunk::tlSetChunk()
{
    Init();
}

tlSetChunk::tlSetChunk(const tlSetChunk& ch)
{
    Init();
    *this = ch;
}

tlSetChunk::~tlSetChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlSetChunk::tlSetChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::CHUNK_SET);
    ident = SRR2::ChunkID::CHUNK_SET;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    version = f->GetLong();
    childcount = f->GetChar();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::CHUNK_SET);
    }
}

void
tlSetChunk::Init()
{
    ident = SRR2::ChunkID::CHUNK_SET;
    name = NULL;
    version = 0;
    childcount = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::CHUNK_SET);
    }
}

long 
tlSetChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( version );
    s += sizeof( childcount );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlSetChunk&
tlSetChunk::operator=(const tlSetChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    version = a.version;
    childcount = a.childcount;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlSetChunk::operator==(const tlDataChunk& a)
{

    const tlSetChunk* aptr = dynamic_cast<const tlSetChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(version == aptr->version))
    {
        return false;
    }
    if(!(childcount == aptr->childcount))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlSetChunk::NameCompare(const tlDataChunk& a)
{

    const tlSetChunk* aptr = dynamic_cast<const tlSetChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlSetChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlSetChunk(ch);
    return dtCh;
}

void
tlSetChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(version);
        f->PutChar(childcount);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlSetChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlSetChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name       = \"%s\"\n",indent,"",name);
    printf("%*s  Version    = %ld\n",indent,"",version);
    printf("%*s  ChildCount = %d\n",indent,"",childcount);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlSetChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name      \n",indent,"",name);
    printf("%*s  int   %12ld # Version   \n",indent,"",version);
    printf("%*s  byte  %12d # ChildCount\n",indent,"",childcount);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlSetChunk::GetType() const
{
    return "tlSetChunk";
}

int 
tlSetChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlSetChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlSetChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlSetChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "UBYTE";

        default:
            return 0;
    }
}

const char* 
tlSetChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Version";

        case 2:
            return "ChildCount";

        default:
            return 0;
    }
}

bool 
tlSetChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%d", childcount);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlSetChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlSetChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlSetChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned char tmp;
            tmp = (unsigned char)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                childcount = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlSetChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlSetChunk::Copy()
{
    return new tlSetChunk(*this);
}

tlDataChunk*
tlSetChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case Pure3D::Texture::TEXTURE:
        dch = new tlTextureChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlSetChunk

const char* 
tlSetChunk::Name()
{
    return name;
}

void 
tlSetChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlSetChunk::Version()
{
    return version;
}

void 
tlSetChunk::SetVersion(unsigned long val)
{
    version = val;
}

unsigned char 
tlSetChunk::ChildCount()
{
    return childcount;
}

void 
tlSetChunk::SetChildCount(unsigned char val)
{
    childcount = val;
}

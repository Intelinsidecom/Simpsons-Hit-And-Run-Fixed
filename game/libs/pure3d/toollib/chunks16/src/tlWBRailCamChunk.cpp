// Automatically generated by Jeeves, don't edit.  Edit tlWBRailCamChunk.sc instead

#include "tlWBRailCamChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlWBRailCamChunk.sc


// tlWBRailCamChunk

int tlWBRailCamChunk::sortPriority = -1;

tlWBRailCamChunk::tlWBRailCamChunk()
{
    Init();
}

tlWBRailCamChunk::tlWBRailCamChunk(const tlWBRailCamChunk& ch)
{
    Init();
    *this = ch;
}

tlWBRailCamChunk::~tlWBRailCamChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlWBRailCamChunk::tlWBRailCamChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::RAIL);
    ident = SRR2::ChunkID::RAIL;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    behaviour = f->GetLong();
    minradius = f->GetFloat();
    maxradius = f->GetFloat();
    trackrail = f->GetLong();
    trackdist = f->GetFloat();
    reversesense = f->GetLong();
    fov = f->GetFloat();
    targetoffset.Read(f);
    axisplay.Read(f);
    positionlag = f->GetFloat();
    targetlag = f->GetFloat();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::RAIL);
    }
}

void
tlWBRailCamChunk::Init()
{
    ident = SRR2::ChunkID::RAIL;
    name = NULL;
    behaviour = 0;
    minradius = 0.0f;
    maxradius = 0.0f;
    trackrail = 0;
    trackdist = 0.0f;
    reversesense = 0;
    fov = 0.0f;
    targetoffset.Init( );
    axisplay.Init( );
    positionlag = 0.0f;
    targetlag = 0.0f;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::RAIL);
    }
}

long 
tlWBRailCamChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( behaviour );
    s += sizeof( minradius );
    s += sizeof( maxradius );
    s += sizeof( trackrail );
    s += sizeof( trackdist );
    s += sizeof( reversesense );
    s += sizeof( fov );
    s += sizeof( targetoffset );
    s += sizeof( axisplay );
    s += sizeof( positionlag );
    s += sizeof( targetlag );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlWBRailCamChunk&
tlWBRailCamChunk::operator=(const tlWBRailCamChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    behaviour = a.behaviour;
    minradius = a.minradius;
    maxradius = a.maxradius;
    trackrail = a.trackrail;
    trackdist = a.trackdist;
    reversesense = a.reversesense;
    fov = a.fov;
    targetoffset = a.targetoffset;
    axisplay = a.axisplay;
    positionlag = a.positionlag;
    targetlag = a.targetlag;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlWBRailCamChunk::operator==(const tlDataChunk& a)
{

    const tlWBRailCamChunk* aptr = dynamic_cast<const tlWBRailCamChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(behaviour == aptr->behaviour))
    {
        return false;
    }
    if(!(minradius == aptr->minradius))
    {
        return false;
    }
    if(!(maxradius == aptr->maxradius))
    {
        return false;
    }
    if(!(trackrail == aptr->trackrail))
    {
        return false;
    }
    if(!(trackdist == aptr->trackdist))
    {
        return false;
    }
    if(!(reversesense == aptr->reversesense))
    {
        return false;
    }
    if(!(fov == aptr->fov))
    {
        return false;
    }
    if(!(targetoffset == aptr->targetoffset))
    {
        return false;
    }
    if(!(axisplay == aptr->axisplay))
    {
        return false;
    }
    if(!(positionlag == aptr->positionlag))
    {
        return false;
    }
    if(!(targetlag == aptr->targetlag))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlWBRailCamChunk::NameCompare(const tlDataChunk& a)
{

    const tlWBRailCamChunk* aptr = dynamic_cast<const tlWBRailCamChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlWBRailCamChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlWBRailCamChunk(ch);
    return dtCh;
}

void
tlWBRailCamChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(behaviour);
        f->PutFloat(minradius);
        f->PutFloat(maxradius);
        f->PutLong(trackrail);
        f->PutFloat(trackdist);
        f->PutLong(reversesense);
        f->PutFloat(fov);
        targetoffset.Write(f);
        axisplay.Write(f);
        f->PutFloat(positionlag);
        f->PutFloat(targetlag);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlWBRailCamChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlWBRailCamChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name         = \"%s\"\n",indent,"",name);
    printf("%*s  Behaviour    = %ld\n",indent,"",behaviour);
    printf("%*s  MinRadius    = %f\n",indent,"",minradius);
    printf("%*s  MaxRadius    = %f\n",indent,"",maxradius);
    printf("%*s  TrackRail    = %ld\n",indent,"",trackrail);
    printf("%*s  TrackDist    = %f\n",indent,"",trackdist);
    printf("%*s  ReverseSense = %ld\n",indent,"",reversesense);
    printf("%*s  FOV          = %f\n",indent,"",fov);
    printf("%*s  TargetOffset:\n",indent,"");
    targetoffset.Print(0, indent+4);
    printf("%*s  AxisPlay    :\n",indent,"");
    axisplay.Print(0, indent+4);
    printf("%*s  PositionLag  = %f\n",indent,"",positionlag);
    printf("%*s  TargetLag    = %f\n",indent,"",targetlag);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlWBRailCamChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name        \n",indent,"",name);
    printf("%*s  int   %12ld # Behaviour   \n",indent,"",behaviour);
    printf("%*s  float %12f # MinRadius   \n",indent,"",minradius);
    printf("%*s  float %12f # MaxRadius   \n",indent,"",maxradius);
    printf("%*s  int   %12ld # TrackRail   \n",indent,"",trackrail);
    printf("%*s  float %12f # TrackDist   \n",indent,"",trackdist);
    printf("%*s  int   %12ld # ReverseSense\n",indent,"",reversesense);
    printf("%*s  float %12f # FOV         \n",indent,"",fov);
    printf("%*s  # TargetOffset:\n",indent,"");
    targetoffset.PrintFormatted(0, indent+4);
    printf("%*s  # AxisPlay    :\n",indent,"");
    axisplay.PrintFormatted(0, indent+4);
    printf("%*s  float %12f # PositionLag \n",indent,"",positionlag);
    printf("%*s  float %12f # TargetLag   \n",indent,"",targetlag);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlWBRailCamChunk::GetType() const
{
    return "tlWBRailCamChunk";
}

int 
tlWBRailCamChunk::GetFieldCount() const
{
    return 12;
}

bool 
tlWBRailCamChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlWBRailCamChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlWBRailCamChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "float";

        case 3:
            return "float";

        case 4:
            return "ULONG";

        case 5:
            return "float";

        case 6:
            return "ULONG";

        case 7:
            return "float";

        case 8:
            return "tlPoint";

        case 9:
            return "tlPoint";

        case 10:
            return "float";

        case 11:
            return "float";

        default:
            return 0;
    }
}

const char* 
tlWBRailCamChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Behaviour";

        case 2:
            return "MinRadius";

        case 3:
            return "MaxRadius";

        case 4:
            return "TrackRail";

        case 5:
            return "TrackDist";

        case 6:
            return "ReverseSense";

        case 7:
            return "FOV";

        case 8:
            return "TargetOffset";

        case 9:
            return "AxisPlay";

        case 10:
            return "PositionLag";

        case 11:
            return "TargetLag";

        default:
            return 0;
    }
}

bool 
tlWBRailCamChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", behaviour);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%f", minradius);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%f", maxradius);
            strncpy(val, buf, len);
            return true;

        case 4:
            sprintf(buf, "%ld", trackrail);
            strncpy(val, buf, len);
            return true;

        case 5:
            sprintf(buf, "%f", trackdist);
            strncpy(val, buf, len);
            return true;

        case 6:
            sprintf(buf, "%ld", reversesense);
            strncpy(val, buf, len);
            return true;

        case 7:
            sprintf(buf, "%f", fov);
            strncpy(val, buf, len);
            return true;

        case 8:
            return targetoffset.GetFieldValue(val, len);

        case 9:
            return axisplay.GetFieldValue(val, len);

        case 10:
            sprintf(buf, "%f", positionlag);
            strncpy(val, buf, len);
            return true;

        case 11:
            sprintf(buf, "%f", targetlag);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlWBRailCamChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlWBRailCamChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return true;

        case 5:     
            return true;

        case 6:     
            return true;

        case 7:     
            return true;

        case 8:     
            return tlPoint::GetFieldUpdatable();

        case 9:     
            return tlPoint::GetFieldUpdatable();

        case 10:     
            return true;

        case 11:     
            return true;

        default:
            return false;
    }
}

bool 
tlWBRailCamChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                behaviour = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                minradius = tmp;
                return true;
            }
            return false;
        }
        case 3:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                maxradius = tmp;
                return true;
            }
            return false;
        }
        case 4:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                trackrail = tmp;
                return true;
            }
            return false;
        }
        case 5:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                trackdist = tmp;
                return true;
            }
            return false;
        }
        case 6:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                reversesense = tmp;
                return true;
            }
            return false;
        }
        case 7:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                fov = tmp;
                return true;
            }
            return false;
        }
        case 8:
            return targetoffset.SetFieldValue(new_val);
        case 9:
            return axisplay.SetFieldValue(new_val);
        case 10:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                positionlag = tmp;
                return true;
            }
            return false;
        }
        case 11:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                targetlag = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlWBRailCamChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlWBRailCamChunk::Copy()
{
    return new tlWBRailCamChunk(*this);
}

tlDataChunk*
tlWBRailCamChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlWBRailCamChunk

const char* 
tlWBRailCamChunk::Name()
{
    return name;
}

void 
tlWBRailCamChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlWBRailCamChunk::Behaviour()
{
    return behaviour;
}

void 
tlWBRailCamChunk::SetBehaviour(unsigned long val)
{
    behaviour = val;
}

float 
tlWBRailCamChunk::MinRadius()
{
    return minradius;
}

void 
tlWBRailCamChunk::SetMinRadius(float val)
{
    minradius = val;
}

float 
tlWBRailCamChunk::MaxRadius()
{
    return maxradius;
}

void 
tlWBRailCamChunk::SetMaxRadius(float val)
{
    maxradius = val;
}

unsigned long 
tlWBRailCamChunk::TrackRail()
{
    return trackrail;
}

void 
tlWBRailCamChunk::SetTrackRail(unsigned long val)
{
    trackrail = val;
}

float 
tlWBRailCamChunk::TrackDist()
{
    return trackdist;
}

void 
tlWBRailCamChunk::SetTrackDist(float val)
{
    trackdist = val;
}

unsigned long 
tlWBRailCamChunk::ReverseSense()
{
    return reversesense;
}

void 
tlWBRailCamChunk::SetReverseSense(unsigned long val)
{
    reversesense = val;
}

float 
tlWBRailCamChunk::FOV()
{
    return fov;
}

void 
tlWBRailCamChunk::SetFOV(float val)
{
    fov = val;
}

tlPoint 
tlWBRailCamChunk::TargetOffset()
{
    return targetoffset;
}

void 
tlWBRailCamChunk::SetTargetOffset(tlPoint val)
{
    targetoffset = val;
}

tlPoint 
tlWBRailCamChunk::AxisPlay()
{
    return axisplay;
}

void 
tlWBRailCamChunk::SetAxisPlay(tlPoint val)
{
    axisplay = val;
}

float 
tlWBRailCamChunk::PositionLag()
{
    return positionlag;
}

void 
tlWBRailCamChunk::SetPositionLag(float val)
{
    positionlag = val;
}

float 
tlWBRailCamChunk::TargetLag()
{
    return targetlag;
}

void 
tlWBRailCamChunk::SetTargetLag(float val)
{
    targetlag = val;
}

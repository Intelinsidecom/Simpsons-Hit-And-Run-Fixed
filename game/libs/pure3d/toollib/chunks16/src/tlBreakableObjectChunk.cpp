// Automatically generated by Jeeves, don't edit.  Edit tlBreakableObjectChunk.sc instead

#include "tlBreakableObjectChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlBreakableObjectChunk.sc


// tlBreakableObjectChunk

int tlBreakableObjectChunk::sortPriority = -1;

tlBreakableObjectChunk::tlBreakableObjectChunk()
{
    Init();
}

tlBreakableObjectChunk::tlBreakableObjectChunk(const tlBreakableObjectChunk& ch)
{
    Init();
    *this = ch;
}

tlBreakableObjectChunk::~tlBreakableObjectChunk()
{

}

tlBreakableObjectChunk::tlBreakableObjectChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::BREAKABLE_OBJECT);
    ident = SRR2::ChunkID::BREAKABLE_OBJECT;

    tlFile* f = ch->GetFile();

    breakabletype = f->GetLong();
    maxinstances = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::BREAKABLE_OBJECT);
    }
}

void
tlBreakableObjectChunk::Init()
{
    ident = SRR2::ChunkID::BREAKABLE_OBJECT;
    breakabletype = 0;
    maxinstances = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::BREAKABLE_OBJECT);
    }
}

long 
tlBreakableObjectChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( breakabletype );
    s += sizeof( maxinstances );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlBreakableObjectChunk&
tlBreakableObjectChunk::operator=(const tlBreakableObjectChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    breakabletype = a.breakabletype;
    maxinstances = a.maxinstances;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlBreakableObjectChunk::operator==(const tlDataChunk& a)
{

    const tlBreakableObjectChunk* aptr = dynamic_cast<const tlBreakableObjectChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(breakabletype == aptr->breakabletype))
    {
        return false;
    }
    if(!(maxinstances == aptr->maxinstances))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlBreakableObjectChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlBreakableObjectChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlBreakableObjectChunk(ch);
    return dtCh;
}

void
tlBreakableObjectChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutLong(breakabletype);
        f->PutLong(maxinstances);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlBreakableObjectChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlBreakableObjectChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  BreakableType = %ld\n",indent,"",breakabletype);
    printf("%*s  MaxInstances  = %ld\n",indent,"",maxinstances);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlBreakableObjectChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # BreakableType\n",indent,"",breakabletype);
    printf("%*s  int   %12ld # MaxInstances \n",indent,"",maxinstances);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlBreakableObjectChunk::GetType() const
{
    return "tlBreakableObjectChunk";
}

int 
tlBreakableObjectChunk::GetFieldCount() const
{
    return 2;
}

bool 
tlBreakableObjectChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlBreakableObjectChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlBreakableObjectChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlBreakableObjectChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "BreakableType";

        case 1:
            return "MaxInstances";

        default:
            return 0;
    }
}

bool 
tlBreakableObjectChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", breakabletype);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", maxinstances);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlBreakableObjectChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlBreakableObjectChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        default:
            return false;
    }
}

bool 
tlBreakableObjectChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                breakabletype = tmp;
                return true;
            }
            return false;
        }
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                maxinstances = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlBreakableObjectChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlBreakableObjectChunk::Copy()
{
    return new tlBreakableObjectChunk(*this);
}

tlDataChunk*
tlBreakableObjectChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case Pure3D::AnimatedObject::FACTORY:
        dch = new tlAnimatedObjectFactoryChunk(&ch);
        break;
    case Pure3D::AnimatedObject::OBJECT:
        dch = new tlAnimatedObjectChunk(&ch);
        break;
    case Pure3D::Animation::FrameControllerData::FRAME_CONTROLLER:
        dch = new tlFrameControllerChunk(&ch);
        break;
    case Pure3D::Mesh::MESH:
        dch = new tlMeshChunk(&ch);
        break;
    case Pure3D::Animation::AnimationData::ANIMATION:
        dch = new tlAnimationChunk(&ch);
        break;
    case P3D_MULTI_CONTROLLER:
        dch = new tlMultiControllerChunk16(&ch);
        break;
    case P3D_SKELETON:
        dch = new tlSkeletonChunk16(&ch);
        break;
    case Pure3D::ParticleSystem::SYSTEM_FACTORY:
        dch = new tlParticleSystemFactoryChunk(&ch);
        break;
    case Pure3D::ParticleSystem::SYSTEM:
        dch = new tlParticleSystemChunk(&ch);
        break;
    case P3D_COMPOSITE_DRAWABLE:
        dch = new tlCompositeDrawableChunk16(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlBreakableObjectChunk

unsigned long 
tlBreakableObjectChunk::BreakableType()
{
    return breakabletype;
}

void 
tlBreakableObjectChunk::SetBreakableType(unsigned long val)
{
    breakabletype = val;
}

unsigned long 
tlBreakableObjectChunk::MaxInstances()
{
    return maxinstances;
}

void 
tlBreakableObjectChunk::SetMaxInstances(unsigned long val)
{
    maxinstances = val;
}

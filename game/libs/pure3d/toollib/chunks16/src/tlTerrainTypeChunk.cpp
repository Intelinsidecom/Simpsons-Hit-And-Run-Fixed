// Automatically generated by Jeeves, don't edit.  Edit tlTerrainTypeChunk.sc instead

#include "tlTerrainTypeChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlTerrainTypeChunk.sc


// tlTerrainTypeChunk

int tlTerrainTypeChunk::sortPriority = -1;

tlTerrainTypeChunk::tlTerrainTypeChunk()
{
    Init();
}

tlTerrainTypeChunk::tlTerrainTypeChunk(const tlTerrainTypeChunk& ch)
{
    Init();
    *this = ch;
}

tlTerrainTypeChunk::~tlTerrainTypeChunk()
{

    if(types != NULL){
        delete[] types;
    }
}

tlTerrainTypeChunk::tlTerrainTypeChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::TERRAIN_TYPE);
    ident = SRR2::ChunkID::TERRAIN_TYPE;

    tlFile* f = ch->GetFile();
    unsigned int i;

    version = f->GetLong();
    numtypes = f->GetLong();
    types = new unsigned char[numtypes];
    for(i = 0; i < numtypes; i++)
    {
        types[i] = f->GetChar();
    }
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::TERRAIN_TYPE);
    }
}

void
tlTerrainTypeChunk::Init()
{
    ident = SRR2::ChunkID::TERRAIN_TYPE;
    version = 0;
    numtypes = 0;
    types = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::TERRAIN_TYPE);
    }
}

long 
tlTerrainTypeChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( version );
    s += sizeof( numtypes );
    for(i = 0; i < (int)numtypes; i++)
    {
        s += sizeof( types[i ] );
    }
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlTerrainTypeChunk&
tlTerrainTypeChunk::operator=(const tlTerrainTypeChunk& a)
{
    unsigned int i;

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    version = a.version;
    numtypes = a.numtypes;
    if(types)
    {
        delete types;
    }
    types = new unsigned char[numtypes];
    for( i = 0; i < numtypes ; i++ )
    {
        types[i] = a.types[i];
    }

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlTerrainTypeChunk::operator==(const tlDataChunk& a)
{
    unsigned int i;

    const tlTerrainTypeChunk* aptr = dynamic_cast<const tlTerrainTypeChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(version == aptr->version))
    {
        return false;
    }
    if(!(numtypes == aptr->numtypes))
    {
        return false;
    }
    for(i = 0; i < numtypes; i++)
    {
        if(!(types[i] == aptr->types[i]))
        {
            return false;
        }
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlTerrainTypeChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlTerrainTypeChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlTerrainTypeChunk(ch);
    return dtCh;
}

void
tlTerrainTypeChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

    unsigned int i;
        f->PutLong(version);
        f->PutLong(numtypes);
    for(i = 0; i < numtypes ; i++)
    {
        f->PutChar(types[i]);
    }

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlTerrainTypeChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{
    unsigned int i;

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlTerrainTypeChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Version  = %ld\n",indent,"",version);
    printf("%*s  NumTypes = %ld\n",indent,"",numtypes);
    printf("%*s  Types:\n",indent,"");
    for( i=0; i < numtypes; i++)
    {
         printf("%*s    %d %d\n",indent,"",i,types[i]);
    }

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlTerrainTypeChunk::PrintFormatted(int indent)
{
    unsigned int i;
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # Version \n",indent,"",version);
    printf("%*s  int   %12ld # NumTypes\n",indent,"",numtypes);
    printf("%*s  repeat 1 byte  # Types   \n",indent,"");
    for( i=0; i < numtypes; i++)
    {
         printf("%*s    %12d\n",indent,"",types[i]);
    }
    printf("%*s  endrepeat\n",indent,"");
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlTerrainTypeChunk::GetType() const
{
    return "tlTerrainTypeChunk";
}

int 
tlTerrainTypeChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlTerrainTypeChunk::GetFieldIsArray(int fieldIndex) const
{

    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return false;

        case 2:
            return true;

        default:
            return false;
    }
}

int 
tlTerrainTypeChunk::GetFieldArrayCount(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 2:
            return numtypes;

        default:
            return 0;
    }
}

const char* 
tlTerrainTypeChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "ULONG";

        case 2:
            return "UBYTE";

        default:
            return 0;
    }
}

const char* 
tlTerrainTypeChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Version";

        case 1:
            return "NumTypes";

        case 2:
            return "Types";

        default:
            return 0;
    }
}

bool 
tlTerrainTypeChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", numtypes);
            strncpy(val, buf, len);
            return true;

        case 2:
            return false;

        default:
            return false;
    }
}

bool 
tlTerrainTypeChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:        
            return false;

        case 1:        
            return false;

        case 2:        


            sprintf(buf, "%d", types[arrayIndex]);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlTerrainTypeChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlTerrainTypeChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numtypes = tmp;
                return true;
            }
            return false;
        }
        case 2:
            return false;
        default:
            return false;
    }
}

bool 
tlTerrainTypeChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return false;

        case 2:
        {
            char* end_ptr;
            unsigned char tmp;
            tmp = (unsigned char)strtol(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                types[arrayIndex] = tmp;
                return true;
            }
            return false;
        }

        default:
            return false;
    }
}

tlDataChunk*
tlTerrainTypeChunk::Copy()
{
    return new tlTerrainTypeChunk(*this);
}

tlDataChunk*
tlTerrainTypeChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlTerrainTypeChunk

unsigned long 
tlTerrainTypeChunk::Version()
{
    return version;
}

void 
tlTerrainTypeChunk::SetVersion(unsigned long val)
{
    version = val;
}

unsigned long 
tlTerrainTypeChunk::NumTypes()
{
    return numtypes;
}

void 
tlTerrainTypeChunk::SetNumTypes(unsigned long val)
{
    numtypes = val;
}

unsigned char* 
tlTerrainTypeChunk::Types()
{
    return types;
}

void 
tlTerrainTypeChunk::SetTypes(unsigned char* val, int count)
{
    if(types)
    {
        delete[] types;
    }      
    unsigned char* newval = new unsigned char[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = val[i];
    }
    types = newval;
}

// Automatically generated by Jeeves, don't edit.  Edit tlRoadSegmentChunk.sc instead

#include "tlRoadSegmentChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlRoadSegmentChunk.sc


// tlRoadSegmentChunk

int tlRoadSegmentChunk::sortPriority = -1;

tlRoadSegmentChunk::tlRoadSegmentChunk()
{
    Init();
}

tlRoadSegmentChunk::tlRoadSegmentChunk(const tlRoadSegmentChunk& ch)
{
    Init();
    *this = ch;
}

tlRoadSegmentChunk::~tlRoadSegmentChunk()
{

    if(name != NULL){
        strdelete(name);
    }
    if(roadsegmentdata != NULL){
        strdelete(roadsegmentdata);
    }
}

tlRoadSegmentChunk::tlRoadSegmentChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::ROAD_SEGMENT);
    ident = SRR2::ChunkID::ROAD_SEGMENT;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    f->GetPString(buf);
    roadsegmentdata = strnew(buf);
    hierarchymatrix.Read(f);
    scalematrix.Read(f);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::ROAD_SEGMENT);
    }
}

void
tlRoadSegmentChunk::Init()
{
    ident = SRR2::ChunkID::ROAD_SEGMENT;
    name = NULL;
    roadsegmentdata = NULL;
    hierarchymatrix.Init( );
    scalematrix.Init( );
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::ROAD_SEGMENT);
    }
}

long 
tlRoadSegmentChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += strlen( roadsegmentdata );
    s += sizeof( hierarchymatrix );
    s += sizeof( scalematrix );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlRoadSegmentChunk&
tlRoadSegmentChunk::operator=(const tlRoadSegmentChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    roadsegmentdata = strnew(a.roadsegmentdata);
    hierarchymatrix = a.hierarchymatrix;
    scalematrix = a.scalematrix;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlRoadSegmentChunk::operator==(const tlDataChunk& a)
{

    const tlRoadSegmentChunk* aptr = dynamic_cast<const tlRoadSegmentChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(strcmp(roadsegmentdata, aptr->roadsegmentdata) != 0)
    {
        return false;
    }
    if(!(hierarchymatrix == aptr->hierarchymatrix))
    {
        return false;
    }
    if(!(scalematrix == aptr->scalematrix))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlRoadSegmentChunk::NameCompare(const tlDataChunk& a)
{

    const tlRoadSegmentChunk* aptr = dynamic_cast<const tlRoadSegmentChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlRoadSegmentChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlRoadSegmentChunk(ch);
    return dtCh;
}

void
tlRoadSegmentChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutPString(roadsegmentdata);
        hierarchymatrix.Write(f);
        scalematrix.Write(f);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlRoadSegmentChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlRoadSegmentChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name            = \"%s\"\n",indent,"",name);
    printf("%*s  RoadSegmentData = \"%s\"\n",indent,"",roadsegmentdata);
    printf("%*s  HierarchyMatrix:\n",indent,"");
    hierarchymatrix.Print(0, indent+4);
    printf("%*s  ScaleMatrix    :\n",indent,"");
    scalematrix.Print(0, indent+4);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlRoadSegmentChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name           \n",indent,"",name);
    printf("%*s  pstring \"%s\" # RoadSegmentData\n",indent,"",roadsegmentdata);
    printf("%*s  # HierarchyMatrix:\n",indent,"");
    hierarchymatrix.PrintFormatted(0, indent+4);
    printf("%*s  # ScaleMatrix    :\n",indent,"");
    scalematrix.PrintFormatted(0, indent+4);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlRoadSegmentChunk::GetType() const
{
    return "tlRoadSegmentChunk";
}

int 
tlRoadSegmentChunk::GetFieldCount() const
{
    return 4;
}

bool 
tlRoadSegmentChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlRoadSegmentChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlRoadSegmentChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "string";

        case 2:
            return "tlMatrix";

        case 3:
            return "tlMatrix";

        default:
            return 0;
    }
}

const char* 
tlRoadSegmentChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "RoadSegmentData";

        case 2:
            return "HierarchyMatrix";

        case 3:
            return "ScaleMatrix";

        default:
            return 0;
    }
}

bool 
tlRoadSegmentChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%s", roadsegmentdata);
            strncpy(val, buf, len);
            return true;

        case 2:
            return hierarchymatrix.GetFieldValue(val, len);

        case 3:
            return scalematrix.GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlRoadSegmentChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlRoadSegmentChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return tlMatrix::GetFieldUpdatable();

        case 3:     
            return tlMatrix::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlRoadSegmentChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
            strdelete(roadsegmentdata);
            roadsegmentdata = strnew(new_val);
            return true;
        case 2:
            return hierarchymatrix.SetFieldValue(new_val);
        case 3:
            return scalematrix.SetFieldValue(new_val);
        default:
            return false;
    }
}

bool 
tlRoadSegmentChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlRoadSegmentChunk::Copy()
{
    return new tlRoadSegmentChunk(*this);
}

tlDataChunk*
tlRoadSegmentChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlRoadSegmentChunk

const char* 
tlRoadSegmentChunk::Name()
{
    return name;
}

void 
tlRoadSegmentChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

const char* 
tlRoadSegmentChunk::RoadSegmentData()
{
    return roadsegmentdata;
}

void 
tlRoadSegmentChunk::SetRoadSegmentData(const char* val)
{
    strdelete(roadsegmentdata);
    roadsegmentdata = strnew(val);
}

tlMatrix 
tlRoadSegmentChunk::HierarchyMatrix()
{
    return hierarchymatrix;
}

void 
tlRoadSegmentChunk::SetHierarchyMatrix(tlMatrix val)
{
    hierarchymatrix = val;
}

tlMatrix 
tlRoadSegmentChunk::ScaleMatrix()
{
    return scalematrix;
}

void 
tlRoadSegmentChunk::SetScaleMatrix(tlMatrix val)
{
    scalematrix = val;
}

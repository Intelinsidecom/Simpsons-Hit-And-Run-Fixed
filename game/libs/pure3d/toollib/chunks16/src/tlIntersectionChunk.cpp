// Automatically generated by Jeeves, don't edit.  Edit tlIntersectionChunk.sc instead

#include "tlIntersectionChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlIntersectionChunk.sc


// tlIntersectionChunk

int tlIntersectionChunk::sortPriority = -1;

tlIntersectionChunk::tlIntersectionChunk()
{
    Init();
}

tlIntersectionChunk::tlIntersectionChunk(const tlIntersectionChunk& ch)
{
    Init();
    *this = ch;
}

tlIntersectionChunk::~tlIntersectionChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlIntersectionChunk::tlIntersectionChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::INTERSECTION);
    ident = SRR2::ChunkID::INTERSECTION;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    centre.Read(f);
    radius = f->GetFloat();
    type = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::INTERSECTION);
    }
}

void
tlIntersectionChunk::Init()
{
    ident = SRR2::ChunkID::INTERSECTION;
    name = NULL;
    centre.Init( );
    radius = 0.0f;
    type = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::INTERSECTION);
    }
}

long 
tlIntersectionChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( centre );
    s += sizeof( radius );
    s += sizeof( type );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlIntersectionChunk&
tlIntersectionChunk::operator=(const tlIntersectionChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    centre = a.centre;
    radius = a.radius;
    type = a.type;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlIntersectionChunk::operator==(const tlDataChunk& a)
{

    const tlIntersectionChunk* aptr = dynamic_cast<const tlIntersectionChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(centre == aptr->centre))
    {
        return false;
    }
    if(!(radius == aptr->radius))
    {
        return false;
    }
    if(!(type == aptr->type))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlIntersectionChunk::NameCompare(const tlDataChunk& a)
{

    const tlIntersectionChunk* aptr = dynamic_cast<const tlIntersectionChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlIntersectionChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlIntersectionChunk(ch);
    return dtCh;
}

void
tlIntersectionChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        centre.Write(f);
        f->PutFloat(radius);
        f->PutLong(type);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlIntersectionChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlIntersectionChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name   = \"%s\"\n",indent,"",name);
    printf("%*s  Centre:\n",indent,"");
    centre.Print(0, indent+4);
    printf("%*s  Radius = %f\n",indent,"",radius);
    printf("%*s  Type   = %ld\n",indent,"",type);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlIntersectionChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name  \n",indent,"",name);
    printf("%*s  # Centre:\n",indent,"");
    centre.PrintFormatted(0, indent+4);
    printf("%*s  float %12f # Radius\n",indent,"",radius);
    printf("%*s  int   %12ld # Type  \n",indent,"",type);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlIntersectionChunk::GetType() const
{
    return "tlIntersectionChunk";
}

int 
tlIntersectionChunk::GetFieldCount() const
{
    return 4;
}

bool 
tlIntersectionChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlIntersectionChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlIntersectionChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "tlPoint";

        case 2:
            return "float";

        case 3:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlIntersectionChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Centre";

        case 2:
            return "Radius";

        case 3:
            return "Type";

        default:
            return 0;
    }
}

bool 
tlIntersectionChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            return centre.GetFieldValue(val, len);

        case 2:
            sprintf(buf, "%f", radius);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%ld", type);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlIntersectionChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlIntersectionChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return tlPoint::GetFieldUpdatable();

        case 2:     
            return true;

        case 3:     
            return true;

        default:
            return false;
    }
}

bool 
tlIntersectionChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
            return centre.SetFieldValue(new_val);
        case 2:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                radius = tmp;
                return true;
            }
            return false;
        }
        case 3:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                type = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlIntersectionChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlIntersectionChunk::Copy()
{
    return new tlIntersectionChunk(*this);
}

tlDataChunk*
tlIntersectionChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlIntersectionChunk

const char* 
tlIntersectionChunk::Name()
{
    return name;
}

void 
tlIntersectionChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

tlPoint 
tlIntersectionChunk::Centre()
{
    return centre;
}

void 
tlIntersectionChunk::SetCentre(tlPoint val)
{
    centre = val;
}

float 
tlIntersectionChunk::Radius()
{
    return radius;
}

void 
tlIntersectionChunk::SetRadius(float val)
{
    radius = val;
}

unsigned long 
tlIntersectionChunk::Type()
{
    return type;
}

void 
tlIntersectionChunk::SetType(unsigned long val)
{
    type = val;
}

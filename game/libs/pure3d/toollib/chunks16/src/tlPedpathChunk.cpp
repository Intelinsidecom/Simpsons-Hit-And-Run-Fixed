// Automatically generated by Jeeves, don't edit.  Edit tlPedpathChunk.sc instead

#include "tlPedpathChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlPedpathChunk.sc


// tlPedpathChunk

int tlPedpathChunk::sortPriority = -1;

tlPedpathChunk::tlPedpathChunk()
{
    Init();
}

tlPedpathChunk::tlPedpathChunk(const tlPedpathChunk& ch)
{
    Init();
    *this = ch;
}

tlPedpathChunk::~tlPedpathChunk()
{

    if(points != NULL){
        delete[] points;
    }
}

tlPedpathChunk::tlPedpathChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::PED_PATH);
    ident = SRR2::ChunkID::PED_PATH;

    tlFile* f = ch->GetFile();
    unsigned int i;

    numpoints = f->GetLong();
    points = new tlPoint[numpoints];
    for(i = 0; i < numpoints; i++)
    {
        points[i].Read(f);
    }
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::PED_PATH);
    }
}

void
tlPedpathChunk::Init()
{
    ident = SRR2::ChunkID::PED_PATH;
    numpoints = 0;
    points = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::PED_PATH);
    }
}

long 
tlPedpathChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( numpoints );
    for(i = 0; i < (int)numpoints; i++)
    {
        s += sizeof( points[i ] );
    }
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlPedpathChunk&
tlPedpathChunk::operator=(const tlPedpathChunk& a)
{
    unsigned int i;

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    numpoints = a.numpoints;
    if(points)
    {
        delete points;
    }
    points = new tlPoint[numpoints];
    for( i = 0; i < numpoints ; i++ )
    {
        points[i] = a.points[i];
    }

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlPedpathChunk::operator==(const tlDataChunk& a)
{
    unsigned int i;

    const tlPedpathChunk* aptr = dynamic_cast<const tlPedpathChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(numpoints == aptr->numpoints))
    {
        return false;
    }
    for(i = 0; i < numpoints; i++)
    {
        if(!(points[i] == aptr->points[i]))
        {
            return false;
        }
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlPedpathChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlPedpathChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlPedpathChunk(ch);
    return dtCh;
}

void
tlPedpathChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

    unsigned int i;
        f->PutLong(numpoints);
    for(i = 0; i < numpoints ; i++)
    {
        points[i].Write(f);
    }

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlPedpathChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{
    unsigned int i;

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlPedpathChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  NumPoints = %ld\n",indent,"",numpoints);
    printf("%*s  Points:\n",indent,"");
    for( i=0; i < numpoints; i++)
    {
         points[i].Print(i, indent + 4);
    }

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlPedpathChunk::PrintFormatted(int indent)
{
    unsigned int i;
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # NumPoints\n",indent,"",numpoints);
    printf("%*s  # Points   \n",indent,""); 
    for( i=0; i < numpoints; i++)
    {
         points[i].PrintFormatted(i,indent+4);
    }
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlPedpathChunk::GetType() const
{
    return "tlPedpathChunk";
}

int 
tlPedpathChunk::GetFieldCount() const
{
    return 2;
}

bool 
tlPedpathChunk::GetFieldIsArray(int fieldIndex) const
{

    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return true;

        default:
            return false;
    }
}

int 
tlPedpathChunk::GetFieldArrayCount(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 1:
            return numpoints;

        default:
            return 0;
    }
}

const char* 
tlPedpathChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "tlPoint";

        default:
            return 0;
    }
}

const char* 
tlPedpathChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "NumPoints";

        case 1:
            return "Points";

        default:
            return 0;
    }
}

bool 
tlPedpathChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", numpoints);
            strncpy(val, buf, len);
            return true;

        case 1:
            return false;

        default:
            return false;
    }
}

bool 
tlPedpathChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    switch(fieldIndex)
    {
        case 0:        
            return false;

        case 1:        


            return points[arrayIndex].GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlPedpathChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return tlPoint::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlPedpathChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numpoints = tmp;
                return true;
            }
            return false;
        }
        case 1:
            return false;
        default:
            return false;
    }
}

bool 
tlPedpathChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return points[arrayIndex].SetFieldValue(new_val);

        default:
            return false;
    }
}

tlDataChunk*
tlPedpathChunk::Copy()
{
    return new tlPedpathChunk(*this);
}

tlDataChunk*
tlPedpathChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlPedpathChunk

unsigned long 
tlPedpathChunk::NumPoints()
{
    return numpoints;
}

void 
tlPedpathChunk::SetNumPoints(unsigned long val)
{
    numpoints = val;
}

tlPoint* 
tlPedpathChunk::Points()
{
    return points;
}

void 
tlPedpathChunk::SetPoints(tlPoint* val, int count)
{
    if(points)
    {
        delete[] points;
    }      
    tlPoint* newval = new tlPoint[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = val[i];
    }
    points = newval;
}

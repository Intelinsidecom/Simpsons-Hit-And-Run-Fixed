// Automatically generated by Jeeves, don't edit.  Edit tlFollowCamDataChunk.sc instead

#include "tlFollowCamDataChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlFollowCamDataChunk.sc


// tlFollowCamDataChunk

int tlFollowCamDataChunk::sortPriority = -1;

tlFollowCamDataChunk::tlFollowCamDataChunk()
{
    Init();
}

tlFollowCamDataChunk::tlFollowCamDataChunk(const tlFollowCamDataChunk& ch)
{
    Init();
    *this = ch;
}

tlFollowCamDataChunk::~tlFollowCamDataChunk()
{

}

tlFollowCamDataChunk::tlFollowCamDataChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::FOLLOWCAM);
    ident = SRR2::ChunkID::FOLLOWCAM;

    tlFile* f = ch->GetFile();

    id = f->GetLong();
    rotation = f->GetFloat();
    elevation = f->GetFloat();
    magnitude = f->GetFloat();
    targetoffset.Read(f);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::FOLLOWCAM);
    }
}

void
tlFollowCamDataChunk::Init()
{
    ident = SRR2::ChunkID::FOLLOWCAM;
    id = 0;
    rotation = 0.0f;
    elevation = 0.0f;
    magnitude = 0.0f;
    targetoffset.Init( );
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::FOLLOWCAM);
    }
}

long 
tlFollowCamDataChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( id );
    s += sizeof( rotation );
    s += sizeof( elevation );
    s += sizeof( magnitude );
    s += sizeof( targetoffset );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlFollowCamDataChunk&
tlFollowCamDataChunk::operator=(const tlFollowCamDataChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    id = a.id;
    rotation = a.rotation;
    elevation = a.elevation;
    magnitude = a.magnitude;
    targetoffset = a.targetoffset;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlFollowCamDataChunk::operator==(const tlDataChunk& a)
{

    const tlFollowCamDataChunk* aptr = dynamic_cast<const tlFollowCamDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(id == aptr->id))
    {
        return false;
    }
    if(!(rotation == aptr->rotation))
    {
        return false;
    }
    if(!(elevation == aptr->elevation))
    {
        return false;
    }
    if(!(magnitude == aptr->magnitude))
    {
        return false;
    }
    if(!(targetoffset == aptr->targetoffset))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlFollowCamDataChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlFollowCamDataChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlFollowCamDataChunk(ch);
    return dtCh;
}

void
tlFollowCamDataChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutLong(id);
        f->PutFloat(rotation);
        f->PutFloat(elevation);
        f->PutFloat(magnitude);
        targetoffset.Write(f);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlFollowCamDataChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlFollowCamDataChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  ID           = %ld\n",indent,"",id);
    printf("%*s  Rotation     = %f\n",indent,"",rotation);
    printf("%*s  Elevation    = %f\n",indent,"",elevation);
    printf("%*s  Magnitude    = %f\n",indent,"",magnitude);
    printf("%*s  TargetOffset:\n",indent,"");
    targetoffset.Print(0, indent+4);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlFollowCamDataChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # ID          \n",indent,"",id);
    printf("%*s  float %12f # Rotation    \n",indent,"",rotation);
    printf("%*s  float %12f # Elevation   \n",indent,"",elevation);
    printf("%*s  float %12f # Magnitude   \n",indent,"",magnitude);
    printf("%*s  # TargetOffset:\n",indent,"");
    targetoffset.PrintFormatted(0, indent+4);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlFollowCamDataChunk::GetType() const
{
    return "tlFollowCamDataChunk";
}

int 
tlFollowCamDataChunk::GetFieldCount() const
{
    return 5;
}

bool 
tlFollowCamDataChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlFollowCamDataChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlFollowCamDataChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "float";

        case 2:
            return "float";

        case 3:
            return "float";

        case 4:
            return "tlPoint";

        default:
            return 0;
    }
}

const char* 
tlFollowCamDataChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ID";

        case 1:
            return "Rotation";

        case 2:
            return "Elevation";

        case 3:
            return "Magnitude";

        case 4:
            return "TargetOffset";

        default:
            return 0;
    }
}

bool 
tlFollowCamDataChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", id);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%f", rotation);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%f", elevation);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%f", magnitude);
            strncpy(val, buf, len);
            return true;

        case 4:
            return targetoffset.GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlFollowCamDataChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlFollowCamDataChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return tlPoint::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlFollowCamDataChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                id = tmp;
                return true;
            }
            return false;
        }
        case 1:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                rotation = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                elevation = tmp;
                return true;
            }
            return false;
        }
        case 3:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                magnitude = tmp;
                return true;
            }
            return false;
        }
        case 4:
            return targetoffset.SetFieldValue(new_val);
        default:
            return false;
    }
}

bool 
tlFollowCamDataChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlFollowCamDataChunk::Copy()
{
    return new tlFollowCamDataChunk(*this);
}

tlDataChunk*
tlFollowCamDataChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlFollowCamDataChunk

unsigned long 
tlFollowCamDataChunk::ID()
{
    return id;
}

void 
tlFollowCamDataChunk::SetID(unsigned long val)
{
    id = val;
}

float 
tlFollowCamDataChunk::Rotation()
{
    return rotation;
}

void 
tlFollowCamDataChunk::SetRotation(float val)
{
    rotation = val;
}

float 
tlFollowCamDataChunk::Elevation()
{
    return elevation;
}

void 
tlFollowCamDataChunk::SetElevation(float val)
{
    elevation = val;
}

float 
tlFollowCamDataChunk::Magnitude()
{
    return magnitude;
}

void 
tlFollowCamDataChunk::SetMagnitude(float val)
{
    magnitude = val;
}

tlPoint 
tlFollowCamDataChunk::TargetOffset()
{
    return targetoffset;
}

void 
tlFollowCamDataChunk::SetTargetOffset(tlPoint val)
{
    targetoffset = val;
}

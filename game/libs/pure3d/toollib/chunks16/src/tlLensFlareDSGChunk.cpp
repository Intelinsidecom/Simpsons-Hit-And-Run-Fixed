// Automatically generated by Jeeves, don't edit.  Edit tlLensFlareDSGChunk.sc instead

#include "tlLensFlareDSGChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlLensFlareDSGChunk.sc


// tlLensFlareDSGChunk

int tlLensFlareDSGChunk::sortPriority = -1;

tlLensFlareDSGChunk::tlLensFlareDSGChunk()
{
    Init();
}

tlLensFlareDSGChunk::tlLensFlareDSGChunk(const tlLensFlareDSGChunk& ch)
{
    Init();
    *this = ch;
}

tlLensFlareDSGChunk::~tlLensFlareDSGChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlLensFlareDSGChunk::tlLensFlareDSGChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::LENS_FLARE_DSG);
    ident = SRR2::ChunkID::LENS_FLARE_DSG;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    version = f->GetLong();
    numbillboardquads = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::LENS_FLARE_DSG);
    }
}

void
tlLensFlareDSGChunk::Init()
{
    ident = SRR2::ChunkID::LENS_FLARE_DSG;
    name = NULL;
    version = 0;
    numbillboardquads = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::LENS_FLARE_DSG);
    }
}

long 
tlLensFlareDSGChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( version );
    s += sizeof( numbillboardquads );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlLensFlareDSGChunk&
tlLensFlareDSGChunk::operator=(const tlLensFlareDSGChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    version = a.version;
    numbillboardquads = a.numbillboardquads;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlLensFlareDSGChunk::operator==(const tlDataChunk& a)
{

    const tlLensFlareDSGChunk* aptr = dynamic_cast<const tlLensFlareDSGChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(version == aptr->version))
    {
        return false;
    }
    if(!(numbillboardquads == aptr->numbillboardquads))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlLensFlareDSGChunk::NameCompare(const tlDataChunk& a)
{

    const tlLensFlareDSGChunk* aptr = dynamic_cast<const tlLensFlareDSGChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlLensFlareDSGChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlLensFlareDSGChunk(ch);
    return dtCh;
}

void
tlLensFlareDSGChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(version);
        f->PutLong(numbillboardquads);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlLensFlareDSGChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlLensFlareDSGChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name              = \"%s\"\n",indent,"",name);
    printf("%*s  Version           = %ld\n",indent,"",version);
    printf("%*s  NumBillBoardQuads = %ld\n",indent,"",numbillboardquads);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlLensFlareDSGChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name             \n",indent,"",name);
    printf("%*s  int   %12ld # Version          \n",indent,"",version);
    printf("%*s  int   %12ld # NumBillBoardQuads\n",indent,"",numbillboardquads);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlLensFlareDSGChunk::GetType() const
{
    return "tlLensFlareDSGChunk";
}

int 
tlLensFlareDSGChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlLensFlareDSGChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlLensFlareDSGChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlLensFlareDSGChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlLensFlareDSGChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Version";

        case 2:
            return "NumBillBoardQuads";

        default:
            return 0;
    }
}

bool 
tlLensFlareDSGChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", numbillboardquads);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlLensFlareDSGChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlLensFlareDSGChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlLensFlareDSGChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numbillboardquads = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlLensFlareDSGChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlLensFlareDSGChunk::Copy()
{
    return new tlLensFlareDSGChunk(*this);
}

tlDataChunk*
tlLensFlareDSGChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case P3D_COMPOSITE_DRAWABLE:
        dch = new tlCompositeDrawableChunk16(&ch);
        break;
    case Pure3D::BillboardObject::QUAD_GROUP:
        dch = new tlBillboardQuadGroupChunk(&ch);
        break;
    case Pure3D::Mesh::MESH:
        dch = new tlMeshChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlLensFlareDSGChunk

const char* 
tlLensFlareDSGChunk::Name()
{
    return name;
}

void 
tlLensFlareDSGChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlLensFlareDSGChunk::Version()
{
    return version;
}

void 
tlLensFlareDSGChunk::SetVersion(unsigned long val)
{
    version = val;
}

unsigned long 
tlLensFlareDSGChunk::NumBillBoardQuads()
{
    return numbillboardquads;
}

void 
tlLensFlareDSGChunk::SetNumBillBoardQuads(unsigned long val)
{
    numbillboardquads = val;
}

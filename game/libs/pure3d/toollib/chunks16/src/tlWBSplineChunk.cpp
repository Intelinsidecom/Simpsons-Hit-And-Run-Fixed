// Automatically generated by Jeeves, don't edit.  Edit tlWBSplineChunk.sc instead

#include "tlWBSplineChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlWBSplineChunk.sc


// tlWBSplineChunk

int tlWBSplineChunk::sortPriority = -1;

tlWBSplineChunk::tlWBSplineChunk()
{
    Init();
}

tlWBSplineChunk::tlWBSplineChunk(const tlWBSplineChunk& ch)
{
    Init();
    *this = ch;
}

tlWBSplineChunk::~tlWBSplineChunk()
{

    if(name != NULL){
        strdelete(name);
    }
    if(cvs != NULL){
        delete[] cvs;
    }
}

tlWBSplineChunk::tlWBSplineChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::SPLINE);
    ident = SRR2::ChunkID::SPLINE;

    tlFile* f = ch->GetFile();
    unsigned int i;
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    numcvs = f->GetLong();
    cvs = new tlPoint[numcvs];
    for(i = 0; i < numcvs; i++)
    {
        cvs[i].Read(f);
    }
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::SPLINE);
    }
}

void
tlWBSplineChunk::Init()
{
    ident = SRR2::ChunkID::SPLINE;
    name = NULL;
    numcvs = 0;
    cvs = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::SPLINE);
    }
}

long 
tlWBSplineChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( numcvs );
    for(i = 0; i < (int)numcvs; i++)
    {
        s += sizeof( cvs[i ] );
    }
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlWBSplineChunk&
tlWBSplineChunk::operator=(const tlWBSplineChunk& a)
{
    unsigned int i;

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    numcvs = a.numcvs;
    if(cvs)
    {
        delete cvs;
    }
    cvs = new tlPoint[numcvs];
    for( i = 0; i < numcvs ; i++ )
    {
        cvs[i] = a.cvs[i];
    }

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlWBSplineChunk::operator==(const tlDataChunk& a)
{
    unsigned int i;

    const tlWBSplineChunk* aptr = dynamic_cast<const tlWBSplineChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(numcvs == aptr->numcvs))
    {
        return false;
    }
    for(i = 0; i < numcvs; i++)
    {
        if(!(cvs[i] == aptr->cvs[i]))
        {
            return false;
        }
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlWBSplineChunk::NameCompare(const tlDataChunk& a)
{

    const tlWBSplineChunk* aptr = dynamic_cast<const tlWBSplineChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlWBSplineChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlWBSplineChunk(ch);
    return dtCh;
}

void
tlWBSplineChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

    unsigned int i;
        f->PutPString(name);
        f->PutLong(numcvs);
    for(i = 0; i < numcvs ; i++)
    {
        cvs[i].Write(f);
    }

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlWBSplineChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{
    unsigned int i;

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlWBSplineChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name   = \"%s\"\n",indent,"",name);
    printf("%*s  NumCVs = %ld\n",indent,"",numcvs);
    printf("%*s  CVs:\n",indent,"");
    for( i=0; i < numcvs; i++)
    {
         cvs[i].Print(i, indent + 4);
    }

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlWBSplineChunk::PrintFormatted(int indent)
{
    unsigned int i;
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name  \n",indent,"",name);
    printf("%*s  int   %12ld # NumCVs\n",indent,"",numcvs);
    printf("%*s  # CVs   \n",indent,""); 
    for( i=0; i < numcvs; i++)
    {
         cvs[i].PrintFormatted(i,indent+4);
    }
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlWBSplineChunk::GetType() const
{
    return "tlWBSplineChunk";
}

int 
tlWBSplineChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlWBSplineChunk::GetFieldIsArray(int fieldIndex) const
{

    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return false;

        case 2:
            return true;

        default:
            return false;
    }
}

int 
tlWBSplineChunk::GetFieldArrayCount(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 2:
            return numcvs;

        default:
            return 0;
    }
}

const char* 
tlWBSplineChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "tlPoint";

        default:
            return 0;
    }
}

const char* 
tlWBSplineChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "NumCVs";

        case 2:
            return "CVs";

        default:
            return 0;
    }
}

bool 
tlWBSplineChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", numcvs);
            strncpy(val, buf, len);
            return true;

        case 2:
            return false;

        default:
            return false;
    }
}

bool 
tlWBSplineChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    switch(fieldIndex)
    {
        case 0:        
            return false;

        case 1:        
            return false;

        case 2:        


            return cvs[arrayIndex].GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlWBSplineChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return tlPoint::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlWBSplineChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numcvs = tmp;
                return true;
            }
            return false;
        }
        case 2:
            return false;
        default:
            return false;
    }
}

bool 
tlWBSplineChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return false;

        case 2:
            return cvs[arrayIndex].SetFieldValue(new_val);

        default:
            return false;
    }
}

tlDataChunk*
tlWBSplineChunk::Copy()
{
    return new tlWBSplineChunk(*this);
}

tlDataChunk*
tlWBSplineChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case SRR2::ChunkID::RAIL:
        dch = new tlWBRailCamChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlWBSplineChunk

const char* 
tlWBSplineChunk::Name()
{
    return name;
}

void 
tlWBSplineChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlWBSplineChunk::NumCVs()
{
    return numcvs;
}

void 
tlWBSplineChunk::SetNumCVs(unsigned long val)
{
    numcvs = val;
}

tlPoint* 
tlWBSplineChunk::CVs()
{
    return cvs;
}

void 
tlWBSplineChunk::SetCVs(tlPoint* val, int count)
{
    if(cvs)
    {
        delete[] cvs;
    }      
    tlPoint* newval = new tlPoint[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = val[i];
    }
    cvs = newval;
}

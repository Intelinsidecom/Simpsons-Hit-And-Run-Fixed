// Automatically generated by Jeeves, don't edit.  Edit tlEntityChannelChunk16.sc instead

#include "tlEntityChannelChunk16.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlEntityChannelChunk16.sc


// tlEntityChannelChunk16

int tlEntityChannelChunk16::sortPriority = -1;

tlEntityChannelChunk16::tlEntityChannelChunk16()
{
    Init();
}

tlEntityChannelChunk16::tlEntityChannelChunk16(const tlEntityChannelChunk16& ch)
{
    Init();
    *this = ch;
}

tlEntityChannelChunk16::~tlEntityChannelChunk16()
{
    unsigned int i;


    if(times != NULL){
        delete[] times;
    }
    for( i = 0; i < numframes ; i++ )
    {
        if(names[i] != NULL)
        {
            strdelete(names[i]);
        }
    }
    if(names != NULL){
        delete[] names;
    }
}

tlEntityChannelChunk16::tlEntityChannelChunk16(tlReadChunk16* ch)
{
    assert(ch->GetID() == P3D_ENTITY_ANIM_CHANNEL);
    ident = P3D_ENTITY_ANIM_CHANNEL;

    tlFile* f = ch->GetFile();
    unsigned int i;
    char buf[P3DMAXNAME];

    numframes = f->GetLong();
    times = new unsigned short[numframes];
    for(i = 0; i < numframes; i++)
    {
        times[i] = f->GetWord();
    }
    names = new char*[numframes];
    for(i = 0; i < numframes; i++)
    {
        f->GetPString(buf);
        names[i] = strnew(buf);
    }
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_ENTITY_ANIM_CHANNEL);
    }
}

void
tlEntityChannelChunk16::Init()
{
    ident = P3D_ENTITY_ANIM_CHANNEL;
    numframes = 0;
    times = NULL;
    names = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_ENTITY_ANIM_CHANNEL);
    }
}

long 
tlEntityChannelChunk16::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( numframes );
    for(i = 0; i < (int)numframes; i++)
    {
        s += sizeof( times[i ] );
    }
    for(i = 0; i < (int) numframes; i++)
    {
        s += strlen( names[ i] );
    }
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlEntityChannelChunk16&
tlEntityChannelChunk16::operator=(const tlEntityChannelChunk16& a)
{
    unsigned int i;

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    numframes = a.numframes;
    if(times)
    {
        delete times;
    }
    times = new unsigned short[numframes];
    for( i = 0; i < numframes ; i++ )
    {
        times[i] = a.times[i];
    }
    if(names)
    {
        delete names;
    }
    names = new char*[numframes];
    for( i = 0; i < numframes ; i++ )
    {
        names[i] = strnew(a.names[i]);
    }

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlEntityChannelChunk16::operator==(const tlDataChunk& a)
{
    unsigned int i;

    const tlEntityChannelChunk16* aptr = dynamic_cast<const tlEntityChannelChunk16*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(numframes == aptr->numframes))
    {
        return false;
    }
    for(i = 0; i < numframes; i++)
    {
        if(!(times[i] == aptr->times[i]))
        {
            return false;
        }
    }
    for(i = 0; i < numframes; i++)
    {
        if(strcmp(names[i], aptr->names[i]) != 0)
        {
            return false;
        }
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlEntityChannelChunk16::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlEntityChannelChunk16::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlEntityChannelChunk16(ch);
    return dtCh;
}

void
tlEntityChannelChunk16::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

    unsigned int i;
        f->PutLong(numframes);
    for(i = 0; i < numframes ; i++)
    {
        f->PutWord(times[i]);
    }
    for(i = 0; i < numframes ; i++)
    {
        f->PutPString(names[i]);
    }

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlEntityChannelChunk16::Print(int indent, unsigned int printdepth, unsigned int typemask)
{
    unsigned int i;

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlEntityChannelChunk16 (%4x)\n",indent,"",(int) ident);
    printf("%*s  NumFrames = %ld\n",indent,"",numframes);
    printf("%*s  Times:\n",indent,"");
    for( i=0; i < numframes; i++)
    {
         printf("%*s    %d %d\n",indent,"",i,times[i]);
    }
    printf("%*s  Names:\n",indent,"");
    for( i=0; i < numframes; i++)
    {
         printf("%*s    %d \"%s\"\n",indent,"",i,names[i]);
    }

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlEntityChannelChunk16::PrintFormatted(int indent)
{
    unsigned int i;
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # NumFrames\n",indent,"",numframes);
    printf("%*s  repeat 1 short # Times    \n",indent,"");
    for( i=0; i < numframes; i++)
    {
         printf("%*s    %12d\n",indent,"",times[i]);
    }
    printf("%*s  endrepeat\n",indent,"");
    printf("%*s  repeat 1 pstring # Names    \n",indent,"");
    for( i=0; i < numframes; i++)
    {
         printf("%*s    \"%s\"\n",indent,"",names[i]);
    }
    printf("%*s  endrepeat\n",indent,"");
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlEntityChannelChunk16::GetType() const
{
    return "tlEntityChannelChunk16";
}

int 
tlEntityChannelChunk16::GetFieldCount() const
{
    return 3;
}

bool 
tlEntityChannelChunk16::GetFieldIsArray(int fieldIndex) const
{

    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return true;

        case 2:
            return true;

        default:
            return false;
    }
}

int 
tlEntityChannelChunk16::GetFieldArrayCount(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 1:
            return numframes;

        case 2:
            return numframes;

        default:
            return 0;
    }
}

const char* 
tlEntityChannelChunk16::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "UWORD";

        case 2:
            return "string";

        default:
            return 0;
    }
}

const char* 
tlEntityChannelChunk16::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "NumFrames";

        case 1:
            return "Times";

        case 2:
            return "Names";

        default:
            return 0;
    }
}

bool 
tlEntityChannelChunk16::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", numframes);
            strncpy(val, buf, len);
            return true;

        case 1:
            return false;

        case 2:
            return false;

        default:
            return false;
    }
}

bool 
tlEntityChannelChunk16::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:        
            return false;

        case 1:        


            sprintf(buf, "%d", times[arrayIndex]);
            strncpy(val, buf, len);
            return true;

        case 2:        


            sprintf(buf, "%s", names[arrayIndex]);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlEntityChannelChunk16::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlEntityChannelChunk16::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numframes = tmp;
                return true;
            }
            return false;
        }
        case 1:
            return false;
        case 2:
            return false;
        default:
            return false;
    }
}

bool 
tlEntityChannelChunk16::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
        {
            char* end_ptr;
            unsigned short tmp;
            tmp = (unsigned short)strtol(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                times[arrayIndex] = tmp;
                return true;
            }
            return false;
        }

        case 2:
            if (names[arrayIndex]) strdelete(names[arrayIndex]);
            names[arrayIndex] = strnew(new_val);
            return true;

        default:
            return false;
    }
}

tlDataChunk*
tlEntityChannelChunk16::Copy()
{
    return new tlEntityChannelChunk16(*this);
}

tlDataChunk*
tlEntityChannelChunk16::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlEntityChannelChunk16

unsigned long 
tlEntityChannelChunk16::NumFrames()
{
    return numframes;
}

void 
tlEntityChannelChunk16::SetNumFrames(unsigned long val)
{
    numframes = val;
}

unsigned short* 
tlEntityChannelChunk16::Times()
{
    return times;
}

void 
tlEntityChannelChunk16::SetTimes(unsigned short* val, int count)
{
    if(times)
    {
        delete[] times;
    }      
    unsigned short* newval = new unsigned short[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = val[i];
    }
    times = newval;
}

char** 
tlEntityChannelChunk16::Names()
{
    return names;
}

void 
tlEntityChannelChunk16::SetNames(char** val, int count)
{
    if(names)
    {
        delete[] names;
    }      
    char** newval = new char*[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = strnew(val[i]);
    }
    names = newval;
}

// Automatically generated by Jeeves, don't edit.  Edit tlFencelineChunk.sc instead

#include "tlFencelineChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlFencelineChunk.sc


// tlFenceLineChunk

int tlFenceLineChunk::sortPriority = -1;

tlFenceLineChunk::tlFenceLineChunk()
{
    Init();
}

tlFenceLineChunk::tlFenceLineChunk(const tlFenceLineChunk& ch)
{
    Init();
    *this = ch;
}

tlFenceLineChunk::~tlFenceLineChunk()
{

}

tlFenceLineChunk::tlFenceLineChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::FENCELINE);
    ident = SRR2::ChunkID::FENCELINE;

    tlFile* f = ch->GetFile();

    numwalls = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::FENCELINE);
    }
}

void
tlFenceLineChunk::Init()
{
    ident = SRR2::ChunkID::FENCELINE;
    numwalls = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::FENCELINE);
    }
}

long 
tlFenceLineChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( numwalls );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlFenceLineChunk&
tlFenceLineChunk::operator=(const tlFenceLineChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    numwalls = a.numwalls;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlFenceLineChunk::operator==(const tlDataChunk& a)
{

    const tlFenceLineChunk* aptr = dynamic_cast<const tlFenceLineChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(numwalls == aptr->numwalls))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlFenceLineChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlFenceLineChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlFenceLineChunk(ch);
    return dtCh;
}

void
tlFenceLineChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutLong(numwalls);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlFenceLineChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlFenceLineChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  NumWalls = %ld\n",indent,"",numwalls);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlFenceLineChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # NumWalls\n",indent,"",numwalls);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlFenceLineChunk::GetType() const
{
    return "tlFenceLineChunk";
}

int 
tlFenceLineChunk::GetFieldCount() const
{
    return 1;
}

bool 
tlFenceLineChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlFenceLineChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlFenceLineChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlFenceLineChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "NumWalls";

        default:
            return 0;
    }
}

bool 
tlFenceLineChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", numwalls);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlFenceLineChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlFenceLineChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        default:
            return false;
    }
}

bool 
tlFenceLineChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numwalls = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlFenceLineChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlFenceLineChunk::Copy()
{
    return new tlFenceLineChunk(*this);
}

tlDataChunk*
tlFenceLineChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case SRR2::ChunkID::WALL:
        dch = new tlWallChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlFenceLineChunk

unsigned long 
tlFenceLineChunk::NumWalls()
{
    return numwalls;
}

void 
tlFenceLineChunk::SetNumWalls(unsigned long val)
{
    numwalls = val;
}

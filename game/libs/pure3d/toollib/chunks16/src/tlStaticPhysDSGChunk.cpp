// Automatically generated by Jeeves, don't edit.  Edit tlStaticPhysDSGChunk.sc instead

#include "tlStaticPhysDSGChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlStaticPhysDSGChunk.sc


// tlStaticPhysDSGChunk

int tlStaticPhysDSGChunk::sortPriority = -1;

tlStaticPhysDSGChunk::tlStaticPhysDSGChunk()
{
    Init();
}

tlStaticPhysDSGChunk::tlStaticPhysDSGChunk(const tlStaticPhysDSGChunk& ch)
{
    Init();
    *this = ch;
}

tlStaticPhysDSGChunk::~tlStaticPhysDSGChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlStaticPhysDSGChunk::tlStaticPhysDSGChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::STATIC_PHYS_DSG);
    ident = SRR2::ChunkID::STATIC_PHYS_DSG;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    version = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::STATIC_PHYS_DSG);
    }
}

void
tlStaticPhysDSGChunk::Init()
{
    ident = SRR2::ChunkID::STATIC_PHYS_DSG;
    name = NULL;
    version = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::STATIC_PHYS_DSG);
    }
}

long 
tlStaticPhysDSGChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( version );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlStaticPhysDSGChunk&
tlStaticPhysDSGChunk::operator=(const tlStaticPhysDSGChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    version = a.version;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlStaticPhysDSGChunk::operator==(const tlDataChunk& a)
{

    const tlStaticPhysDSGChunk* aptr = dynamic_cast<const tlStaticPhysDSGChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(version == aptr->version))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlStaticPhysDSGChunk::NameCompare(const tlDataChunk& a)
{

    const tlStaticPhysDSGChunk* aptr = dynamic_cast<const tlStaticPhysDSGChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlStaticPhysDSGChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlStaticPhysDSGChunk(ch);
    return dtCh;
}

void
tlStaticPhysDSGChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(version);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlStaticPhysDSGChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlStaticPhysDSGChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name    = \"%s\"\n",indent,"",name);
    printf("%*s  Version = %ld\n",indent,"",version);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlStaticPhysDSGChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name   \n",indent,"",name);
    printf("%*s  int   %12ld # Version\n",indent,"",version);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlStaticPhysDSGChunk::GetType() const
{
    return "tlStaticPhysDSGChunk";
}

int 
tlStaticPhysDSGChunk::GetFieldCount() const
{
    return 2;
}

bool 
tlStaticPhysDSGChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlStaticPhysDSGChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlStaticPhysDSGChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlStaticPhysDSGChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Version";

        default:
            return 0;
    }
}

bool 
tlStaticPhysDSGChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlStaticPhysDSGChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlStaticPhysDSGChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        default:
            return false;
    }
}

bool 
tlStaticPhysDSGChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlStaticPhysDSGChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlStaticPhysDSGChunk::Copy()
{
    return new tlStaticPhysDSGChunk(*this);
}

tlDataChunk*
tlStaticPhysDSGChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case SRR2::ChunkID::OBJECT_ATTRIBUTES:
        dch = new tlObjectAttributeChunk(&ch);
        break;
    case Simulation::Collision::OBJECT:
        dch = new tlCollisionObjectChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlStaticPhysDSGChunk

const char* 
tlStaticPhysDSGChunk::Name()
{
    return name;
}

void 
tlStaticPhysDSGChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlStaticPhysDSGChunk::Version()
{
    return version;
}

void 
tlStaticPhysDSGChunk::SetVersion(unsigned long val)
{
    version = val;
}

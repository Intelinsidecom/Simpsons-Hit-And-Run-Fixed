// Automatically generated by Jeeves, don't edit.  Edit tlAttributeTableChunk.sc instead

#include "tlAttributeTableChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlAttributeTableChunk.sc
// tlAttributeRow

tlAttributeRow::~tlAttributeRow()
{
    strdelete( sound );
    strdelete( particle );
    strdelete( animation );
}

void
tlAttributeRow::Read(tlFile*f )
{
    char buf[P3DMAXNAME];
    f->GetPString( buf );
    sound = strnew( buf );
    f->GetPString( buf );
    particle = strnew( buf );
    f->GetPString( buf );
    animation = strnew( buf );
    friction = f->GetFloat();        
    mass = f->GetFloat();        
    elasticity = f->GetFloat();        
}

void
tlAttributeRow::Write(tlFile*f )
{
    f->PutPString( sound );
    f->PutPString( particle );
    f->PutPString( animation );
    f->PutFloat( friction );
    f->PutFloat( mass );
    f->PutFloat( elasticity );
}

void
tlAttributeRow::Print(int print_index, int indent)
{
    printf("%*s%d\n", indent, "", (int)print_index);
    printf("%*s  Sound      = \"%s\"\n", indent, "", sound);
    printf("%*s  Particle   = \"%s\"\n", indent, "", particle);
    printf("%*s  Animation  = \"%s\"\n", indent, "", animation);
    printf("%*s  Friction   = %f\n", indent, "", friction);
    printf("%*s  Mass       = %f\n", indent, "", mass);
    printf("%*s  Elasticity = %f\n", indent, "", elasticity);
}

void
tlAttributeRow::PrintFormatted(int print_index, int indent)
{
    printf("%*s pstring \"%s\"  # Sound     \n", indent, "", sound);
    printf("%*s pstring \"%s\"  # Particle  \n", indent, "", particle);
    printf("%*s pstring \"%s\"  # Animation \n", indent, "", animation);
    printf("%*s float %12f  # Friction  \n", indent, "", friction);
    printf("%*s float %12f  # Mass      \n", indent, "", mass);
    printf("%*s float %12f  # Elasticity\n", indent, "", elasticity);
}

const char* 
tlAttributeRow::GetType() const
{
    return "tlAttributeRow";
}

int 
tlAttributeRow::GetFieldCount() const
{
    return 6;
}

bool 
tlAttributeRow::GetFieldValue(char* val, int len) const
{
    char buf[65536];
    int val_length = 0, cur_length;
    bool length_exceeded = false;

    val[0] = 0;

    sprintf(buf, "Sound= %s, ", sound);
    cur_length = strlen(buf);
    if (val_length + cur_length <= len)
    {
        strcat(val, buf);
        val_length += cur_length;
    }
    else
    {
        length_exceeded = true;
    }

    sprintf(buf, "Particle= %s, ", particle);
    cur_length = strlen(buf);
    if (val_length + cur_length <= len)
    {
        strcat(val, buf);
        val_length += cur_length;
    }
    else
    {
        length_exceeded = true;
    }

    sprintf(buf, "Animation= %s, ", animation);
    cur_length = strlen(buf);
    if (val_length + cur_length <= len)
    {
        strcat(val, buf);
        val_length += cur_length;
    }
    else
    {
        length_exceeded = true;
    }

    sprintf(buf, "Friction= %f, ", friction);
    cur_length = strlen(buf);
    if (val_length + cur_length <= len)
    {
        strcat(val, buf);
        val_length += cur_length;
    }
    else
    {
        length_exceeded = true;
    }

    sprintf(buf, "Mass= %f, ", mass);
    cur_length = strlen(buf);
    if (val_length + cur_length <= len)
    {
        strcat(val, buf);
        val_length += cur_length;
    }
    else
    {
        length_exceeded = true;
    }

    sprintf(buf, "Elasticity= %f, ", elasticity);
    cur_length = strlen(buf);
    if (val_length + cur_length <= len)
    {
        strcat(val, buf);
        val_length += cur_length;
    }
    else
    {
        length_exceeded = true;
    }


    if (length_exceeded)
    {
        if (val_length + 3 <= len)
        {
            strcat(val, "...");
        }
    }
    else if ((val_length > 2) && (val[val_length-2] == ','))
    {
        val[val_length-2] = 0;
    }

    return true;
}

bool 
tlAttributeRow::GetFieldUpdatable()
{
    return false;
}

bool 
tlAttributeRow::SetFieldValue(const char* new_val)
{
    return false;
}

void
tlAttributeRow::Init()
{
    sound = NULL;
    particle = NULL;
    animation = NULL;
    friction = 0.0f;
    mass = 0.0f;
    elasticity = 0.0f;

}

tlAttributeRow&
tlAttributeRow::operator=(const tlAttributeRow& a)
{
    sound = strnew(a.sound);
    particle = strnew(a.particle);
    animation = strnew(a.animation);
    friction = a.friction;
    mass = a.mass;
    elasticity = a.elasticity;
    return *this;
}

int
tlAttributeRow::operator==(const tlAttributeRow& a)
{
    if(strcmp(sound, a.sound) != 0)
    if(strcmp(particle, a.particle) != 0)
    if(strcmp(animation, a.animation) != 0)
    if(!(friction == a.friction))
    {
        return false;
    }
    if(!(mass == a.mass))
    {
        return false;
    }
    if(!(elasticity == a.elasticity))
    {
        return false;
    }

    return true;
}



// tlAttributeTableChunk

int tlAttributeTableChunk::sortPriority = -1;

tlAttributeTableChunk::tlAttributeTableChunk()
{
    Init();
}

tlAttributeTableChunk::tlAttributeTableChunk(const tlAttributeTableChunk& ch)
{
    Init();
    *this = ch;
}

tlAttributeTableChunk::~tlAttributeTableChunk()
{

    if(table != NULL){
        delete[] table;
    }
}

tlAttributeTableChunk::tlAttributeTableChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::ATTRIBUTE_TABLE);
    ident = SRR2::ChunkID::ATTRIBUTE_TABLE;

    tlFile* f = ch->GetFile();
    unsigned int i;

    numrows = f->GetLong();
    table = new tlAttributeRow[numrows];
    for(i = 0; i < numrows; i++)
    {
        table[i].Read(f);
    }
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::ATTRIBUTE_TABLE);
    }
}

void
tlAttributeTableChunk::Init()
{
    ident = SRR2::ChunkID::ATTRIBUTE_TABLE;
    numrows = 0;
    table = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::ATTRIBUTE_TABLE);
    }
}

long 
tlAttributeTableChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( numrows );
    for(i = 0; i < (int)numrows; i++)
    {
        s += sizeof( table[i ] );
    }
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlAttributeTableChunk&
tlAttributeTableChunk::operator=(const tlAttributeTableChunk& a)
{
    unsigned int i;

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    numrows = a.numrows;
    if(table)
    {
        delete table;
    }
    table = new tlAttributeRow[numrows];
    for( i = 0; i < numrows ; i++ )
    {
        table[i] = a.table[i];
    }

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlAttributeTableChunk::operator==(const tlDataChunk& a)
{
    unsigned int i;

    const tlAttributeTableChunk* aptr = dynamic_cast<const tlAttributeTableChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(numrows == aptr->numrows))
    {
        return false;
    }
    for(i = 0; i < numrows; i++)
    {
        if(!(table[i] == aptr->table[i]))
        {
            return false;
        }
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlAttributeTableChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlAttributeTableChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlAttributeTableChunk(ch);
    return dtCh;
}

void
tlAttributeTableChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

    unsigned int i;
        f->PutLong(numrows);
    for(i = 0; i < numrows ; i++)
    {
        table[i].Write(f);
    }

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlAttributeTableChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{
    unsigned int i;

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlAttributeTableChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  NumRows = %ld\n",indent,"",numrows);
    printf("%*s  Table:\n",indent,"");
    for( i=0; i < numrows; i++)
    {
         table[i].Print(i, indent + 4);
    }

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlAttributeTableChunk::PrintFormatted(int indent)
{
    unsigned int i;
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # NumRows\n",indent,"",numrows);
    printf("%*s  # Table  \n",indent,""); 
    for( i=0; i < numrows; i++)
    {
         table[i].PrintFormatted(i,indent+4);
    }
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlAttributeTableChunk::GetType() const
{
    return "tlAttributeTableChunk";
}

int 
tlAttributeTableChunk::GetFieldCount() const
{
    return 2;
}

bool 
tlAttributeTableChunk::GetFieldIsArray(int fieldIndex) const
{

    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return true;

        default:
            return false;
    }
}

int 
tlAttributeTableChunk::GetFieldArrayCount(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 1:
            return numrows;

        default:
            return 0;
    }
}

const char* 
tlAttributeTableChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "tlAttributeRow";

        default:
            return 0;
    }
}

const char* 
tlAttributeTableChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "NumRows";

        case 1:
            return "Table";

        default:
            return 0;
    }
}

bool 
tlAttributeTableChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", numrows);
            strncpy(val, buf, len);
            return true;

        case 1:
            return false;

        default:
            return false;
    }
}

bool 
tlAttributeTableChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    switch(fieldIndex)
    {
        case 0:        
            return false;

        case 1:        


            return table[arrayIndex].GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlAttributeTableChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return tlAttributeRow::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlAttributeTableChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numrows = tmp;
                return true;
            }
            return false;
        }
        case 1:
            return false;
        default:
            return false;
    }
}

bool 
tlAttributeTableChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return table[arrayIndex].SetFieldValue(new_val);

        default:
            return false;
    }
}

tlDataChunk*
tlAttributeTableChunk::Copy()
{
    return new tlAttributeTableChunk(*this);
}

tlDataChunk*
tlAttributeTableChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlAttributeTableChunk

unsigned long 
tlAttributeTableChunk::NumRows()
{
    return numrows;
}

void 
tlAttributeTableChunk::SetNumRows(unsigned long val)
{
    numrows = val;
}

tlAttributeRow* 
tlAttributeTableChunk::Table()
{
    return table;
}

void 
tlAttributeTableChunk::SetTable(tlAttributeRow* val, int count)
{
    if(table)
    {
        delete[] table;
    }      
    tlAttributeRow* newval = new tlAttributeRow[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = val[i];
    }
    table = newval;
}

// Automatically generated by Jeeves, don't edit.  Edit tlPhotonMapChunk.sc instead

#include "tlPhotonMapChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlPhotonMapChunk.sc


// tlPhotonMapChunk

int tlPhotonMapChunk::sortPriority = -1;

tlPhotonMapChunk::tlPhotonMapChunk()
{
    Init();
}

tlPhotonMapChunk::tlPhotonMapChunk(const tlPhotonMapChunk& ch)
{
    Init();
    *this = ch;
}

tlPhotonMapChunk::~tlPhotonMapChunk()
{
    unsigned int i;


    if(name != NULL){
        strdelete(name);
    }
    for( i = 0; i < numlights ; i++ )
    {
        if(lights[i] != NULL)
        {
            strdelete(lights[i]);
        }
    }
    if(lights != NULL){
        delete[] lights;
    }
    if(lightscales != NULL){
        delete[] lightscales;
    }
    if(photons != NULL){
        delete[] photons;
    }
}

tlPhotonMapChunk::tlPhotonMapChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == Pure3D::Light::PHOTON_MAP);
    ident = Pure3D::Light::PHOTON_MAP;

    tlFile* f = ch->GetFile();
    unsigned int i;
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    version = f->GetLong();
    numlights = f->GetLong();
    lights = new char*[numlights];
    for(i = 0; i < numlights; i++)
    {
        f->GetPString(buf);
        lights[i] = strnew(buf);
    }
    lightscales = new float[numlights];
    for(i = 0; i < numlights; i++)
    {
        lightscales[i] = f->GetFloat();
    }
    numphotons = f->GetLong();
    photons = new tlPhoton[numphotons];
    for(i = 0; i < numphotons; i++)
    {
        photons[i].Read(f);
    }
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Light::PHOTON_MAP);
    }
}

void
tlPhotonMapChunk::Init()
{
    ident = Pure3D::Light::PHOTON_MAP;
    name = NULL;
    version = 0;
    numlights = 0;
    lights = NULL;
    lightscales = NULL;
    numphotons = 0;
    photons = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Light::PHOTON_MAP);
    }
}

long 
tlPhotonMapChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( version );
    s += sizeof( numlights );
    for(i = 0; i < (int) numlights; i++)
    {
        s += strlen( lights[ i] );
    }
    for(i = 0; i < (int)numlights; i++)
    {
        s += sizeof( lightscales[i ] );
    }
    s += sizeof( numphotons );
    for(i = 0; i < (int)numphotons; i++)
    {
        s += sizeof( photons[i ] );
    }
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlPhotonMapChunk&
tlPhotonMapChunk::operator=(const tlPhotonMapChunk& a)
{
    unsigned int i;

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    version = a.version;
    numlights = a.numlights;
    if(lights)
    {
        delete lights;
    }
    lights = new char*[numlights];
    for( i = 0; i < numlights ; i++ )
    {
        lights[i] = strnew(a.lights[i]);
    }
    if(lightscales)
    {
        delete lightscales;
    }
    lightscales = new float[numlights];
    for( i = 0; i < numlights ; i++ )
    {
        lightscales[i] = a.lightscales[i];
    }
    numphotons = a.numphotons;
    if(photons)
    {
        delete photons;
    }
    photons = new tlPhoton[numphotons];
    for( i = 0; i < numphotons ; i++ )
    {
        photons[i] = a.photons[i];
    }

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlPhotonMapChunk::operator==(const tlDataChunk& a)
{
    unsigned int i;

    const tlPhotonMapChunk* aptr = dynamic_cast<const tlPhotonMapChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(version == aptr->version))
    {
        return false;
    }
    if(!(numlights == aptr->numlights))
    {
        return false;
    }
    for(i = 0; i < numlights; i++)
    {
        if(strcmp(lights[i], aptr->lights[i]) != 0)
        {
            return false;
        }
    }
    for(i = 0; i < numlights; i++)
    {
        if(!(lightscales[i] == aptr->lightscales[i]))
        {
            return false;
        }
    }
    if(!(numphotons == aptr->numphotons))
    {
        return false;
    }
    for(i = 0; i < numphotons; i++)
    {
        if(!(photons[i] == aptr->photons[i]))
        {
            return false;
        }
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlPhotonMapChunk::NameCompare(const tlDataChunk& a)
{

    const tlPhotonMapChunk* aptr = dynamic_cast<const tlPhotonMapChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlPhotonMapChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlPhotonMapChunk(ch);
    return dtCh;
}

void
tlPhotonMapChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

    unsigned int i;
        f->PutPString(name);
        f->PutLong(version);
        f->PutLong(numlights);
    for(i = 0; i < numlights ; i++)
    {
        f->PutPString(lights[i]);
    }
    for(i = 0; i < numlights ; i++)
    {
        f->PutFloat(lightscales[i]);
    }
        f->PutLong(numphotons);
    for(i = 0; i < numphotons ; i++)
    {
        photons[i].Write(f);
    }

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlPhotonMapChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{
    unsigned int i;

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlPhotonMapChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name        = \"%s\"\n",indent,"",name);
    printf("%*s  Version     = %ld\n",indent,"",version);
    printf("%*s  NumLights   = %ld\n",indent,"",numlights);
    printf("%*s  Lights:\n",indent,"");
    for( i=0; i < numlights; i++)
    {
         printf("%*s    %d \"%s\"\n",indent,"",i,lights[i]);
    }
    printf("%*s  LightScales:\n",indent,"");
    for( i=0; i < numlights; i++)
    {
         printf("%*s    %d %f\n",indent,"",i,lightscales[i]);
    }
    printf("%*s  NumPhotons  = %ld\n",indent,"",numphotons);
    printf("%*s  Photons:\n",indent,"");
    for( i=0; i < numphotons; i++)
    {
         photons[i].Print(i, indent + 4);
    }

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlPhotonMapChunk::PrintFormatted(int indent)
{
    unsigned int i;
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name       \n",indent,"",name);
    printf("%*s  int   %12ld # Version    \n",indent,"",version);
    printf("%*s  int   %12ld # NumLights  \n",indent,"",numlights);
    printf("%*s  repeat 1 pstring # Lights     \n",indent,"");
    for( i=0; i < numlights; i++)
    {
         printf("%*s    \"%s\"\n",indent,"",lights[i]);
    }
    printf("%*s  endrepeat\n",indent,"");
    printf("%*s  repeat 1 float # LightScales\n",indent,"");
    for( i=0; i < numlights; i++)
    {
         printf("%*s    %12f\n",indent,"",lightscales[i]);
    }
    printf("%*s  endrepeat\n",indent,"");
    printf("%*s  int   %12ld # NumPhotons \n",indent,"",numphotons);
    printf("%*s  # Photons    \n",indent,""); 
    for( i=0; i < numphotons; i++)
    {
         photons[i].PrintFormatted(i,indent+4);
    }
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlPhotonMapChunk::GetType() const
{
    return "tlPhotonMapChunk";
}

int 
tlPhotonMapChunk::GetFieldCount() const
{
    return 7;
}

bool 
tlPhotonMapChunk::GetFieldIsArray(int fieldIndex) const
{

    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return false;

        case 2:
            return false;

        case 3:
            return true;

        case 4:
            return true;

        case 5:
            return false;

        case 6:
            return true;

        default:
            return false;
    }
}

int 
tlPhotonMapChunk::GetFieldArrayCount(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 3:
            return numlights;

        case 4:
            return numlights;

        case 6:
            return numphotons;

        default:
            return 0;
    }
}

const char* 
tlPhotonMapChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        case 3:
            return "string";

        case 4:
            return "float";

        case 5:
            return "ULONG";

        case 6:
            return "tlPhoton";

        default:
            return 0;
    }
}

const char* 
tlPhotonMapChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Version";

        case 2:
            return "NumLights";

        case 3:
            return "Lights";

        case 4:
            return "LightScales";

        case 5:
            return "NumPhotons";

        case 6:
            return "Photons";

        default:
            return 0;
    }
}

bool 
tlPhotonMapChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", numlights);
            strncpy(val, buf, len);
            return true;

        case 3:
            return false;

        case 4:
            return false;

        case 5:
            sprintf(buf, "%ld", numphotons);
            strncpy(val, buf, len);
            return true;

        case 6:
            return false;

        default:
            return false;
    }
}

bool 
tlPhotonMapChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:        
            return false;

        case 1:        
            return false;

        case 2:        
            return false;

        case 3:        


            sprintf(buf, "%s", lights[arrayIndex]);
            strncpy(val, buf, len);
            return true;

        case 4:        


            sprintf(buf, "%f", lightscales[arrayIndex]);
            strncpy(val, buf, len);
            return true;

        case 5:        
            return false;

        case 6:        


            return photons[arrayIndex].GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlPhotonMapChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return true;

        case 5:     
            return true;

        case 6:     
            return tlPhoton::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlPhotonMapChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numlights = tmp;
                return true;
            }
            return false;
        }
        case 3:
            return false;
        case 4:
            return false;
        case 5:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numphotons = tmp;
                return true;
            }
            return false;
        }
        case 6:
            return false;
        default:
            return false;
    }
}

bool 
tlPhotonMapChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return false;

        case 2:
            return false;

        case 3:
            if (lights[arrayIndex]) strdelete(lights[arrayIndex]);
            lights[arrayIndex] = strnew(new_val);
            return true;

        case 4:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                lightscales[arrayIndex] = tmp;
                return true;
            }
            return false;
        }

        case 5:
            return false;

        case 6:
            return photons[arrayIndex].SetFieldValue(new_val);

        default:
            return false;
    }
}

tlDataChunk*
tlPhotonMapChunk::Copy()
{
    return new tlPhotonMapChunk(*this);
}

tlDataChunk*
tlPhotonMapChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlPhotonMapChunk

const char* 
tlPhotonMapChunk::Name()
{
    return name;
}

void 
tlPhotonMapChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlPhotonMapChunk::Version()
{
    return version;
}

void 
tlPhotonMapChunk::SetVersion(unsigned long val)
{
    version = val;
}

unsigned long 
tlPhotonMapChunk::NumLights()
{
    return numlights;
}

void 
tlPhotonMapChunk::SetNumLights(unsigned long val)
{
    numlights = val;
}

char** 
tlPhotonMapChunk::Lights()
{
    return lights;
}

void 
tlPhotonMapChunk::SetLights(char** val, int count)
{
    if(lights)
    {
        delete[] lights;
    }      
    char** newval = new char*[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = strnew(val[i]);
    }
    lights = newval;
}

float* 
tlPhotonMapChunk::LightScales()
{
    return lightscales;
}

void 
tlPhotonMapChunk::SetLightScales(float* val, int count)
{
    if(lightscales)
    {
        delete[] lightscales;
    }      
    float* newval = new float[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = val[i];
    }
    lightscales = newval;
}

unsigned long 
tlPhotonMapChunk::NumPhotons()
{
    return numphotons;
}

void 
tlPhotonMapChunk::SetNumPhotons(unsigned long val)
{
    numphotons = val;
}

tlPhoton* 
tlPhotonMapChunk::Photons()
{
    return photons;
}

void 
tlPhotonMapChunk::SetPhotons(tlPhoton* val, int count)
{
    if(photons)
    {
        delete[] photons;
    }      
    tlPhoton* newval = new tlPhoton[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = val[i];
    }
    photons = newval;
}

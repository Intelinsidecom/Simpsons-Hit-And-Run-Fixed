// Automatically generated by Jeeves, don't edit.  Edit tlBBoxChunk.sc instead

#include "tlBBoxChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlBBoxChunk.sc


// tlBBoxChunk

int tlBBoxChunk::sortPriority = -1;

tlBBoxChunk::tlBBoxChunk()
{
    Init();
}

tlBBoxChunk::tlBBoxChunk(const tlBBoxChunk& ch)
{
    Init();
    *this = ch;
}

tlBBoxChunk::~tlBBoxChunk()
{

}

tlBBoxChunk::tlBBoxChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == Pure3D::Mesh::BOX);
    ident = Pure3D::Mesh::BOX;

    tlFile* f = ch->GetFile();

    box.Read(f);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Mesh::BOX);
    }
}

void
tlBBoxChunk::Init()
{
    ident = Pure3D::Mesh::BOX;
    box.Init( );
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Mesh::BOX);
    }
}

long 
tlBBoxChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( box );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlBBoxChunk&
tlBBoxChunk::operator=(const tlBBoxChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    box = a.box;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlBBoxChunk::operator==(const tlDataChunk& a)
{

    const tlBBoxChunk* aptr = dynamic_cast<const tlBBoxChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(box == aptr->box))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlBBoxChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlBBoxChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlBBoxChunk(ch);
    return dtCh;
}

void
tlBBoxChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        box.Write(f);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlBBoxChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlBBoxChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Box:\n",indent,"");
    box.Print(0, indent+4);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlBBoxChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  # Box:\n",indent,"");
    box.PrintFormatted(0, indent+4);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlBBoxChunk::GetType() const
{
    return "tlBBoxChunk";
}

int 
tlBBoxChunk::GetFieldCount() const
{
    return 1;
}

bool 
tlBBoxChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlBBoxChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlBBoxChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "tlBox";

        default:
            return 0;
    }
}

const char* 
tlBBoxChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Box";

        default:
            return 0;
    }
}

bool 
tlBBoxChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    switch(fieldIndex)
    {
        case 0:
            return box.GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlBBoxChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlBBoxChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return tlBox::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlBBoxChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return box.SetFieldValue(new_val);
        default:
            return false;
    }
}

bool 
tlBBoxChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlBBoxChunk::Copy()
{
    return new tlBBoxChunk(*this);
}

tlDataChunk*
tlBBoxChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlBBoxChunk

tlBox 
tlBBoxChunk::Box()
{
    return box;
}

void 
tlBBoxChunk::SetBox(tlBox val)
{
    box = val;
}

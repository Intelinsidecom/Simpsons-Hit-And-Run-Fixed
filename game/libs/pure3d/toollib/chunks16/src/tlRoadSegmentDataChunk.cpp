// Automatically generated by Jeeves, don't edit.  Edit tlRoadSegmentDataChunk.sc instead

#include "tlRoadSegmentDataChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlRoadSegmentDataChunk.sc


// tlRoadSegmentDataChunk

int tlRoadSegmentDataChunk::sortPriority = -1;

tlRoadSegmentDataChunk::tlRoadSegmentDataChunk()
{
    Init();
}

tlRoadSegmentDataChunk::tlRoadSegmentDataChunk(const tlRoadSegmentDataChunk& ch)
{
    Init();
    *this = ch;
}

tlRoadSegmentDataChunk::~tlRoadSegmentDataChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlRoadSegmentDataChunk::tlRoadSegmentDataChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::ROAD_SEGMENT_DATA);
    ident = SRR2::ChunkID::ROAD_SEGMENT_DATA;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    type = f->GetLong();
    numlanes = f->GetLong();
    hasshoulder = f->GetLong();
    direction.Read(f);
    top.Read(f);
    bottom.Read(f);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::ROAD_SEGMENT_DATA);
    }
}

void
tlRoadSegmentDataChunk::Init()
{
    ident = SRR2::ChunkID::ROAD_SEGMENT_DATA;
    name = NULL;
    type = 0;
    numlanes = 0;
    hasshoulder = 0;
    direction.Init( );
    top.Init( );
    bottom.Init( );
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::ROAD_SEGMENT_DATA);
    }
}

long 
tlRoadSegmentDataChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( type );
    s += sizeof( numlanes );
    s += sizeof( hasshoulder );
    s += sizeof( direction );
    s += sizeof( top );
    s += sizeof( bottom );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlRoadSegmentDataChunk&
tlRoadSegmentDataChunk::operator=(const tlRoadSegmentDataChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    type = a.type;
    numlanes = a.numlanes;
    hasshoulder = a.hasshoulder;
    direction = a.direction;
    top = a.top;
    bottom = a.bottom;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlRoadSegmentDataChunk::operator==(const tlDataChunk& a)
{

    const tlRoadSegmentDataChunk* aptr = dynamic_cast<const tlRoadSegmentDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(type == aptr->type))
    {
        return false;
    }
    if(!(numlanes == aptr->numlanes))
    {
        return false;
    }
    if(!(hasshoulder == aptr->hasshoulder))
    {
        return false;
    }
    if(!(direction == aptr->direction))
    {
        return false;
    }
    if(!(top == aptr->top))
    {
        return false;
    }
    if(!(bottom == aptr->bottom))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlRoadSegmentDataChunk::NameCompare(const tlDataChunk& a)
{

    const tlRoadSegmentDataChunk* aptr = dynamic_cast<const tlRoadSegmentDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlRoadSegmentDataChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlRoadSegmentDataChunk(ch);
    return dtCh;
}

void
tlRoadSegmentDataChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(type);
        f->PutLong(numlanes);
        f->PutLong(hasshoulder);
        direction.Write(f);
        top.Write(f);
        bottom.Write(f);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlRoadSegmentDataChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlRoadSegmentDataChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name        = \"%s\"\n",indent,"",name);
    printf("%*s  Type        = %ld\n",indent,"",type);
    printf("%*s  NumLanes    = %ld\n",indent,"",numlanes);
    printf("%*s  HasShoulder = %ld\n",indent,"",hasshoulder);
    printf("%*s  Direction  :\n",indent,"");
    direction.Print(0, indent+4);
    printf("%*s  Top        :\n",indent,"");
    top.Print(0, indent+4);
    printf("%*s  Bottom     :\n",indent,"");
    bottom.Print(0, indent+4);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlRoadSegmentDataChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name       \n",indent,"",name);
    printf("%*s  int   %12ld # Type       \n",indent,"",type);
    printf("%*s  int   %12ld # NumLanes   \n",indent,"",numlanes);
    printf("%*s  int   %12ld # HasShoulder\n",indent,"",hasshoulder);
    printf("%*s  # Direction  :\n",indent,"");
    direction.PrintFormatted(0, indent+4);
    printf("%*s  # Top        :\n",indent,"");
    top.PrintFormatted(0, indent+4);
    printf("%*s  # Bottom     :\n",indent,"");
    bottom.PrintFormatted(0, indent+4);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlRoadSegmentDataChunk::GetType() const
{
    return "tlRoadSegmentDataChunk";
}

int 
tlRoadSegmentDataChunk::GetFieldCount() const
{
    return 7;
}

bool 
tlRoadSegmentDataChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlRoadSegmentDataChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlRoadSegmentDataChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        case 3:
            return "ULONG";

        case 4:
            return "tlPoint";

        case 5:
            return "tlPoint";

        case 6:
            return "tlPoint";

        default:
            return 0;
    }
}

const char* 
tlRoadSegmentDataChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Type";

        case 2:
            return "NumLanes";

        case 3:
            return "HasShoulder";

        case 4:
            return "Direction";

        case 5:
            return "Top";

        case 6:
            return "Bottom";

        default:
            return 0;
    }
}

bool 
tlRoadSegmentDataChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", type);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", numlanes);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%ld", hasshoulder);
            strncpy(val, buf, len);
            return true;

        case 4:
            return direction.GetFieldValue(val, len);

        case 5:
            return top.GetFieldValue(val, len);

        case 6:
            return bottom.GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlRoadSegmentDataChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlRoadSegmentDataChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return tlPoint::GetFieldUpdatable();

        case 5:     
            return tlPoint::GetFieldUpdatable();

        case 6:     
            return tlPoint::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlRoadSegmentDataChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                type = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numlanes = tmp;
                return true;
            }
            return false;
        }
        case 3:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                hasshoulder = tmp;
                return true;
            }
            return false;
        }
        case 4:
            return direction.SetFieldValue(new_val);
        case 5:
            return top.SetFieldValue(new_val);
        case 6:
            return bottom.SetFieldValue(new_val);
        default:
            return false;
    }
}

bool 
tlRoadSegmentDataChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlRoadSegmentDataChunk::Copy()
{
    return new tlRoadSegmentDataChunk(*this);
}

tlDataChunk*
tlRoadSegmentDataChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlRoadSegmentDataChunk

const char* 
tlRoadSegmentDataChunk::Name()
{
    return name;
}

void 
tlRoadSegmentDataChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlRoadSegmentDataChunk::Type()
{
    return type;
}

void 
tlRoadSegmentDataChunk::SetType(unsigned long val)
{
    type = val;
}

unsigned long 
tlRoadSegmentDataChunk::NumLanes()
{
    return numlanes;
}

void 
tlRoadSegmentDataChunk::SetNumLanes(unsigned long val)
{
    numlanes = val;
}

unsigned long 
tlRoadSegmentDataChunk::HasShoulder()
{
    return hasshoulder;
}

void 
tlRoadSegmentDataChunk::SetHasShoulder(unsigned long val)
{
    hasshoulder = val;
}

tlPoint 
tlRoadSegmentDataChunk::Direction()
{
    return direction;
}

void 
tlRoadSegmentDataChunk::SetDirection(tlPoint val)
{
    direction = val;
}

tlPoint 
tlRoadSegmentDataChunk::Top()
{
    return top;
}

void 
tlRoadSegmentDataChunk::SetTop(tlPoint val)
{
    top = val;
}

tlPoint 
tlRoadSegmentDataChunk::Bottom()
{
    return bottom;
}

void 
tlRoadSegmentDataChunk::SetBottom(tlPoint val)
{
    bottom = val;
}

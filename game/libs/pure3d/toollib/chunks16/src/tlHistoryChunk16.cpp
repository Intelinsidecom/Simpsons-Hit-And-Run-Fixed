// Automatically generated by Jeeves, don't edit.  Edit tlHistoryChunk16.sc instead

#include "tlHistoryChunk16.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlHistoryChunk16.sc


// tlHistoryChunk16

int tlHistoryChunk16::sortPriority = -1;

tlHistoryChunk16::tlHistoryChunk16()
{
    Init();
}

tlHistoryChunk16::tlHistoryChunk16(const tlHistoryChunk16& ch)
{
    Init();
    *this = ch;
}

tlHistoryChunk16::~tlHistoryChunk16()
{
    unsigned int i;


    for( i = 0; i < numlines ; i++ )
    {
        if(history[i] != NULL)
        {
            strdelete(history[i]);
        }
    }
    if(history != NULL){
        delete[] history;
    }
}

tlHistoryChunk16::tlHistoryChunk16(tlReadChunk16* ch)
{
    assert(ch->GetID() == P3D_HISTORY);
    ident = P3D_HISTORY;

    tlFile* f = ch->GetFile();
    unsigned int i;
    char buf[P3DMAXNAME];

    numlines = f->GetWord();
    history = new char*[numlines];
    for(i = 0; i < numlines; i++)
    {
        f->GetPString(buf);
        history[i] = strnew(buf);
    }
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_HISTORY);
    }
}

void
tlHistoryChunk16::Init()
{
    ident = P3D_HISTORY;
    numlines = 0;
    history = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_HISTORY);
    }
}

long 
tlHistoryChunk16::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( numlines );
    for(i = 0; i < (int) numlines; i++)
    {
        s += strlen( history[ i] );
    }
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlHistoryChunk16&
tlHistoryChunk16::operator=(const tlHistoryChunk16& a)
{
    unsigned int i;

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    numlines = a.numlines;
    if(history)
    {
        delete history;
    }
    history = new char*[numlines];
    for( i = 0; i < numlines ; i++ )
    {
        history[i] = strnew(a.history[i]);
    }

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlHistoryChunk16::operator==(const tlDataChunk& a)
{
    unsigned int i;

    const tlHistoryChunk16* aptr = dynamic_cast<const tlHistoryChunk16*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(numlines == aptr->numlines))
    {
        return false;
    }
    for(i = 0; i < numlines; i++)
    {
        if(strcmp(history[i], aptr->history[i]) != 0)
        {
            return false;
        }
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlHistoryChunk16::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlHistoryChunk16::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlHistoryChunk16(ch);
    return dtCh;
}

void
tlHistoryChunk16::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

    unsigned int i;
        f->PutWord(numlines);
    for(i = 0; i < numlines ; i++)
    {
        f->PutPString(history[i]);
    }

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlHistoryChunk16::Print(int indent, unsigned int printdepth, unsigned int typemask)
{
    unsigned int i;

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlHistoryChunk16 (%4x)\n",indent,"",(int) ident);
    printf("%*s  NumLines = %d\n",indent,"",numlines);
    printf("%*s  History:\n",indent,"");
    for( i=0; i < numlines; i++)
    {
         printf("%*s    %d \"%s\"\n",indent,"",i,history[i]);
    }

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlHistoryChunk16::PrintFormatted(int indent)
{
    unsigned int i;
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  short %12d # NumLines\n",indent,"",numlines);
    printf("%*s  repeat 1 pstring # History \n",indent,"");
    for( i=0; i < numlines; i++)
    {
         printf("%*s    \"%s\"\n",indent,"",history[i]);
    }
    printf("%*s  endrepeat\n",indent,"");
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlHistoryChunk16::GetType() const
{
    return "tlHistoryChunk16";
}

int 
tlHistoryChunk16::GetFieldCount() const
{
    return 2;
}

bool 
tlHistoryChunk16::GetFieldIsArray(int fieldIndex) const
{

    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return true;

        default:
            return false;
    }
}

int 
tlHistoryChunk16::GetFieldArrayCount(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 1:
            return numlines;

        default:
            return 0;
    }
}

const char* 
tlHistoryChunk16::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "UWORD";

        case 1:
            return "string";

        default:
            return 0;
    }
}

const char* 
tlHistoryChunk16::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "NumLines";

        case 1:
            return "History";

        default:
            return 0;
    }
}

bool 
tlHistoryChunk16::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%d", numlines);
            strncpy(val, buf, len);
            return true;

        case 1:
            return false;

        default:
            return false;
    }
}

bool 
tlHistoryChunk16::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:        
            return false;

        case 1:        


            sprintf(buf, "%s", history[arrayIndex]);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlHistoryChunk16::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        default:
            return false;
    }
}

bool 
tlHistoryChunk16::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned short tmp;
            tmp = (unsigned short)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numlines = tmp;
                return true;
            }
            return false;
        }
        case 1:
            return false;
        default:
            return false;
    }
}

bool 
tlHistoryChunk16::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            if (history[arrayIndex]) strdelete(history[arrayIndex]);
            history[arrayIndex] = strnew(new_val);
            return true;

        default:
            return false;
    }
}

tlDataChunk*
tlHistoryChunk16::Copy()
{
    return new tlHistoryChunk16(*this);
}

tlDataChunk*
tlHistoryChunk16::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlHistoryChunk16

unsigned short 
tlHistoryChunk16::NumLines()
{
    return numlines;
}

void 
tlHistoryChunk16::SetNumLines(unsigned short val)
{
    numlines = val;
}

char** 
tlHistoryChunk16::History()
{
    return history;
}

void 
tlHistoryChunk16::SetHistory(char** val, int count)
{
    if(history)
    {
        delete[] history;
    }      
    char** newval = new char*[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = strnew(val[i]);
    }
    history = newval;
}

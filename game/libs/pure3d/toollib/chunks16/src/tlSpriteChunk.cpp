// Automatically generated by Jeeves, don't edit.  Edit tlSpriteChunk.sc instead

#include "tlSpriteChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlSpriteChunk.sc


// tlSpriteChunk

int tlSpriteChunk::sortPriority = -1;

tlSpriteChunk::tlSpriteChunk()
{
    Init();
}

tlSpriteChunk::tlSpriteChunk(const tlSpriteChunk& ch)
{
    Init();
    *this = ch;
}

tlSpriteChunk::~tlSpriteChunk()
{

    if(name != NULL){
        strdelete(name);
    }
    if(shader != NULL){
        strdelete(shader);
    }
}

tlSpriteChunk::tlSpriteChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == Pure3D::Texture::SPRITE);
    ident = Pure3D::Texture::SPRITE;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    nativex = f->GetLong();
    nativey = f->GetLong();
    f->GetPString(buf);
    shader = strnew(buf);
    imagewidth = f->GetLong();
    imageheight = f->GetLong();
    imagecount = f->GetLong();
    blitborder = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Texture::SPRITE);
    }
}

void
tlSpriteChunk::Init()
{
    ident = Pure3D::Texture::SPRITE;
    name = NULL;
    nativex = 0;
    nativey = 0;
    shader = NULL;
    imagewidth = 0;
    imageheight = 0;
    imagecount = 0;
    blitborder = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Texture::SPRITE);
    }
}

long 
tlSpriteChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( nativex );
    s += sizeof( nativey );
    s += strlen( shader );
    s += sizeof( imagewidth );
    s += sizeof( imageheight );
    s += sizeof( imagecount );
    s += sizeof( blitborder );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlSpriteChunk&
tlSpriteChunk::operator=(const tlSpriteChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    nativex = a.nativex;
    nativey = a.nativey;
    shader = strnew(a.shader);
    imagewidth = a.imagewidth;
    imageheight = a.imageheight;
    imagecount = a.imagecount;
    blitborder = a.blitborder;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlSpriteChunk::operator==(const tlDataChunk& a)
{

    const tlSpriteChunk* aptr = dynamic_cast<const tlSpriteChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(nativex == aptr->nativex))
    {
        return false;
    }
    if(!(nativey == aptr->nativey))
    {
        return false;
    }
    if(strcmp(shader, aptr->shader) != 0)
    {
        return false;
    }
    if(!(imagewidth == aptr->imagewidth))
    {
        return false;
    }
    if(!(imageheight == aptr->imageheight))
    {
        return false;
    }
    if(!(imagecount == aptr->imagecount))
    {
        return false;
    }
    if(!(blitborder == aptr->blitborder))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlSpriteChunk::NameCompare(const tlDataChunk& a)
{

    const tlSpriteChunk* aptr = dynamic_cast<const tlSpriteChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlSpriteChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlSpriteChunk(ch);
    return dtCh;
}

void
tlSpriteChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(nativex);
        f->PutLong(nativey);
        f->PutPString(shader);
        f->PutLong(imagewidth);
        f->PutLong(imageheight);
        f->PutLong(imagecount);
        f->PutLong(blitborder);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlSpriteChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlSpriteChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name        = \"%s\"\n",indent,"",name);
    printf("%*s  NativeX     = %ld\n",indent,"",nativex);
    printf("%*s  NativeY     = %ld\n",indent,"",nativey);
    printf("%*s  Shader      = \"%s\"\n",indent,"",shader);
    printf("%*s  ImageWidth  = %ld\n",indent,"",imagewidth);
    printf("%*s  ImageHeight = %ld\n",indent,"",imageheight);
    printf("%*s  ImageCount  = %ld\n",indent,"",imagecount);
    printf("%*s  BlitBorder  = %ld\n",indent,"",blitborder);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlSpriteChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name       \n",indent,"",name);
    printf("%*s  int   %12ld # NativeX    \n",indent,"",nativex);
    printf("%*s  int   %12ld # NativeY    \n",indent,"",nativey);
    printf("%*s  pstring \"%s\" # Shader     \n",indent,"",shader);
    printf("%*s  int   %12ld # ImageWidth \n",indent,"",imagewidth);
    printf("%*s  int   %12ld # ImageHeight\n",indent,"",imageheight);
    printf("%*s  int   %12ld # ImageCount \n",indent,"",imagecount);
    printf("%*s  int   %12ld # BlitBorder \n",indent,"",blitborder);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlSpriteChunk::GetType() const
{
    return "tlSpriteChunk";
}

int 
tlSpriteChunk::GetFieldCount() const
{
    return 8;
}

bool 
tlSpriteChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlSpriteChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlSpriteChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        case 3:
            return "string";

        case 4:
            return "ULONG";

        case 5:
            return "ULONG";

        case 6:
            return "ULONG";

        case 7:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlSpriteChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "NativeX";

        case 2:
            return "NativeY";

        case 3:
            return "Shader";

        case 4:
            return "ImageWidth";

        case 5:
            return "ImageHeight";

        case 6:
            return "ImageCount";

        case 7:
            return "BlitBorder";

        default:
            return 0;
    }
}

bool 
tlSpriteChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", nativex);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", nativey);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%s", shader);
            strncpy(val, buf, len);
            return true;

        case 4:
            sprintf(buf, "%ld", imagewidth);
            strncpy(val, buf, len);
            return true;

        case 5:
            sprintf(buf, "%ld", imageheight);
            strncpy(val, buf, len);
            return true;

        case 6:
            sprintf(buf, "%ld", imagecount);
            strncpy(val, buf, len);
            return true;

        case 7:
            sprintf(buf, "%ld", blitborder);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlSpriteChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlSpriteChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return true;

        case 5:     
            return true;

        case 6:     
            return true;

        case 7:     
            return true;

        default:
            return false;
    }
}

bool 
tlSpriteChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                nativex = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                nativey = tmp;
                return true;
            }
            return false;
        }
        case 3:
            strdelete(shader);
            shader = strnew(new_val);
            return true;
        case 4:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                imagewidth = tmp;
                return true;
            }
            return false;
        }
        case 5:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                imageheight = tmp;
                return true;
            }
            return false;
        }
        case 6:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                imagecount = tmp;
                return true;
            }
            return false;
        }
        case 7:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                blitborder = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlSpriteChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlSpriteChunk::Copy()
{
    return new tlSpriteChunk(*this);
}

tlDataChunk*
tlSpriteChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case Pure3D::Texture::IMAGE:
        dch = new tlImageChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlSpriteChunk

const char* 
tlSpriteChunk::Name()
{
    return name;
}

void 
tlSpriteChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlSpriteChunk::NativeX()
{
    return nativex;
}

void 
tlSpriteChunk::SetNativeX(unsigned long val)
{
    nativex = val;
}

unsigned long 
tlSpriteChunk::NativeY()
{
    return nativey;
}

void 
tlSpriteChunk::SetNativeY(unsigned long val)
{
    nativey = val;
}

const char* 
tlSpriteChunk::Shader()
{
    return shader;
}

void 
tlSpriteChunk::SetShader(const char* val)
{
    strdelete(shader);
    shader = strnew(val);
}

unsigned long 
tlSpriteChunk::ImageWidth()
{
    return imagewidth;
}

void 
tlSpriteChunk::SetImageWidth(unsigned long val)
{
    imagewidth = val;
}

unsigned long 
tlSpriteChunk::ImageHeight()
{
    return imageheight;
}

void 
tlSpriteChunk::SetImageHeight(unsigned long val)
{
    imageheight = val;
}

unsigned long 
tlSpriteChunk::ImageCount()
{
    return imagecount;
}

void 
tlSpriteChunk::SetImageCount(unsigned long val)
{
    imagecount = val;
}

unsigned long 
tlSpriteChunk::BlitBorder()
{
    return blitborder;
}

void 
tlSpriteChunk::SetBlitBorder(unsigned long val)
{
    blitborder = val;
}

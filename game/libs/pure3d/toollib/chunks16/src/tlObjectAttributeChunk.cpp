// Automatically generated by Jeeves, don't edit.  Edit tlObjectAttributeChunk.sc instead

#include "tlObjectAttributeChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlObjectAttributeChunk.sc


// tlObjectAttributeChunk

int tlObjectAttributeChunk::sortPriority = -1;

tlObjectAttributeChunk::tlObjectAttributeChunk()
{
    Init();
}

tlObjectAttributeChunk::tlObjectAttributeChunk(const tlObjectAttributeChunk& ch)
{
    Init();
    *this = ch;
}

tlObjectAttributeChunk::~tlObjectAttributeChunk()
{

    if(sound != NULL){
        strdelete(sound);
    }
}

tlObjectAttributeChunk::tlObjectAttributeChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::OBJECT_ATTRIBUTES);
    ident = SRR2::ChunkID::OBJECT_ATTRIBUTES;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    classtype = f->GetLong();
    phypropid = f->GetLong();
    f->GetPString(buf);
    sound = strnew(buf);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::OBJECT_ATTRIBUTES);
    }
}

void
tlObjectAttributeChunk::Init()
{
    ident = SRR2::ChunkID::OBJECT_ATTRIBUTES;
    classtype = 0;
    phypropid = 0;
    sound = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::OBJECT_ATTRIBUTES);
    }
}

long 
tlObjectAttributeChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( classtype );
    s += sizeof( phypropid );
    s += strlen( sound );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlObjectAttributeChunk&
tlObjectAttributeChunk::operator=(const tlObjectAttributeChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    classtype = a.classtype;
    phypropid = a.phypropid;
    sound = strnew(a.sound);

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlObjectAttributeChunk::operator==(const tlDataChunk& a)
{

    const tlObjectAttributeChunk* aptr = dynamic_cast<const tlObjectAttributeChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(classtype == aptr->classtype))
    {
        return false;
    }
    if(!(phypropid == aptr->phypropid))
    {
        return false;
    }
    if(strcmp(sound, aptr->sound) != 0)
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlObjectAttributeChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlObjectAttributeChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlObjectAttributeChunk(ch);
    return dtCh;
}

void
tlObjectAttributeChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutLong(classtype);
        f->PutLong(phypropid);
        f->PutPString(sound);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlObjectAttributeChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlObjectAttributeChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  ClassType = %ld\n",indent,"",classtype);
    printf("%*s  PhyPropID = %ld\n",indent,"",phypropid);
    printf("%*s  Sound     = \"%s\"\n",indent,"",sound);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlObjectAttributeChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # ClassType\n",indent,"",classtype);
    printf("%*s  int   %12ld # PhyPropID\n",indent,"",phypropid);
    printf("%*s  pstring \"%s\" # Sound    \n",indent,"",sound);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlObjectAttributeChunk::GetType() const
{
    return "tlObjectAttributeChunk";
}

int 
tlObjectAttributeChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlObjectAttributeChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlObjectAttributeChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlObjectAttributeChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "ULONG";

        case 2:
            return "string";

        default:
            return 0;
    }
}

const char* 
tlObjectAttributeChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ClassType";

        case 1:
            return "PhyPropID";

        case 2:
            return "Sound";

        default:
            return 0;
    }
}

bool 
tlObjectAttributeChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", classtype);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", phypropid);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%s", sound);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlObjectAttributeChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlObjectAttributeChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlObjectAttributeChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                classtype = tmp;
                return true;
            }
            return false;
        }
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                phypropid = tmp;
                return true;
            }
            return false;
        }
        case 2:
            strdelete(sound);
            sound = strnew(new_val);
            return true;
        default:
            return false;
    }
}

bool 
tlObjectAttributeChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlObjectAttributeChunk::Copy()
{
    return new tlObjectAttributeChunk(*this);
}

tlDataChunk*
tlObjectAttributeChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlObjectAttributeChunk

unsigned long 
tlObjectAttributeChunk::ClassType()
{
    return classtype;
}

void 
tlObjectAttributeChunk::SetClassType(unsigned long val)
{
    classtype = val;
}

unsigned long 
tlObjectAttributeChunk::PhyPropID()
{
    return phypropid;
}

void 
tlObjectAttributeChunk::SetPhyPropID(unsigned long val)
{
    phypropid = val;
}

const char* 
tlObjectAttributeChunk::Sound()
{
    return sound;
}

void 
tlObjectAttributeChunk::SetSound(const char* val)
{
    strdelete(sound);
    sound = strnew(val);
}

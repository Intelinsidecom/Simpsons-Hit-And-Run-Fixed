// Automatically generated by Jeeves, don't edit.  Edit tlBSphereChunk.sc instead

#include "tlBSphereChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlBSphereChunk.sc


// tlBSphereChunk

int tlBSphereChunk::sortPriority = -1;

tlBSphereChunk::tlBSphereChunk()
{
    Init();
}

tlBSphereChunk::tlBSphereChunk(const tlBSphereChunk& ch)
{
    Init();
    *this = ch;
}

tlBSphereChunk::~tlBSphereChunk()
{

}

tlBSphereChunk::tlBSphereChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == Pure3D::Mesh::SPHERE);
    ident = Pure3D::Mesh::SPHERE;

    tlFile* f = ch->GetFile();

    sphere.Read(f);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Mesh::SPHERE);
    }
}

void
tlBSphereChunk::Init()
{
    ident = Pure3D::Mesh::SPHERE;
    sphere.Init( );
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Mesh::SPHERE);
    }
}

long 
tlBSphereChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( sphere );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlBSphereChunk&
tlBSphereChunk::operator=(const tlBSphereChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    sphere = a.sphere;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlBSphereChunk::operator==(const tlDataChunk& a)
{

    const tlBSphereChunk* aptr = dynamic_cast<const tlBSphereChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(sphere == aptr->sphere))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlBSphereChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlBSphereChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlBSphereChunk(ch);
    return dtCh;
}

void
tlBSphereChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        sphere.Write(f);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlBSphereChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlBSphereChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Sphere:\n",indent,"");
    sphere.Print(0, indent+4);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlBSphereChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  # Sphere:\n",indent,"");
    sphere.PrintFormatted(0, indent+4);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlBSphereChunk::GetType() const
{
    return "tlBSphereChunk";
}

int 
tlBSphereChunk::GetFieldCount() const
{
    return 1;
}

bool 
tlBSphereChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlBSphereChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlBSphereChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "tlSphere";

        default:
            return 0;
    }
}

const char* 
tlBSphereChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Sphere";

        default:
            return 0;
    }
}

bool 
tlBSphereChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    switch(fieldIndex)
    {
        case 0:
            return sphere.GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlBSphereChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlBSphereChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return tlSphere::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlBSphereChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return sphere.SetFieldValue(new_val);
        default:
            return false;
    }
}

bool 
tlBSphereChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlBSphereChunk::Copy()
{
    return new tlBSphereChunk(*this);
}

tlDataChunk*
tlBSphereChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlBSphereChunk

tlSphere 
tlBSphereChunk::Sphere()
{
    return sphere;
}

void 
tlBSphereChunk::SetSphere(tlSphere val)
{
    sphere = val;
}

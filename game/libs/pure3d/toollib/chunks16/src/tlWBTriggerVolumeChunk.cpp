// Automatically generated by Jeeves, don't edit.  Edit tlWBTriggerVolumeChunk.sc instead

#include "tlWBTriggerVolumeChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlWBTriggerVolumeChunk.sc


// tlWBTriggerVolumeChunk

int tlWBTriggerVolumeChunk::sortPriority = -1;

tlWBTriggerVolumeChunk::tlWBTriggerVolumeChunk()
{
    Init();
}

tlWBTriggerVolumeChunk::tlWBTriggerVolumeChunk(const tlWBTriggerVolumeChunk& ch)
{
    Init();
    *this = ch;
}

tlWBTriggerVolumeChunk::~tlWBTriggerVolumeChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlWBTriggerVolumeChunk::tlWBTriggerVolumeChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::TRIGGER_VOLUME);
    ident = SRR2::ChunkID::TRIGGER_VOLUME;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    type = f->GetLong();
    scale.Read(f);
    matrix.Read(f);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::TRIGGER_VOLUME);
    }
}

void
tlWBTriggerVolumeChunk::Init()
{
    ident = SRR2::ChunkID::TRIGGER_VOLUME;
    name = NULL;
    type = 0;
    scale.Init( );
    matrix.Init( );
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::TRIGGER_VOLUME);
    }
}

long 
tlWBTriggerVolumeChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( type );
    s += sizeof( scale );
    s += sizeof( matrix );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlWBTriggerVolumeChunk&
tlWBTriggerVolumeChunk::operator=(const tlWBTriggerVolumeChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    type = a.type;
    scale = a.scale;
    matrix = a.matrix;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlWBTriggerVolumeChunk::operator==(const tlDataChunk& a)
{

    const tlWBTriggerVolumeChunk* aptr = dynamic_cast<const tlWBTriggerVolumeChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(type == aptr->type))
    {
        return false;
    }
    if(!(scale == aptr->scale))
    {
        return false;
    }
    if(!(matrix == aptr->matrix))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlWBTriggerVolumeChunk::NameCompare(const tlDataChunk& a)
{

    const tlWBTriggerVolumeChunk* aptr = dynamic_cast<const tlWBTriggerVolumeChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlWBTriggerVolumeChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlWBTriggerVolumeChunk(ch);
    return dtCh;
}

void
tlWBTriggerVolumeChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(type);
        scale.Write(f);
        matrix.Write(f);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlWBTriggerVolumeChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlWBTriggerVolumeChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name   = \"%s\"\n",indent,"",name);
    printf("%*s  Type   = %ld\n",indent,"",type);
    printf("%*s  Scale :\n",indent,"");
    scale.Print(0, indent+4);
    printf("%*s  Matrix:\n",indent,"");
    matrix.Print(0, indent+4);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlWBTriggerVolumeChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name  \n",indent,"",name);
    printf("%*s  int   %12ld # Type  \n",indent,"",type);
    printf("%*s  # Scale :\n",indent,"");
    scale.PrintFormatted(0, indent+4);
    printf("%*s  # Matrix:\n",indent,"");
    matrix.PrintFormatted(0, indent+4);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlWBTriggerVolumeChunk::GetType() const
{
    return "tlWBTriggerVolumeChunk";
}

int 
tlWBTriggerVolumeChunk::GetFieldCount() const
{
    return 4;
}

bool 
tlWBTriggerVolumeChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlWBTriggerVolumeChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlWBTriggerVolumeChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "tlPoint";

        case 3:
            return "tlMatrix";

        default:
            return 0;
    }
}

const char* 
tlWBTriggerVolumeChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Type";

        case 2:
            return "Scale";

        case 3:
            return "Matrix";

        default:
            return 0;
    }
}

bool 
tlWBTriggerVolumeChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", type);
            strncpy(val, buf, len);
            return true;

        case 2:
            return scale.GetFieldValue(val, len);

        case 3:
            return matrix.GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlWBTriggerVolumeChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlWBTriggerVolumeChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return tlPoint::GetFieldUpdatable();

        case 3:     
            return tlMatrix::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlWBTriggerVolumeChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                type = tmp;
                return true;
            }
            return false;
        }
        case 2:
            return scale.SetFieldValue(new_val);
        case 3:
            return matrix.SetFieldValue(new_val);
        default:
            return false;
    }
}

bool 
tlWBTriggerVolumeChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlWBTriggerVolumeChunk::Copy()
{
    return new tlWBTriggerVolumeChunk(*this);
}

tlDataChunk*
tlWBTriggerVolumeChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlWBTriggerVolumeChunk

const char* 
tlWBTriggerVolumeChunk::Name()
{
    return name;
}

void 
tlWBTriggerVolumeChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlWBTriggerVolumeChunk::Type()
{
    return type;
}

void 
tlWBTriggerVolumeChunk::SetType(unsigned long val)
{
    type = val;
}

tlPoint 
tlWBTriggerVolumeChunk::Scale()
{
    return scale;
}

void 
tlWBTriggerVolumeChunk::SetScale(tlPoint val)
{
    scale = val;
}

tlMatrix 
tlWBTriggerVolumeChunk::Matrix()
{
    return matrix;
}

void 
tlWBTriggerVolumeChunk::SetMatrix(tlMatrix val)
{
    matrix = val;
}

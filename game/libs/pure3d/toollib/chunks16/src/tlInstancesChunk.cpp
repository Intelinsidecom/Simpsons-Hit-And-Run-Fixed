// Automatically generated by Jeeves, don't edit.  Edit tlInstancesChunk.sc instead

#include "tlInstancesChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlInstancesChunk.sc


// tlInstancesChunk

int tlInstancesChunk::sortPriority = -1;

tlInstancesChunk::tlInstancesChunk()
{
    Init();
}

tlInstancesChunk::tlInstancesChunk(const tlInstancesChunk& ch)
{
    Init();
    *this = ch;
}

tlInstancesChunk::~tlInstancesChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlInstancesChunk::tlInstancesChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::INSTANCES);
    ident = SRR2::ChunkID::INSTANCES;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::INSTANCES);
    }
}

void
tlInstancesChunk::Init()
{
    ident = SRR2::ChunkID::INSTANCES;
    name = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::INSTANCES);
    }
}

long 
tlInstancesChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlInstancesChunk&
tlInstancesChunk::operator=(const tlInstancesChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlInstancesChunk::operator==(const tlDataChunk& a)
{

    const tlInstancesChunk* aptr = dynamic_cast<const tlInstancesChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlInstancesChunk::NameCompare(const tlDataChunk& a)
{

    const tlInstancesChunk* aptr = dynamic_cast<const tlInstancesChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlInstancesChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlInstancesChunk(ch);
    return dtCh;
}

void
tlInstancesChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlInstancesChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlInstancesChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name = \"%s\"\n",indent,"",name);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlInstancesChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name\n",indent,"",name);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlInstancesChunk::GetType() const
{
    return "tlInstancesChunk";
}

int 
tlInstancesChunk::GetFieldCount() const
{
    return 1;
}

bool 
tlInstancesChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlInstancesChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlInstancesChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        default:
            return 0;
    }
}

const char* 
tlInstancesChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        default:
            return 0;
    }
}

bool 
tlInstancesChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlInstancesChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlInstancesChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        default:
            return false;
    }
}

bool 
tlInstancesChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        default:
            return false;
    }
}

bool 
tlInstancesChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlInstancesChunk::Copy()
{
    return new tlInstancesChunk(*this);
}

tlDataChunk*
tlInstancesChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case Pure3D::SceneGraph::SCENEGRAPH:
        dch = new tlScenegraphChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlInstancesChunk

const char* 
tlInstancesChunk::Name()
{
    return name;
}

void 
tlInstancesChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

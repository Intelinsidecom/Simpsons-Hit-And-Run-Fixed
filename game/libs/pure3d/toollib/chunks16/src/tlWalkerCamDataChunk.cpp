// Automatically generated by Jeeves, don't edit.  Edit tlWalkerCamDataChunk.sc instead

#include "tlWalkerCamDataChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlWalkerCamDataChunk.sc


// tlWalkerCamDataChunk

int tlWalkerCamDataChunk::sortPriority = -1;

tlWalkerCamDataChunk::tlWalkerCamDataChunk()
{
    Init();
}

tlWalkerCamDataChunk::tlWalkerCamDataChunk(const tlWalkerCamDataChunk& ch)
{
    Init();
    *this = ch;
}

tlWalkerCamDataChunk::~tlWalkerCamDataChunk()
{

}

tlWalkerCamDataChunk::tlWalkerCamDataChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::WALKERCAM);
    ident = SRR2::ChunkID::WALKERCAM;

    tlFile* f = ch->GetFile();

    id = f->GetLong();
    minmagnitude = f->GetFloat();
    maxmagnitude = f->GetFloat();
    elevation = f->GetFloat();
    targetoffset.Read(f);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::WALKERCAM);
    }
}

void
tlWalkerCamDataChunk::Init()
{
    ident = SRR2::ChunkID::WALKERCAM;
    id = 0;
    minmagnitude = 0.0f;
    maxmagnitude = 0.0f;
    elevation = 0.0f;
    targetoffset.Init( );
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::WALKERCAM);
    }
}

long 
tlWalkerCamDataChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( id );
    s += sizeof( minmagnitude );
    s += sizeof( maxmagnitude );
    s += sizeof( elevation );
    s += sizeof( targetoffset );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlWalkerCamDataChunk&
tlWalkerCamDataChunk::operator=(const tlWalkerCamDataChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    id = a.id;
    minmagnitude = a.minmagnitude;
    maxmagnitude = a.maxmagnitude;
    elevation = a.elevation;
    targetoffset = a.targetoffset;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlWalkerCamDataChunk::operator==(const tlDataChunk& a)
{

    const tlWalkerCamDataChunk* aptr = dynamic_cast<const tlWalkerCamDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(id == aptr->id))
    {
        return false;
    }
    if(!(minmagnitude == aptr->minmagnitude))
    {
        return false;
    }
    if(!(maxmagnitude == aptr->maxmagnitude))
    {
        return false;
    }
    if(!(elevation == aptr->elevation))
    {
        return false;
    }
    if(!(targetoffset == aptr->targetoffset))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlWalkerCamDataChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlWalkerCamDataChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlWalkerCamDataChunk(ch);
    return dtCh;
}

void
tlWalkerCamDataChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutLong(id);
        f->PutFloat(minmagnitude);
        f->PutFloat(maxmagnitude);
        f->PutFloat(elevation);
        targetoffset.Write(f);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlWalkerCamDataChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlWalkerCamDataChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  ID           = %ld\n",indent,"",id);
    printf("%*s  MinMagnitude = %f\n",indent,"",minmagnitude);
    printf("%*s  MaxMagnitude = %f\n",indent,"",maxmagnitude);
    printf("%*s  Elevation    = %f\n",indent,"",elevation);
    printf("%*s  TargetOffset:\n",indent,"");
    targetoffset.Print(0, indent+4);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlWalkerCamDataChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # ID          \n",indent,"",id);
    printf("%*s  float %12f # MinMagnitude\n",indent,"",minmagnitude);
    printf("%*s  float %12f # MaxMagnitude\n",indent,"",maxmagnitude);
    printf("%*s  float %12f # Elevation   \n",indent,"",elevation);
    printf("%*s  # TargetOffset:\n",indent,"");
    targetoffset.PrintFormatted(0, indent+4);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlWalkerCamDataChunk::GetType() const
{
    return "tlWalkerCamDataChunk";
}

int 
tlWalkerCamDataChunk::GetFieldCount() const
{
    return 5;
}

bool 
tlWalkerCamDataChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlWalkerCamDataChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlWalkerCamDataChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "float";

        case 2:
            return "float";

        case 3:
            return "float";

        case 4:
            return "tlPoint";

        default:
            return 0;
    }
}

const char* 
tlWalkerCamDataChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ID";

        case 1:
            return "MinMagnitude";

        case 2:
            return "MaxMagnitude";

        case 3:
            return "Elevation";

        case 4:
            return "TargetOffset";

        default:
            return 0;
    }
}

bool 
tlWalkerCamDataChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", id);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%f", minmagnitude);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%f", maxmagnitude);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%f", elevation);
            strncpy(val, buf, len);
            return true;

        case 4:
            return targetoffset.GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlWalkerCamDataChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlWalkerCamDataChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return tlPoint::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlWalkerCamDataChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                id = tmp;
                return true;
            }
            return false;
        }
        case 1:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                minmagnitude = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                maxmagnitude = tmp;
                return true;
            }
            return false;
        }
        case 3:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                elevation = tmp;
                return true;
            }
            return false;
        }
        case 4:
            return targetoffset.SetFieldValue(new_val);
        default:
            return false;
    }
}

bool 
tlWalkerCamDataChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlWalkerCamDataChunk::Copy()
{
    return new tlWalkerCamDataChunk(*this);
}

tlDataChunk*
tlWalkerCamDataChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlWalkerCamDataChunk

unsigned long 
tlWalkerCamDataChunk::ID()
{
    return id;
}

void 
tlWalkerCamDataChunk::SetID(unsigned long val)
{
    id = val;
}

float 
tlWalkerCamDataChunk::MinMagnitude()
{
    return minmagnitude;
}

void 
tlWalkerCamDataChunk::SetMinMagnitude(float val)
{
    minmagnitude = val;
}

float 
tlWalkerCamDataChunk::MaxMagnitude()
{
    return maxmagnitude;
}

void 
tlWalkerCamDataChunk::SetMaxMagnitude(float val)
{
    maxmagnitude = val;
}

float 
tlWalkerCamDataChunk::Elevation()
{
    return elevation;
}

void 
tlWalkerCamDataChunk::SetElevation(float val)
{
    elevation = val;
}

tlPoint 
tlWalkerCamDataChunk::TargetOffset()
{
    return targetoffset;
}

void 
tlWalkerCamDataChunk::SetTargetOffset(tlPoint val)
{
    targetoffset = val;
}

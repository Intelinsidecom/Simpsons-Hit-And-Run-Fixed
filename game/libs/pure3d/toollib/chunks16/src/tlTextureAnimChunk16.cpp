// Automatically generated by Jeeves, don't edit.  Edit tlTextureAnimChunk16.sc instead

#include "tlTextureAnimChunk16.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlTextureAnimChunk16.sc


// tlTextureAnimChunk16

int tlTextureAnimChunk16::sortPriority = -1;

tlTextureAnimChunk16::tlTextureAnimChunk16()
{
    Init();
}

tlTextureAnimChunk16::tlTextureAnimChunk16(const tlTextureAnimChunk16& ch)
{
    Init();
    *this = ch;
}

tlTextureAnimChunk16::~tlTextureAnimChunk16()
{

    if(animname != NULL){
        strdelete(animname);
    }
    if(materialname != NULL){
        strdelete(materialname);
    }
}

tlTextureAnimChunk16::tlTextureAnimChunk16(tlReadChunk16* ch)
{
    assert(ch->GetID() == P3D_TEXTURE_ANIM);
    ident = P3D_TEXTURE_ANIM;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    animname = strnew(buf);
    version = f->GetLong();
    f->GetPString(buf);
    materialname = strnew(buf);
    numframes = f->GetLong();
    framerate = f->GetFloat();
    cyclic = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_TEXTURE_ANIM);
    }
}

void
tlTextureAnimChunk16::Init()
{
    ident = P3D_TEXTURE_ANIM;
    animname = NULL;
    version = 0;
    materialname = NULL;
    numframes = 0;
    framerate = 0.0f;
    cyclic = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_TEXTURE_ANIM);
    }
}

long 
tlTextureAnimChunk16::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( animname );
    s += sizeof( version );
    s += strlen( materialname );
    s += sizeof( numframes );
    s += sizeof( framerate );
    s += sizeof( cyclic );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlTextureAnimChunk16&
tlTextureAnimChunk16::operator=(const tlTextureAnimChunk16& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    animname = strnew(a.animname);
    version = a.version;
    materialname = strnew(a.materialname);
    numframes = a.numframes;
    framerate = a.framerate;
    cyclic = a.cyclic;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlTextureAnimChunk16::operator==(const tlDataChunk& a)
{

    const tlTextureAnimChunk16* aptr = dynamic_cast<const tlTextureAnimChunk16*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(animname, aptr->animname) != 0)
    {
        return false;
    }
    if(!(version == aptr->version))
    {
        return false;
    }
    if(strcmp(materialname, aptr->materialname) != 0)
    {
        return false;
    }
    if(!(numframes == aptr->numframes))
    {
        return false;
    }
    if(!(framerate == aptr->framerate))
    {
        return false;
    }
    if(!(cyclic == aptr->cyclic))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlTextureAnimChunk16::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlTextureAnimChunk16::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlTextureAnimChunk16(ch);
    return dtCh;
}

void
tlTextureAnimChunk16::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(animname);
        f->PutLong(version);
        f->PutPString(materialname);
        f->PutLong(numframes);
        f->PutFloat(framerate);
        f->PutLong(cyclic);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlTextureAnimChunk16::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlTextureAnimChunk16 (%4x)\n",indent,"",(int) ident);
    printf("%*s  AnimName     = \"%s\"\n",indent,"",animname);
    printf("%*s  Version      = %ld\n",indent,"",version);
    printf("%*s  MaterialName = \"%s\"\n",indent,"",materialname);
    printf("%*s  NumFrames    = %ld\n",indent,"",numframes);
    printf("%*s  FrameRate    = %f\n",indent,"",framerate);
    printf("%*s  Cyclic       = %ld\n",indent,"",cyclic);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlTextureAnimChunk16::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # AnimName    \n",indent,"",animname);
    printf("%*s  int   %12ld # Version     \n",indent,"",version);
    printf("%*s  pstring \"%s\" # MaterialName\n",indent,"",materialname);
    printf("%*s  int   %12ld # NumFrames   \n",indent,"",numframes);
    printf("%*s  float %12f # FrameRate   \n",indent,"",framerate);
    printf("%*s  int   %12ld # Cyclic      \n",indent,"",cyclic);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlTextureAnimChunk16::GetType() const
{
    return "tlTextureAnimChunk16";
}

int 
tlTextureAnimChunk16::GetFieldCount() const
{
    return 6;
}

bool 
tlTextureAnimChunk16::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlTextureAnimChunk16::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlTextureAnimChunk16::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "string";

        case 3:
            return "ULONG";

        case 4:
            return "float";

        case 5:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlTextureAnimChunk16::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "AnimName";

        case 1:
            return "Version";

        case 2:
            return "MaterialName";

        case 3:
            return "NumFrames";

        case 4:
            return "FrameRate";

        case 5:
            return "Cyclic";

        default:
            return 0;
    }
}

bool 
tlTextureAnimChunk16::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", animname);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%s", materialname);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%ld", numframes);
            strncpy(val, buf, len);
            return true;

        case 4:
            sprintf(buf, "%f", framerate);
            strncpy(val, buf, len);
            return true;

        case 5:
            sprintf(buf, "%ld", cyclic);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlTextureAnimChunk16::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlTextureAnimChunk16::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return true;

        case 5:     
            return true;

        default:
            return false;
    }
}

bool 
tlTextureAnimChunk16::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(animname);
            animname = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 2:
            strdelete(materialname);
            materialname = strnew(new_val);
            return true;
        case 3:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numframes = tmp;
                return true;
            }
            return false;
        }
        case 4:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                framerate = tmp;
                return true;
            }
            return false;
        }
        case 5:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                cyclic = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlTextureAnimChunk16::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlTextureAnimChunk16::Copy()
{
    return new tlTextureAnimChunk16(*this);
}

tlDataChunk*
tlTextureAnimChunk16::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case P3D_TEXTURE_ANIM_CHANNEL:
        dch = new tlTextureAnimChannelChunk16(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlTextureAnimChunk16

const char* 
tlTextureAnimChunk16::AnimName()
{
    return animname;
}

void 
tlTextureAnimChunk16::SetAnimName(const char* val)
{
    strdelete(animname);
    animname = strnew(val);
}

unsigned long 
tlTextureAnimChunk16::Version()
{
    return version;
}

void 
tlTextureAnimChunk16::SetVersion(unsigned long val)
{
    version = val;
}

const char* 
tlTextureAnimChunk16::MaterialName()
{
    return materialname;
}

void 
tlTextureAnimChunk16::SetMaterialName(const char* val)
{
    strdelete(materialname);
    materialname = strnew(val);
}

unsigned long 
tlTextureAnimChunk16::NumFrames()
{
    return numframes;
}

void 
tlTextureAnimChunk16::SetNumFrames(unsigned long val)
{
    numframes = val;
}

float 
tlTextureAnimChunk16::FrameRate()
{
    return framerate;
}

void 
tlTextureAnimChunk16::SetFrameRate(float val)
{
    framerate = val;
}

unsigned long 
tlTextureAnimChunk16::Cyclic()
{
    return cyclic;
}

void 
tlTextureAnimChunk16::SetCyclic(unsigned long val)
{
    cyclic = val;
}
// tlTextureAnimChannelChunk16

int tlTextureAnimChannelChunk16::sortPriority = -1;

tlTextureAnimChannelChunk16::tlTextureAnimChannelChunk16()
{
    Init();
}

tlTextureAnimChannelChunk16::tlTextureAnimChannelChunk16(const tlTextureAnimChannelChunk16& ch)
{
    Init();
    *this = ch;
}

tlTextureAnimChannelChunk16::~tlTextureAnimChannelChunk16()
{

}

tlTextureAnimChannelChunk16::tlTextureAnimChannelChunk16(tlReadChunk16* ch)
{
    assert(ch->GetID() == P3D_TEXTURE_ANIM_CHANNEL);
    ident = P3D_TEXTURE_ANIM_CHANNEL;

    tlFile* f = ch->GetFile();

    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_TEXTURE_ANIM_CHANNEL);
    }
}

void
tlTextureAnimChannelChunk16::Init()
{
    ident = P3D_TEXTURE_ANIM_CHANNEL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_TEXTURE_ANIM_CHANNEL);
    }
}

long 
tlTextureAnimChannelChunk16::Size( )
{

    long s = 2*sizeof( long );

    int i;

    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlTextureAnimChannelChunk16&
tlTextureAnimChannelChunk16::operator=(const tlTextureAnimChannelChunk16& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }


    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlTextureAnimChannelChunk16::operator==(const tlDataChunk& a)
{

    const tlTextureAnimChannelChunk16* aptr = dynamic_cast<const tlTextureAnimChannelChunk16*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }



    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlTextureAnimChannelChunk16::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlTextureAnimChannelChunk16::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlTextureAnimChannelChunk16(ch);
    return dtCh;
}

void
tlTextureAnimChannelChunk16::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);


    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlTextureAnimChannelChunk16::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlTextureAnimChannelChunk16 (%4x)\n",indent,"",(int) ident);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlTextureAnimChannelChunk16::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlTextureAnimChannelChunk16::GetType() const
{
    return "tlTextureAnimChannelChunk16";
}

int 
tlTextureAnimChannelChunk16::GetFieldCount() const
{
    return 0;
}

bool 
tlTextureAnimChannelChunk16::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlTextureAnimChannelChunk16::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlTextureAnimChannelChunk16::GetFieldType(int fieldIndex) const
{
    return 0;
}

const char* 
tlTextureAnimChannelChunk16::GetFieldName(int fieldIndex) const
{
    return 0;
}

bool 
tlTextureAnimChannelChunk16::GetFieldValue(int fieldIndex, char* val, int len) const
{
    return false;
}

bool 
tlTextureAnimChannelChunk16::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlTextureAnimChannelChunk16::GetFieldUpdatable(int fieldIndex) const
{
    return false;
}

bool 
tlTextureAnimChannelChunk16::SetFieldValue(int fieldIndex, const char* new_val)
{
    return false;
}

bool 
tlTextureAnimChannelChunk16::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlTextureAnimChannelChunk16::Copy()
{
    return new tlTextureAnimChannelChunk16(*this);
}

tlDataChunk*
tlTextureAnimChannelChunk16::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case P3D_ENTITY_ANIM_CHANNEL:
        dch = new tlEntityChannelChunk16(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlTextureAnimChannelChunk16

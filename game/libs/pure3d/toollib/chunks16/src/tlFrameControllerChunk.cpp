// Automatically generated by Jeeves, don't edit.  Edit tlFrameControllerChunk.sc instead

#include "tlFrameControllerChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlFrameControllerChunk.sc


// tlFrameControllerChunk

int tlFrameControllerChunk::sortPriority = -1;

tlFrameControllerChunk::tlFrameControllerChunk()
{
    Init();
}

tlFrameControllerChunk::tlFrameControllerChunk(const tlFrameControllerChunk& ch)
{
    Init();
    *this = ch;
}

tlFrameControllerChunk::~tlFrameControllerChunk()
{

    if(name != NULL){
        strdelete(name);
    }
    if(hierarchyname != NULL){
        strdelete(hierarchyname);
    }
    if(animationname != NULL){
        strdelete(animationname);
    }
}

tlFrameControllerChunk::tlFrameControllerChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == Pure3D::Animation::FrameControllerData::FRAME_CONTROLLER);
    ident = Pure3D::Animation::FrameControllerData::FRAME_CONTROLLER;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    version = f->GetLong();
    f->GetPString(buf);
    name = strnew(buf);
    type = f->GetLong();
    frameoffset = f->GetFloat();
    f->GetPString(buf);
    hierarchyname = strnew(buf);
    f->GetPString(buf);
    animationname = strnew(buf);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Animation::FrameControllerData::FRAME_CONTROLLER);
    }
}

void
tlFrameControllerChunk::Init()
{
    ident = Pure3D::Animation::FrameControllerData::FRAME_CONTROLLER;
    version = 0;
    name = NULL;
    type = 0;
    frameoffset = 0.0f;
    hierarchyname = NULL;
    animationname = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Animation::FrameControllerData::FRAME_CONTROLLER);
    }
}

long 
tlFrameControllerChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( version );
    s += strlen( name );
    s += sizeof( type );
    s += sizeof( frameoffset );
    s += strlen( hierarchyname );
    s += strlen( animationname );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlFrameControllerChunk&
tlFrameControllerChunk::operator=(const tlFrameControllerChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    version = a.version;
    name = strnew(a.name);
    type = a.type;
    frameoffset = a.frameoffset;
    hierarchyname = strnew(a.hierarchyname);
    animationname = strnew(a.animationname);

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlFrameControllerChunk::operator==(const tlDataChunk& a)
{

    const tlFrameControllerChunk* aptr = dynamic_cast<const tlFrameControllerChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(version == aptr->version))
    {
        return false;
    }
    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(type == aptr->type))
    {
        return false;
    }
    if(!(frameoffset == aptr->frameoffset))
    {
        return false;
    }
    if(strcmp(hierarchyname, aptr->hierarchyname) != 0)
    {
        return false;
    }
    if(strcmp(animationname, aptr->animationname) != 0)
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlFrameControllerChunk::NameCompare(const tlDataChunk& a)
{

    const tlFrameControllerChunk* aptr = dynamic_cast<const tlFrameControllerChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlFrameControllerChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlFrameControllerChunk(ch);
    return dtCh;
}

void
tlFrameControllerChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutLong(version);
        f->PutPString(name);
        f->PutLong(type);
        f->PutFloat(frameoffset);
        f->PutPString(hierarchyname);
        f->PutPString(animationname);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlFrameControllerChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlFrameControllerChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Version       = %ld\n",indent,"",version);
    printf("%*s  Name          = \"%s\"\n",indent,"",name);
    printf("%*s  Type          = %08lx\n",indent,"",type);
    printf("%*s  FrameOffset   = %f\n",indent,"",frameoffset);
    printf("%*s  HierarchyName = \"%s\"\n",indent,"",hierarchyname);
    printf("%*s  AnimationName = \"%s\"\n",indent,"",animationname);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlFrameControllerChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # Version      \n",indent,"",version);
    printf("%*s  pstring \"%s\" # Name         \n",indent,"",name);
    printf("%*s  int   0x%08lx # Type         \n",indent,"",type);
    printf("%*s  float %12f # FrameOffset  \n",indent,"",frameoffset);
    printf("%*s  pstring \"%s\" # HierarchyName\n",indent,"",hierarchyname);
    printf("%*s  pstring \"%s\" # AnimationName\n",indent,"",animationname);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlFrameControllerChunk::GetType() const
{
    return "tlFrameControllerChunk";
}

int 
tlFrameControllerChunk::GetFieldCount() const
{
    return 6;
}

bool 
tlFrameControllerChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlFrameControllerChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlFrameControllerChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "string";

        case 2:
            return "FOURCC";

        case 3:
            return "float";

        case 4:
            return "string";

        case 5:
            return "string";

        default:
            return 0;
    }
}

const char* 
tlFrameControllerChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Version";

        case 1:
            return "Name";

        case 2:
            return "Type";

        case 3:
            return "FrameOffset";

        case 4:
            return "HierarchyName";

        case 5:
            return "AnimationName";

        default:
            return 0;
    }
}

bool 
tlFrameControllerChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%s", tlFourCC(type).AsChar());
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%f", frameoffset);
            strncpy(val, buf, len);
            return true;

        case 4:
            sprintf(buf, "%s", hierarchyname);
            strncpy(val, buf, len);
            return true;

        case 5:
            sprintf(buf, "%s", animationname);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlFrameControllerChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlFrameControllerChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return true;

        case 5:     
            return true;

        default:
            return false;
    }
}

bool 
tlFrameControllerChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 1:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 2:
        {
            type = (unsigned long) tlFourCC(new_val);
            return true;
        }
        case 3:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                frameoffset = tmp;
                return true;
            }
            return false;
        }
        case 4:
            strdelete(hierarchyname);
            hierarchyname = strnew(new_val);
            return true;
        case 5:
            strdelete(animationname);
            animationname = strnew(new_val);
            return true;
        default:
            return false;
    }
}

bool 
tlFrameControllerChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlFrameControllerChunk::Copy()
{
    return new tlFrameControllerChunk(*this);
}

tlDataChunk*
tlFrameControllerChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlFrameControllerChunk

unsigned long 
tlFrameControllerChunk::Version()
{
    return version;
}

void 
tlFrameControllerChunk::SetVersion(unsigned long val)
{
    version = val;
}

const char* 
tlFrameControllerChunk::Name()
{
    return name;
}

void 
tlFrameControllerChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlFrameControllerChunk::Type()
{
    return type;
}

void 
tlFrameControllerChunk::SetType(unsigned long val)
{
    type = val;
}

float 
tlFrameControllerChunk::FrameOffset()
{
    return frameoffset;
}

void 
tlFrameControllerChunk::SetFrameOffset(float val)
{
    frameoffset = val;
}

const char* 
tlFrameControllerChunk::HierarchyName()
{
    return hierarchyname;
}

void 
tlFrameControllerChunk::SetHierarchyName(const char* val)
{
    strdelete(hierarchyname);
    hierarchyname = strnew(val);
}

const char* 
tlFrameControllerChunk::AnimationName()
{
    return animationname;
}

void 
tlFrameControllerChunk::SetAnimationName(const char* val)
{
    strdelete(animationname);
    animationname = strnew(val);
}

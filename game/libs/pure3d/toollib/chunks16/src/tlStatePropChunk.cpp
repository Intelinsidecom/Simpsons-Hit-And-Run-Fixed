// Automatically generated by Jeeves, don't edit.  Edit tlStatePropChunk.sc instead

#include "tlStatePropChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlStatePropChunk.sc


// tlStatePropChunk

int tlStatePropChunk::sortPriority = -1;

tlStatePropChunk::tlStatePropChunk()
{
    Init();
}

tlStatePropChunk::tlStatePropChunk(const tlStatePropChunk& ch)
{
    Init();
    *this = ch;
}

tlStatePropChunk::~tlStatePropChunk()
{

    if(name != NULL){
        strdelete(name);
    }
    if(objectfactoryname != NULL){
        strdelete(objectfactoryname);
    }
}

tlStatePropChunk::tlStatePropChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == StateProp::STATEPROP);
    ident = StateProp::STATEPROP;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    version = f->GetLong();
    f->GetPString(buf);
    name = strnew(buf);
    f->GetPString(buf);
    objectfactoryname = strnew(buf);
    numstates = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROP);
    }
}

void
tlStatePropChunk::Init()
{
    ident = StateProp::STATEPROP;
    version = 0;
    name = NULL;
    objectfactoryname = NULL;
    numstates = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROP);
    }
}

long 
tlStatePropChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( version );
    s += strlen( name );
    s += strlen( objectfactoryname );
    s += sizeof( numstates );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlStatePropChunk&
tlStatePropChunk::operator=(const tlStatePropChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    version = a.version;
    name = strnew(a.name);
    objectfactoryname = strnew(a.objectfactoryname);
    numstates = a.numstates;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlStatePropChunk::operator==(const tlDataChunk& a)
{

    const tlStatePropChunk* aptr = dynamic_cast<const tlStatePropChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(version == aptr->version))
    {
        return false;
    }
    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(strcmp(objectfactoryname, aptr->objectfactoryname) != 0)
    {
        return false;
    }
    if(!(numstates == aptr->numstates))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlStatePropChunk::NameCompare(const tlDataChunk& a)
{

    const tlStatePropChunk* aptr = dynamic_cast<const tlStatePropChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlStatePropChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlStatePropChunk(ch);
    return dtCh;
}

void
tlStatePropChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutLong(version);
        f->PutPString(name);
        f->PutPString(objectfactoryname);
        f->PutLong(numstates);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlStatePropChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlStatePropChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Version           = %ld\n",indent,"",version);
    printf("%*s  Name              = \"%s\"\n",indent,"",name);
    printf("%*s  ObjectFactoryName = \"%s\"\n",indent,"",objectfactoryname);
    printf("%*s  NumStates         = %ld\n",indent,"",numstates);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlStatePropChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # Version          \n",indent,"",version);
    printf("%*s  pstring \"%s\" # Name             \n",indent,"",name);
    printf("%*s  pstring \"%s\" # ObjectFactoryName\n",indent,"",objectfactoryname);
    printf("%*s  int   %12ld # NumStates        \n",indent,"",numstates);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlStatePropChunk::GetType() const
{
    return "tlStatePropChunk";
}

int 
tlStatePropChunk::GetFieldCount() const
{
    return 4;
}

bool 
tlStatePropChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlStatePropChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlStatePropChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "string";

        case 2:
            return "string";

        case 3:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlStatePropChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Version";

        case 1:
            return "Name";

        case 2:
            return "ObjectFactoryName";

        case 3:
            return "NumStates";

        default:
            return 0;
    }
}

bool 
tlStatePropChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%s", objectfactoryname);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%ld", numstates);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlStatePropChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 1:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 2:
            strdelete(objectfactoryname);
            objectfactoryname = strnew(new_val);
            return true;
        case 3:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numstates = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlStatePropChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlStatePropChunk::Copy()
{
    return new tlStatePropChunk(*this);
}

tlDataChunk*
tlStatePropChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case StateProp::STATEPROPSTATEDATA:
        dch = new tlStatePropStateDataChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlStatePropChunk

unsigned long 
tlStatePropChunk::Version()
{
    return version;
}

void 
tlStatePropChunk::SetVersion(unsigned long val)
{
    version = val;
}

const char* 
tlStatePropChunk::Name()
{
    return name;
}

void 
tlStatePropChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

const char* 
tlStatePropChunk::ObjectFactoryName()
{
    return objectfactoryname;
}

void 
tlStatePropChunk::SetObjectFactoryName(const char* val)
{
    strdelete(objectfactoryname);
    objectfactoryname = strnew(val);
}

unsigned long 
tlStatePropChunk::NumStates()
{
    return numstates;
}

void 
tlStatePropChunk::SetNumStates(unsigned long val)
{
    numstates = val;
}
// tlStatePropStateDataChunk

int tlStatePropStateDataChunk::sortPriority = -1;

tlStatePropStateDataChunk::tlStatePropStateDataChunk()
{
    Init();
}

tlStatePropStateDataChunk::tlStatePropStateDataChunk(const tlStatePropStateDataChunk& ch)
{
    Init();
    *this = ch;
}

tlStatePropStateDataChunk::~tlStatePropStateDataChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlStatePropStateDataChunk::tlStatePropStateDataChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == StateProp::STATEPROPSTATEDATA);
    ident = StateProp::STATEPROPSTATEDATA;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    autotransition = f->GetLong();
    outstate = f->GetLong();
    numdrawables = f->GetLong();
    numframecontrollers = f->GetLong();
    numevents = f->GetLong();
    numcallbacks = f->GetLong();
    outframe = f->GetFloat();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROPSTATEDATA);
    }
}

void
tlStatePropStateDataChunk::Init()
{
    ident = StateProp::STATEPROPSTATEDATA;
    name = NULL;
    autotransition = 0;
    outstate = 0;
    numdrawables = 0;
    numframecontrollers = 0;
    numevents = 0;
    numcallbacks = 0;
    outframe = 0.0f;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROPSTATEDATA);
    }
}

long 
tlStatePropStateDataChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( autotransition );
    s += sizeof( outstate );
    s += sizeof( numdrawables );
    s += sizeof( numframecontrollers );
    s += sizeof( numevents );
    s += sizeof( numcallbacks );
    s += sizeof( outframe );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlStatePropStateDataChunk&
tlStatePropStateDataChunk::operator=(const tlStatePropStateDataChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    autotransition = a.autotransition;
    outstate = a.outstate;
    numdrawables = a.numdrawables;
    numframecontrollers = a.numframecontrollers;
    numevents = a.numevents;
    numcallbacks = a.numcallbacks;
    outframe = a.outframe;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlStatePropStateDataChunk::operator==(const tlDataChunk& a)
{

    const tlStatePropStateDataChunk* aptr = dynamic_cast<const tlStatePropStateDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(autotransition == aptr->autotransition))
    {
        return false;
    }
    if(!(outstate == aptr->outstate))
    {
        return false;
    }
    if(!(numdrawables == aptr->numdrawables))
    {
        return false;
    }
    if(!(numframecontrollers == aptr->numframecontrollers))
    {
        return false;
    }
    if(!(numevents == aptr->numevents))
    {
        return false;
    }
    if(!(numcallbacks == aptr->numcallbacks))
    {
        return false;
    }
    if(!(outframe == aptr->outframe))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlStatePropStateDataChunk::NameCompare(const tlDataChunk& a)
{

    const tlStatePropStateDataChunk* aptr = dynamic_cast<const tlStatePropStateDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlStatePropStateDataChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlStatePropStateDataChunk(ch);
    return dtCh;
}

void
tlStatePropStateDataChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(autotransition);
        f->PutLong(outstate);
        f->PutLong(numdrawables);
        f->PutLong(numframecontrollers);
        f->PutLong(numevents);
        f->PutLong(numcallbacks);
        f->PutFloat(outframe);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlStatePropStateDataChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlStatePropStateDataChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name                = \"%s\"\n",indent,"",name);
    printf("%*s  AutoTransition      = %ld\n",indent,"",autotransition);
    printf("%*s  OutState            = %ld\n",indent,"",outstate);
    printf("%*s  NumDrawables        = %ld\n",indent,"",numdrawables);
    printf("%*s  NumFrameControllers = %ld\n",indent,"",numframecontrollers);
    printf("%*s  NumEvents           = %ld\n",indent,"",numevents);
    printf("%*s  NumCallbacks        = %ld\n",indent,"",numcallbacks);
    printf("%*s  OutFrame            = %f\n",indent,"",outframe);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlStatePropStateDataChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name               \n",indent,"",name);
    printf("%*s  int   %12ld # AutoTransition     \n",indent,"",autotransition);
    printf("%*s  int   %12ld # OutState           \n",indent,"",outstate);
    printf("%*s  int   %12ld # NumDrawables       \n",indent,"",numdrawables);
    printf("%*s  int   %12ld # NumFrameControllers\n",indent,"",numframecontrollers);
    printf("%*s  int   %12ld # NumEvents          \n",indent,"",numevents);
    printf("%*s  int   %12ld # NumCallbacks       \n",indent,"",numcallbacks);
    printf("%*s  float %12f # OutFrame           \n",indent,"",outframe);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlStatePropStateDataChunk::GetType() const
{
    return "tlStatePropStateDataChunk";
}

int 
tlStatePropStateDataChunk::GetFieldCount() const
{
    return 8;
}

bool 
tlStatePropStateDataChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlStatePropStateDataChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlStatePropStateDataChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        case 3:
            return "ULONG";

        case 4:
            return "ULONG";

        case 5:
            return "ULONG";

        case 6:
            return "ULONG";

        case 7:
            return "float";

        default:
            return 0;
    }
}

const char* 
tlStatePropStateDataChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "AutoTransition";

        case 2:
            return "OutState";

        case 3:
            return "NumDrawables";

        case 4:
            return "NumFrameControllers";

        case 5:
            return "NumEvents";

        case 6:
            return "NumCallbacks";

        case 7:
            return "OutFrame";

        default:
            return 0;
    }
}

bool 
tlStatePropStateDataChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", autotransition);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", outstate);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%ld", numdrawables);
            strncpy(val, buf, len);
            return true;

        case 4:
            sprintf(buf, "%ld", numframecontrollers);
            strncpy(val, buf, len);
            return true;

        case 5:
            sprintf(buf, "%ld", numevents);
            strncpy(val, buf, len);
            return true;

        case 6:
            sprintf(buf, "%ld", numcallbacks);
            strncpy(val, buf, len);
            return true;

        case 7:
            sprintf(buf, "%f", outframe);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropStateDataChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlStatePropStateDataChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return true;

        case 5:     
            return true;

        case 6:     
            return true;

        case 7:     
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropStateDataChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                autotransition = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                outstate = tmp;
                return true;
            }
            return false;
        }
        case 3:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numdrawables = tmp;
                return true;
            }
            return false;
        }
        case 4:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numframecontrollers = tmp;
                return true;
            }
            return false;
        }
        case 5:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numevents = tmp;
                return true;
            }
            return false;
        }
        case 6:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numcallbacks = tmp;
                return true;
            }
            return false;
        }
        case 7:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                outframe = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlStatePropStateDataChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlStatePropStateDataChunk::Copy()
{
    return new tlStatePropStateDataChunk(*this);
}

tlDataChunk*
tlStatePropStateDataChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case StateProp::STATEPROPVISIBILITYDATA:
        dch = new tlStatePropVisibilitiesDataChunk(&ch);
        break;
    case StateProp::STATEPROPFRAMECONTROLLERDATA:
        dch = new tlStatePropFrameControllerDataChunk(&ch);
        break;
    case StateProp::STATEPROPEVENTDATA:
        dch = new tlStatePropEventDataChunk(&ch);
        break;
    case StateProp::STATEPROPCALLBACKDATA:
        dch = new tlStatePropCallbackDataChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlStatePropStateDataChunk

const char* 
tlStatePropStateDataChunk::Name()
{
    return name;
}

void 
tlStatePropStateDataChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlStatePropStateDataChunk::AutoTransition()
{
    return autotransition;
}

void 
tlStatePropStateDataChunk::SetAutoTransition(unsigned long val)
{
    autotransition = val;
}

unsigned long 
tlStatePropStateDataChunk::OutState()
{
    return outstate;
}

void 
tlStatePropStateDataChunk::SetOutState(unsigned long val)
{
    outstate = val;
}

unsigned long 
tlStatePropStateDataChunk::NumDrawables()
{
    return numdrawables;
}

void 
tlStatePropStateDataChunk::SetNumDrawables(unsigned long val)
{
    numdrawables = val;
}

unsigned long 
tlStatePropStateDataChunk::NumFrameControllers()
{
    return numframecontrollers;
}

void 
tlStatePropStateDataChunk::SetNumFrameControllers(unsigned long val)
{
    numframecontrollers = val;
}

unsigned long 
tlStatePropStateDataChunk::NumEvents()
{
    return numevents;
}

void 
tlStatePropStateDataChunk::SetNumEvents(unsigned long val)
{
    numevents = val;
}

unsigned long 
tlStatePropStateDataChunk::NumCallbacks()
{
    return numcallbacks;
}

void 
tlStatePropStateDataChunk::SetNumCallbacks(unsigned long val)
{
    numcallbacks = val;
}

float 
tlStatePropStateDataChunk::OutFrame()
{
    return outframe;
}

void 
tlStatePropStateDataChunk::SetOutFrame(float val)
{
    outframe = val;
}
// tlStatePropVisibilitiesDataChunk

int tlStatePropVisibilitiesDataChunk::sortPriority = -1;

tlStatePropVisibilitiesDataChunk::tlStatePropVisibilitiesDataChunk()
{
    Init();
}

tlStatePropVisibilitiesDataChunk::tlStatePropVisibilitiesDataChunk(const tlStatePropVisibilitiesDataChunk& ch)
{
    Init();
    *this = ch;
}

tlStatePropVisibilitiesDataChunk::~tlStatePropVisibilitiesDataChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlStatePropVisibilitiesDataChunk::tlStatePropVisibilitiesDataChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == StateProp::STATEPROPVISIBILITYDATA);
    ident = StateProp::STATEPROPVISIBILITYDATA;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    visible = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROPVISIBILITYDATA);
    }
}

void
tlStatePropVisibilitiesDataChunk::Init()
{
    ident = StateProp::STATEPROPVISIBILITYDATA;
    name = NULL;
    visible = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROPVISIBILITYDATA);
    }
}

long 
tlStatePropVisibilitiesDataChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( visible );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlStatePropVisibilitiesDataChunk&
tlStatePropVisibilitiesDataChunk::operator=(const tlStatePropVisibilitiesDataChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    visible = a.visible;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlStatePropVisibilitiesDataChunk::operator==(const tlDataChunk& a)
{

    const tlStatePropVisibilitiesDataChunk* aptr = dynamic_cast<const tlStatePropVisibilitiesDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(visible == aptr->visible))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlStatePropVisibilitiesDataChunk::NameCompare(const tlDataChunk& a)
{

    const tlStatePropVisibilitiesDataChunk* aptr = dynamic_cast<const tlStatePropVisibilitiesDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlStatePropVisibilitiesDataChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlStatePropVisibilitiesDataChunk(ch);
    return dtCh;
}

void
tlStatePropVisibilitiesDataChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(visible);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlStatePropVisibilitiesDataChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlStatePropVisibilitiesDataChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name    = \"%s\"\n",indent,"",name);
    printf("%*s  Visible = %ld\n",indent,"",visible);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlStatePropVisibilitiesDataChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name   \n",indent,"",name);
    printf("%*s  int   %12ld # Visible\n",indent,"",visible);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlStatePropVisibilitiesDataChunk::GetType() const
{
    return "tlStatePropVisibilitiesDataChunk";
}

int 
tlStatePropVisibilitiesDataChunk::GetFieldCount() const
{
    return 2;
}

bool 
tlStatePropVisibilitiesDataChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlStatePropVisibilitiesDataChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlStatePropVisibilitiesDataChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlStatePropVisibilitiesDataChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Visible";

        default:
            return 0;
    }
}

bool 
tlStatePropVisibilitiesDataChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", visible);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropVisibilitiesDataChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlStatePropVisibilitiesDataChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropVisibilitiesDataChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                visible = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlStatePropVisibilitiesDataChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlStatePropVisibilitiesDataChunk::Copy()
{
    return new tlStatePropVisibilitiesDataChunk(*this);
}

tlDataChunk*
tlStatePropVisibilitiesDataChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlStatePropVisibilitiesDataChunk

const char* 
tlStatePropVisibilitiesDataChunk::Name()
{
    return name;
}

void 
tlStatePropVisibilitiesDataChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlStatePropVisibilitiesDataChunk::Visible()
{
    return visible;
}

void 
tlStatePropVisibilitiesDataChunk::SetVisible(unsigned long val)
{
    visible = val;
}
// tlStatePropFrameControllerDataChunk

int tlStatePropFrameControllerDataChunk::sortPriority = -1;

tlStatePropFrameControllerDataChunk::tlStatePropFrameControllerDataChunk()
{
    Init();
}

tlStatePropFrameControllerDataChunk::tlStatePropFrameControllerDataChunk(const tlStatePropFrameControllerDataChunk& ch)
{
    Init();
    *this = ch;
}

tlStatePropFrameControllerDataChunk::~tlStatePropFrameControllerDataChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlStatePropFrameControllerDataChunk::tlStatePropFrameControllerDataChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == StateProp::STATEPROPFRAMECONTROLLERDATA);
    ident = StateProp::STATEPROPFRAMECONTROLLERDATA;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    cyclic = f->GetLong();
    numberofcycles = f->GetLong();
    holdframe = f->GetLong();
    minframe = f->GetFloat();
    maxframe = f->GetFloat();
    relativespeed = f->GetFloat();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROPFRAMECONTROLLERDATA);
    }
}

void
tlStatePropFrameControllerDataChunk::Init()
{
    ident = StateProp::STATEPROPFRAMECONTROLLERDATA;
    name = NULL;
    cyclic = 0;
    numberofcycles = 0;
    holdframe = 0;
    minframe = 0.0f;
    maxframe = 0.0f;
    relativespeed = 0.0f;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROPFRAMECONTROLLERDATA);
    }
}

long 
tlStatePropFrameControllerDataChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( cyclic );
    s += sizeof( numberofcycles );
    s += sizeof( holdframe );
    s += sizeof( minframe );
    s += sizeof( maxframe );
    s += sizeof( relativespeed );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlStatePropFrameControllerDataChunk&
tlStatePropFrameControllerDataChunk::operator=(const tlStatePropFrameControllerDataChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    cyclic = a.cyclic;
    numberofcycles = a.numberofcycles;
    holdframe = a.holdframe;
    minframe = a.minframe;
    maxframe = a.maxframe;
    relativespeed = a.relativespeed;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlStatePropFrameControllerDataChunk::operator==(const tlDataChunk& a)
{

    const tlStatePropFrameControllerDataChunk* aptr = dynamic_cast<const tlStatePropFrameControllerDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(cyclic == aptr->cyclic))
    {
        return false;
    }
    if(!(numberofcycles == aptr->numberofcycles))
    {
        return false;
    }
    if(!(holdframe == aptr->holdframe))
    {
        return false;
    }
    if(!(minframe == aptr->minframe))
    {
        return false;
    }
    if(!(maxframe == aptr->maxframe))
    {
        return false;
    }
    if(!(relativespeed == aptr->relativespeed))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlStatePropFrameControllerDataChunk::NameCompare(const tlDataChunk& a)
{

    const tlStatePropFrameControllerDataChunk* aptr = dynamic_cast<const tlStatePropFrameControllerDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlStatePropFrameControllerDataChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlStatePropFrameControllerDataChunk(ch);
    return dtCh;
}

void
tlStatePropFrameControllerDataChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(cyclic);
        f->PutLong(numberofcycles);
        f->PutLong(holdframe);
        f->PutFloat(minframe);
        f->PutFloat(maxframe);
        f->PutFloat(relativespeed);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlStatePropFrameControllerDataChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlStatePropFrameControllerDataChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name           = \"%s\"\n",indent,"",name);
    printf("%*s  Cyclic         = %ld\n",indent,"",cyclic);
    printf("%*s  NumberOfCycles = %ld\n",indent,"",numberofcycles);
    printf("%*s  HoldFrame      = %ld\n",indent,"",holdframe);
    printf("%*s  MinFrame       = %f\n",indent,"",minframe);
    printf("%*s  MaxFrame       = %f\n",indent,"",maxframe);
    printf("%*s  RelativeSpeed  = %f\n",indent,"",relativespeed);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlStatePropFrameControllerDataChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name          \n",indent,"",name);
    printf("%*s  int   %12ld # Cyclic        \n",indent,"",cyclic);
    printf("%*s  int   %12ld # NumberOfCycles\n",indent,"",numberofcycles);
    printf("%*s  int   %12ld # HoldFrame     \n",indent,"",holdframe);
    printf("%*s  float %12f # MinFrame      \n",indent,"",minframe);
    printf("%*s  float %12f # MaxFrame      \n",indent,"",maxframe);
    printf("%*s  float %12f # RelativeSpeed \n",indent,"",relativespeed);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlStatePropFrameControllerDataChunk::GetType() const
{
    return "tlStatePropFrameControllerDataChunk";
}

int 
tlStatePropFrameControllerDataChunk::GetFieldCount() const
{
    return 7;
}

bool 
tlStatePropFrameControllerDataChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlStatePropFrameControllerDataChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlStatePropFrameControllerDataChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        case 3:
            return "ULONG";

        case 4:
            return "float";

        case 5:
            return "float";

        case 6:
            return "float";

        default:
            return 0;
    }
}

const char* 
tlStatePropFrameControllerDataChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Cyclic";

        case 2:
            return "NumberOfCycles";

        case 3:
            return "HoldFrame";

        case 4:
            return "MinFrame";

        case 5:
            return "MaxFrame";

        case 6:
            return "RelativeSpeed";

        default:
            return 0;
    }
}

bool 
tlStatePropFrameControllerDataChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", cyclic);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", numberofcycles);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%ld", holdframe);
            strncpy(val, buf, len);
            return true;

        case 4:
            sprintf(buf, "%f", minframe);
            strncpy(val, buf, len);
            return true;

        case 5:
            sprintf(buf, "%f", maxframe);
            strncpy(val, buf, len);
            return true;

        case 6:
            sprintf(buf, "%f", relativespeed);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropFrameControllerDataChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlStatePropFrameControllerDataChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return true;

        case 5:     
            return true;

        case 6:     
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropFrameControllerDataChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                cyclic = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numberofcycles = tmp;
                return true;
            }
            return false;
        }
        case 3:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                holdframe = tmp;
                return true;
            }
            return false;
        }
        case 4:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                minframe = tmp;
                return true;
            }
            return false;
        }
        case 5:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                maxframe = tmp;
                return true;
            }
            return false;
        }
        case 6:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                relativespeed = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlStatePropFrameControllerDataChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlStatePropFrameControllerDataChunk::Copy()
{
    return new tlStatePropFrameControllerDataChunk(*this);
}

tlDataChunk*
tlStatePropFrameControllerDataChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlStatePropFrameControllerDataChunk

const char* 
tlStatePropFrameControllerDataChunk::Name()
{
    return name;
}

void 
tlStatePropFrameControllerDataChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlStatePropFrameControllerDataChunk::Cyclic()
{
    return cyclic;
}

void 
tlStatePropFrameControllerDataChunk::SetCyclic(unsigned long val)
{
    cyclic = val;
}

unsigned long 
tlStatePropFrameControllerDataChunk::NumberOfCycles()
{
    return numberofcycles;
}

void 
tlStatePropFrameControllerDataChunk::SetNumberOfCycles(unsigned long val)
{
    numberofcycles = val;
}

unsigned long 
tlStatePropFrameControllerDataChunk::HoldFrame()
{
    return holdframe;
}

void 
tlStatePropFrameControllerDataChunk::SetHoldFrame(unsigned long val)
{
    holdframe = val;
}

float 
tlStatePropFrameControllerDataChunk::MinFrame()
{
    return minframe;
}

void 
tlStatePropFrameControllerDataChunk::SetMinFrame(float val)
{
    minframe = val;
}

float 
tlStatePropFrameControllerDataChunk::MaxFrame()
{
    return maxframe;
}

void 
tlStatePropFrameControllerDataChunk::SetMaxFrame(float val)
{
    maxframe = val;
}

float 
tlStatePropFrameControllerDataChunk::RelativeSpeed()
{
    return relativespeed;
}

void 
tlStatePropFrameControllerDataChunk::SetRelativeSpeed(float val)
{
    relativespeed = val;
}
// tlStatePropEventDataChunk

int tlStatePropEventDataChunk::sortPriority = -1;

tlStatePropEventDataChunk::tlStatePropEventDataChunk()
{
    Init();
}

tlStatePropEventDataChunk::tlStatePropEventDataChunk(const tlStatePropEventDataChunk& ch)
{
    Init();
    *this = ch;
}

tlStatePropEventDataChunk::~tlStatePropEventDataChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlStatePropEventDataChunk::tlStatePropEventDataChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == StateProp::STATEPROPEVENTDATA);
    ident = StateProp::STATEPROPEVENTDATA;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    state = f->GetLong();
    eventenum = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROPEVENTDATA);
    }
}

void
tlStatePropEventDataChunk::Init()
{
    ident = StateProp::STATEPROPEVENTDATA;
    name = NULL;
    state = 0;
    eventenum = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROPEVENTDATA);
    }
}

long 
tlStatePropEventDataChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( state );
    s += sizeof( eventenum );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlStatePropEventDataChunk&
tlStatePropEventDataChunk::operator=(const tlStatePropEventDataChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    state = a.state;
    eventenum = a.eventenum;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlStatePropEventDataChunk::operator==(const tlDataChunk& a)
{

    const tlStatePropEventDataChunk* aptr = dynamic_cast<const tlStatePropEventDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(state == aptr->state))
    {
        return false;
    }
    if(!(eventenum == aptr->eventenum))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlStatePropEventDataChunk::NameCompare(const tlDataChunk& a)
{

    const tlStatePropEventDataChunk* aptr = dynamic_cast<const tlStatePropEventDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlStatePropEventDataChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlStatePropEventDataChunk(ch);
    return dtCh;
}

void
tlStatePropEventDataChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(state);
        f->PutLong(eventenum);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlStatePropEventDataChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlStatePropEventDataChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name      = \"%s\"\n",indent,"",name);
    printf("%*s  State     = %ld\n",indent,"",state);
    printf("%*s  EventEnum = %ld\n",indent,"",eventenum);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlStatePropEventDataChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name     \n",indent,"",name);
    printf("%*s  int   %12ld # State    \n",indent,"",state);
    printf("%*s  int   %12ld # EventEnum\n",indent,"",eventenum);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlStatePropEventDataChunk::GetType() const
{
    return "tlStatePropEventDataChunk";
}

int 
tlStatePropEventDataChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlStatePropEventDataChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlStatePropEventDataChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlStatePropEventDataChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlStatePropEventDataChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "State";

        case 2:
            return "EventEnum";

        default:
            return 0;
    }
}

bool 
tlStatePropEventDataChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", state);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", eventenum);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropEventDataChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlStatePropEventDataChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropEventDataChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                state = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                eventenum = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlStatePropEventDataChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlStatePropEventDataChunk::Copy()
{
    return new tlStatePropEventDataChunk(*this);
}

tlDataChunk*
tlStatePropEventDataChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlStatePropEventDataChunk

const char* 
tlStatePropEventDataChunk::Name()
{
    return name;
}

void 
tlStatePropEventDataChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlStatePropEventDataChunk::State()
{
    return state;
}

void 
tlStatePropEventDataChunk::SetState(unsigned long val)
{
    state = val;
}

unsigned long 
tlStatePropEventDataChunk::EventEnum()
{
    return eventenum;
}

void 
tlStatePropEventDataChunk::SetEventEnum(unsigned long val)
{
    eventenum = val;
}
// tlStatePropCallbackDataChunk

int tlStatePropCallbackDataChunk::sortPriority = -1;

tlStatePropCallbackDataChunk::tlStatePropCallbackDataChunk()
{
    Init();
}

tlStatePropCallbackDataChunk::tlStatePropCallbackDataChunk(const tlStatePropCallbackDataChunk& ch)
{
    Init();
    *this = ch;
}

tlStatePropCallbackDataChunk::~tlStatePropCallbackDataChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlStatePropCallbackDataChunk::tlStatePropCallbackDataChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == StateProp::STATEPROPCALLBACKDATA);
    ident = StateProp::STATEPROPCALLBACKDATA;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    eventenum = f->GetLong();
    onframe = f->GetFloat();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROPCALLBACKDATA);
    }
}

void
tlStatePropCallbackDataChunk::Init()
{
    ident = StateProp::STATEPROPCALLBACKDATA;
    name = NULL;
    eventenum = 0;
    onframe = 0.0f;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(StateProp::STATEPROPCALLBACKDATA);
    }
}

long 
tlStatePropCallbackDataChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( eventenum );
    s += sizeof( onframe );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlStatePropCallbackDataChunk&
tlStatePropCallbackDataChunk::operator=(const tlStatePropCallbackDataChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    eventenum = a.eventenum;
    onframe = a.onframe;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlStatePropCallbackDataChunk::operator==(const tlDataChunk& a)
{

    const tlStatePropCallbackDataChunk* aptr = dynamic_cast<const tlStatePropCallbackDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(eventenum == aptr->eventenum))
    {
        return false;
    }
    if(!(onframe == aptr->onframe))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlStatePropCallbackDataChunk::NameCompare(const tlDataChunk& a)
{

    const tlStatePropCallbackDataChunk* aptr = dynamic_cast<const tlStatePropCallbackDataChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlStatePropCallbackDataChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlStatePropCallbackDataChunk(ch);
    return dtCh;
}

void
tlStatePropCallbackDataChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(eventenum);
        f->PutFloat(onframe);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlStatePropCallbackDataChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlStatePropCallbackDataChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name      = \"%s\"\n",indent,"",name);
    printf("%*s  EventEnum = %ld\n",indent,"",eventenum);
    printf("%*s  OnFrame   = %f\n",indent,"",onframe);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlStatePropCallbackDataChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name     \n",indent,"",name);
    printf("%*s  int   %12ld # EventEnum\n",indent,"",eventenum);
    printf("%*s  float %12f # OnFrame  \n",indent,"",onframe);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlStatePropCallbackDataChunk::GetType() const
{
    return "tlStatePropCallbackDataChunk";
}

int 
tlStatePropCallbackDataChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlStatePropCallbackDataChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlStatePropCallbackDataChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlStatePropCallbackDataChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "float";

        default:
            return 0;
    }
}

const char* 
tlStatePropCallbackDataChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "EventEnum";

        case 2:
            return "OnFrame";

        default:
            return 0;
    }
}

bool 
tlStatePropCallbackDataChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", eventenum);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%f", onframe);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropCallbackDataChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlStatePropCallbackDataChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlStatePropCallbackDataChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                eventenum = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                onframe = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlStatePropCallbackDataChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlStatePropCallbackDataChunk::Copy()
{
    return new tlStatePropCallbackDataChunk(*this);
}

tlDataChunk*
tlStatePropCallbackDataChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlStatePropCallbackDataChunk

const char* 
tlStatePropCallbackDataChunk::Name()
{
    return name;
}

void 
tlStatePropCallbackDataChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlStatePropCallbackDataChunk::EventEnum()
{
    return eventenum;
}

void 
tlStatePropCallbackDataChunk::SetEventEnum(unsigned long val)
{
    eventenum = val;
}

float 
tlStatePropCallbackDataChunk::OnFrame()
{
    return onframe;
}

void 
tlStatePropCallbackDataChunk::SetOnFrame(float val)
{
    onframe = val;
}

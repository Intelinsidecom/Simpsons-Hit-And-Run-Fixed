// Automatically generated by Jeeves, don't edit.  Edit tlMeshChunk.sc instead

#include "tlMeshChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlMeshChunk.sc


// tlMeshChunk

int tlMeshChunk::sortPriority = -1;

tlMeshChunk::tlMeshChunk()
{
    Init();
}

tlMeshChunk::tlMeshChunk(const tlMeshChunk& ch)
{
    Init();
    *this = ch;
}

tlMeshChunk::~tlMeshChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlMeshChunk::tlMeshChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == Pure3D::Mesh::MESH);
    ident = Pure3D::Mesh::MESH;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    version = f->GetLong();
    numprimgroups = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Mesh::MESH);
    }
}

void
tlMeshChunk::Init()
{
    ident = Pure3D::Mesh::MESH;
    name = NULL;
    version = 0;
    numprimgroups = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Mesh::MESH);
    }
}

long 
tlMeshChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( version );
    s += sizeof( numprimgroups );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlMeshChunk&
tlMeshChunk::operator=(const tlMeshChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    version = a.version;
    numprimgroups = a.numprimgroups;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlMeshChunk::operator==(const tlDataChunk& a)
{

    const tlMeshChunk* aptr = dynamic_cast<const tlMeshChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(version == aptr->version))
    {
        return false;
    }
    if(!(numprimgroups == aptr->numprimgroups))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlMeshChunk::NameCompare(const tlDataChunk& a)
{

    const tlMeshChunk* aptr = dynamic_cast<const tlMeshChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlMeshChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlMeshChunk(ch);
    return dtCh;
}

void
tlMeshChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(version);
        f->PutLong(numprimgroups);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlMeshChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlMeshChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name          = \"%s\"\n",indent,"",name);
    printf("%*s  Version       = %ld\n",indent,"",version);
    printf("%*s  NumPrimGroups = %ld\n",indent,"",numprimgroups);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlMeshChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name         \n",indent,"",name);
    printf("%*s  int   %12ld # Version      \n",indent,"",version);
    printf("%*s  int   %12ld # NumPrimGroups\n",indent,"",numprimgroups);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlMeshChunk::GetType() const
{
    return "tlMeshChunk";
}

int 
tlMeshChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlMeshChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlMeshChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlMeshChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlMeshChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Version";

        case 2:
            return "NumPrimGroups";

        default:
            return 0;
    }
}

bool 
tlMeshChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", numprimgroups);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlMeshChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlMeshChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlMeshChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numprimgroups = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlMeshChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlMeshChunk::Copy()
{
    return new tlMeshChunk(*this);
}

tlDataChunk*
tlMeshChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case Pure3D::Mesh::PRIMGROUP:
        dch = new tlPrimGroupChunk(&ch);
        break;
    case Pure3D::Mesh::BOX:
        dch = new tlBBoxChunk(&ch);
        break;
    case Pure3D::Mesh::SPHERE:
        dch = new tlBSphereChunk(&ch);
        break;
    case Pure3D::Mesh::RENDERSTATUS:
        dch = new tlRenderStatusChunk(&ch);
        break;
    case Pure3D::Mesh::EXPRESSIONOFFSETS:
        dch = new tlExpressionOffsetsChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlMeshChunk

const char* 
tlMeshChunk::Name()
{
    return name;
}

void 
tlMeshChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlMeshChunk::Version()
{
    return version;
}

void 
tlMeshChunk::SetVersion(unsigned long val)
{
    version = val;
}

unsigned long 
tlMeshChunk::NumPrimGroups()
{
    return numprimgroups;
}

void 
tlMeshChunk::SetNumPrimGroups(unsigned long val)
{
    numprimgroups = val;
}
// tlRenderStatusChunk

int tlRenderStatusChunk::sortPriority = -1;

tlRenderStatusChunk::tlRenderStatusChunk()
{
    Init();
}

tlRenderStatusChunk::tlRenderStatusChunk(const tlRenderStatusChunk& ch)
{
    Init();
    *this = ch;
}

tlRenderStatusChunk::~tlRenderStatusChunk()
{

}

tlRenderStatusChunk::tlRenderStatusChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == Pure3D::Mesh::RENDERSTATUS);
    ident = Pure3D::Mesh::RENDERSTATUS;

    tlFile* f = ch->GetFile();

    castshadow = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Mesh::RENDERSTATUS);
    }
}

void
tlRenderStatusChunk::Init()
{
    ident = Pure3D::Mesh::RENDERSTATUS;
    castshadow = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(Pure3D::Mesh::RENDERSTATUS);
    }
}

long 
tlRenderStatusChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( castshadow );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlRenderStatusChunk&
tlRenderStatusChunk::operator=(const tlRenderStatusChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    castshadow = a.castshadow;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlRenderStatusChunk::operator==(const tlDataChunk& a)
{

    const tlRenderStatusChunk* aptr = dynamic_cast<const tlRenderStatusChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(castshadow == aptr->castshadow))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlRenderStatusChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlRenderStatusChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlRenderStatusChunk(ch);
    return dtCh;
}

void
tlRenderStatusChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutLong(castshadow);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlRenderStatusChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlRenderStatusChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  CastShadow = %ld\n",indent,"",castshadow);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlRenderStatusChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # CastShadow\n",indent,"",castshadow);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlRenderStatusChunk::GetType() const
{
    return "tlRenderStatusChunk";
}

int 
tlRenderStatusChunk::GetFieldCount() const
{
    return 1;
}

bool 
tlRenderStatusChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlRenderStatusChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlRenderStatusChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlRenderStatusChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "CastShadow";

        default:
            return 0;
    }
}

bool 
tlRenderStatusChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", castshadow);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlRenderStatusChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlRenderStatusChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        default:
            return false;
    }
}

bool 
tlRenderStatusChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                castshadow = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlRenderStatusChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlRenderStatusChunk::Copy()
{
    return new tlRenderStatusChunk(*this);
}

tlDataChunk*
tlRenderStatusChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlRenderStatusChunk

unsigned long 
tlRenderStatusChunk::CastShadow()
{
    return castshadow;
}

void 
tlRenderStatusChunk::SetCastShadow(unsigned long val)
{
    castshadow = val;
}

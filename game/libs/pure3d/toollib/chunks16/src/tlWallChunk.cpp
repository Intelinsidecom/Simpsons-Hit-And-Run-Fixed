// Automatically generated by Jeeves, don't edit.  Edit tlWallChunk.sc instead

#include "tlWallChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlWallChunk.sc


// tlWallChunk

int tlWallChunk::sortPriority = -1;

tlWallChunk::tlWallChunk()
{
    Init();
}

tlWallChunk::tlWallChunk(const tlWallChunk& ch)
{
    Init();
    *this = ch;
}

tlWallChunk::~tlWallChunk()
{

}

tlWallChunk::tlWallChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::WALL);
    ident = SRR2::ChunkID::WALL;

    tlFile* f = ch->GetFile();

    start.Read(f);
    end.Read(f);
    normal.Read(f);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::WALL);
    }
}

void
tlWallChunk::Init()
{
    ident = SRR2::ChunkID::WALL;
    start.Init( );
    end.Init( );
    normal.Init( );
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::WALL);
    }
}

long 
tlWallChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( start );
    s += sizeof( end );
    s += sizeof( normal );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlWallChunk&
tlWallChunk::operator=(const tlWallChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    start = a.start;
    end = a.end;
    normal = a.normal;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlWallChunk::operator==(const tlDataChunk& a)
{

    const tlWallChunk* aptr = dynamic_cast<const tlWallChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(start == aptr->start))
    {
        return false;
    }
    if(!(end == aptr->end))
    {
        return false;
    }
    if(!(normal == aptr->normal))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlWallChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlWallChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlWallChunk(ch);
    return dtCh;
}

void
tlWallChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        start.Write(f);
        end.Write(f);
        normal.Write(f);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlWallChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlWallChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Start :\n",indent,"");
    start.Print(0, indent+4);
    printf("%*s  End   :\n",indent,"");
    end.Print(0, indent+4);
    printf("%*s  Normal:\n",indent,"");
    normal.Print(0, indent+4);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlWallChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  # Start :\n",indent,"");
    start.PrintFormatted(0, indent+4);
    printf("%*s  # End   :\n",indent,"");
    end.PrintFormatted(0, indent+4);
    printf("%*s  # Normal:\n",indent,"");
    normal.PrintFormatted(0, indent+4);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlWallChunk::GetType() const
{
    return "tlWallChunk";
}

int 
tlWallChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlWallChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlWallChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlWallChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "tlPoint";

        case 1:
            return "tlPoint";

        case 2:
            return "tlPoint";

        default:
            return 0;
    }
}

const char* 
tlWallChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Start";

        case 1:
            return "End";

        case 2:
            return "Normal";

        default:
            return 0;
    }
}

bool 
tlWallChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    switch(fieldIndex)
    {
        case 0:
            return start.GetFieldValue(val, len);

        case 1:
            return end.GetFieldValue(val, len);

        case 2:
            return normal.GetFieldValue(val, len);

        default:
            return false;
    }
}

bool 
tlWallChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlWallChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return tlPoint::GetFieldUpdatable();

        case 1:     
            return tlPoint::GetFieldUpdatable();

        case 2:     
            return tlPoint::GetFieldUpdatable();

        default:
            return false;
    }
}

bool 
tlWallChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return start.SetFieldValue(new_val);
        case 1:
            return end.SetFieldValue(new_val);
        case 2:
            return normal.SetFieldValue(new_val);
        default:
            return false;
    }
}

bool 
tlWallChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlWallChunk::Copy()
{
    return new tlWallChunk(*this);
}

tlDataChunk*
tlWallChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlWallChunk

tlPoint 
tlWallChunk::Start()
{
    return start;
}

void 
tlWallChunk::SetStart(tlPoint val)
{
    start = val;
}

tlPoint 
tlWallChunk::End()
{
    return end;
}

void 
tlWallChunk::SetEnd(tlPoint val)
{
    end = val;
}

tlPoint 
tlWallChunk::Normal()
{
    return normal;
}

void 
tlWallChunk::SetNormal(tlPoint val)
{
    normal = val;
}

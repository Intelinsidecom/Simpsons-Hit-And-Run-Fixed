// Automatically generated by Jeeves, don't edit.  Edit tlInstaStaticPhysDSGChunk.sc instead

#include "tlInstaStaticPhysDSGChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlInstaStaticPhysDSGChunk.sc


// tlInstaStaticPhysDSGChunk

int tlInstaStaticPhysDSGChunk::sortPriority = -1;

tlInstaStaticPhysDSGChunk::tlInstaStaticPhysDSGChunk()
{
    Init();
}

tlInstaStaticPhysDSGChunk::tlInstaStaticPhysDSGChunk(const tlInstaStaticPhysDSGChunk& ch)
{
    Init();
    *this = ch;
}

tlInstaStaticPhysDSGChunk::~tlInstaStaticPhysDSGChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlInstaStaticPhysDSGChunk::tlInstaStaticPhysDSGChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::INSTA_STATIC_PHYS_DSG);
    ident = SRR2::ChunkID::INSTA_STATIC_PHYS_DSG;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    version = f->GetLong();
    hasalpha = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::INSTA_STATIC_PHYS_DSG);
    }
}

void
tlInstaStaticPhysDSGChunk::Init()
{
    ident = SRR2::ChunkID::INSTA_STATIC_PHYS_DSG;
    name = NULL;
    version = 0;
    hasalpha = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::INSTA_STATIC_PHYS_DSG);
    }
}

long 
tlInstaStaticPhysDSGChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( version );
    s += sizeof( hasalpha );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlInstaStaticPhysDSGChunk&
tlInstaStaticPhysDSGChunk::operator=(const tlInstaStaticPhysDSGChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    version = a.version;
    hasalpha = a.hasalpha;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlInstaStaticPhysDSGChunk::operator==(const tlDataChunk& a)
{

    const tlInstaStaticPhysDSGChunk* aptr = dynamic_cast<const tlInstaStaticPhysDSGChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(version == aptr->version))
    {
        return false;
    }
    if(!(hasalpha == aptr->hasalpha))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlInstaStaticPhysDSGChunk::NameCompare(const tlDataChunk& a)
{

    const tlInstaStaticPhysDSGChunk* aptr = dynamic_cast<const tlInstaStaticPhysDSGChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlInstaStaticPhysDSGChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlInstaStaticPhysDSGChunk(ch);
    return dtCh;
}

void
tlInstaStaticPhysDSGChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(version);
        f->PutLong(hasalpha);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlInstaStaticPhysDSGChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlInstaStaticPhysDSGChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name     = \"%s\"\n",indent,"",name);
    printf("%*s  Version  = %ld\n",indent,"",version);
    printf("%*s  HasAlpha = %ld\n",indent,"",hasalpha);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlInstaStaticPhysDSGChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name    \n",indent,"",name);
    printf("%*s  int   %12ld # Version \n",indent,"",version);
    printf("%*s  int   %12ld # HasAlpha\n",indent,"",hasalpha);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlInstaStaticPhysDSGChunk::GetType() const
{
    return "tlInstaStaticPhysDSGChunk";
}

int 
tlInstaStaticPhysDSGChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlInstaStaticPhysDSGChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlInstaStaticPhysDSGChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlInstaStaticPhysDSGChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlInstaStaticPhysDSGChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Version";

        case 2:
            return "HasAlpha";

        default:
            return 0;
    }
}

bool 
tlInstaStaticPhysDSGChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", hasalpha);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlInstaStaticPhysDSGChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlInstaStaticPhysDSGChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlInstaStaticPhysDSGChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                hasalpha = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlInstaStaticPhysDSGChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlInstaStaticPhysDSGChunk::Copy()
{
    return new tlInstaStaticPhysDSGChunk(*this);
}

tlDataChunk*
tlInstaStaticPhysDSGChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case Pure3D::Mesh::MESH:
        dch = new tlMeshChunk(&ch);
        break;
    case SRR2::ChunkID::OBJECT_ATTRIBUTES:
        dch = new tlObjectAttributeChunk(&ch);
        break;
    case Simulation::Collision::OBJECT:
        dch = new tlCollisionObjectChunk(&ch);
        break;
    case SRR2::ChunkID::INSTANCES:
        dch = new tlInstancesChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlInstaStaticPhysDSGChunk

const char* 
tlInstaStaticPhysDSGChunk::Name()
{
    return name;
}

void 
tlInstaStaticPhysDSGChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlInstaStaticPhysDSGChunk::Version()
{
    return version;
}

void 
tlInstaStaticPhysDSGChunk::SetVersion(unsigned long val)
{
    version = val;
}

unsigned long 
tlInstaStaticPhysDSGChunk::HasAlpha()
{
    return hasalpha;
}

void 
tlInstaStaticPhysDSGChunk::SetHasAlpha(unsigned long val)
{
    hasalpha = val;
}

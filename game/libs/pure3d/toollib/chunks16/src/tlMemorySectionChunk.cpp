// Automatically generated by Jeeves, don't edit.  Edit tlMemorySectionChunk.sc instead

#include "tlMemorySectionChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlMemorySectionChunk.sc


// tlMemorySectionChunk

int tlMemorySectionChunk::sortPriority = -1;

tlMemorySectionChunk::tlMemorySectionChunk()
{
    Init();
}

tlMemorySectionChunk::tlMemorySectionChunk(const tlMemorySectionChunk& ch)
{
    Init();
    *this = ch;
}

tlMemorySectionChunk::~tlMemorySectionChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlMemorySectionChunk::tlMemorySectionChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == MemorySection::MEMORYSECTION);
    ident = MemorySection::MEMORYSECTION;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    beginsection = f->GetLong();
    endsection = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(MemorySection::MEMORYSECTION);
    }
}

void
tlMemorySectionChunk::Init()
{
    ident = MemorySection::MEMORYSECTION;
    name = NULL;
    beginsection = 0;
    endsection = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(MemorySection::MEMORYSECTION);
    }
}

long 
tlMemorySectionChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( beginsection );
    s += sizeof( endsection );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlMemorySectionChunk&
tlMemorySectionChunk::operator=(const tlMemorySectionChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    beginsection = a.beginsection;
    endsection = a.endsection;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlMemorySectionChunk::operator==(const tlDataChunk& a)
{

    const tlMemorySectionChunk* aptr = dynamic_cast<const tlMemorySectionChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(beginsection == aptr->beginsection))
    {
        return false;
    }
    if(!(endsection == aptr->endsection))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlMemorySectionChunk::NameCompare(const tlDataChunk& a)
{

    const tlMemorySectionChunk* aptr = dynamic_cast<const tlMemorySectionChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlMemorySectionChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlMemorySectionChunk(ch);
    return dtCh;
}

void
tlMemorySectionChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(beginsection);
        f->PutLong(endsection);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlMemorySectionChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlMemorySectionChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name         = \"%s\"\n",indent,"",name);
    printf("%*s  BeginSection = %ld\n",indent,"",beginsection);
    printf("%*s  EndSection   = %ld\n",indent,"",endsection);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlMemorySectionChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name        \n",indent,"",name);
    printf("%*s  int   %12ld # BeginSection\n",indent,"",beginsection);
    printf("%*s  int   %12ld # EndSection  \n",indent,"",endsection);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlMemorySectionChunk::GetType() const
{
    return "tlMemorySectionChunk";
}

int 
tlMemorySectionChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlMemorySectionChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlMemorySectionChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlMemorySectionChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlMemorySectionChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "BeginSection";

        case 2:
            return "EndSection";

        default:
            return 0;
    }
}

bool 
tlMemorySectionChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", beginsection);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", endsection);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlMemorySectionChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlMemorySectionChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlMemorySectionChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                beginsection = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                endsection = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlMemorySectionChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlMemorySectionChunk::Copy()
{
    return new tlMemorySectionChunk(*this);
}

tlDataChunk*
tlMemorySectionChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlMemorySectionChunk

const char* 
tlMemorySectionChunk::Name()
{
    return name;
}

void 
tlMemorySectionChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlMemorySectionChunk::BeginSection()
{
    return beginsection;
}

void 
tlMemorySectionChunk::SetBeginSection(unsigned long val)
{
    beginsection = val;
}

unsigned long 
tlMemorySectionChunk::EndSection()
{
    return endsection;
}

void 
tlMemorySectionChunk::SetEndSection(unsigned long val)
{
    endsection = val;
}

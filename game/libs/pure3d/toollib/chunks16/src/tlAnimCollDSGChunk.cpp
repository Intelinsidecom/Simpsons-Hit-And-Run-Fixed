// Automatically generated by Jeeves, don't edit.  Edit tlAnimCollDSGChunk.sc instead

#include "tlAnimCollDSGChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlAnimCollDSGChunk.sc


// tlAnimCollDSGChunk

int tlAnimCollDSGChunk::sortPriority = -1;

tlAnimCollDSGChunk::tlAnimCollDSGChunk()
{
    Init();
}

tlAnimCollDSGChunk::tlAnimCollDSGChunk(const tlAnimCollDSGChunk& ch)
{
    Init();
    *this = ch;
}

tlAnimCollDSGChunk::~tlAnimCollDSGChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlAnimCollDSGChunk::tlAnimCollDSGChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::ANIM_COLL_DSG);
    ident = SRR2::ChunkID::ANIM_COLL_DSG;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    version = f->GetLong();
    hasalpha = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::ANIM_COLL_DSG);
    }
}

void
tlAnimCollDSGChunk::Init()
{
    ident = SRR2::ChunkID::ANIM_COLL_DSG;
    name = NULL;
    version = 0;
    hasalpha = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::ANIM_COLL_DSG);
    }
}

long 
tlAnimCollDSGChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( version );
    s += sizeof( hasalpha );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlAnimCollDSGChunk&
tlAnimCollDSGChunk::operator=(const tlAnimCollDSGChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    version = a.version;
    hasalpha = a.hasalpha;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlAnimCollDSGChunk::operator==(const tlDataChunk& a)
{

    const tlAnimCollDSGChunk* aptr = dynamic_cast<const tlAnimCollDSGChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(version == aptr->version))
    {
        return false;
    }
    if(!(hasalpha == aptr->hasalpha))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlAnimCollDSGChunk::NameCompare(const tlDataChunk& a)
{

    const tlAnimCollDSGChunk* aptr = dynamic_cast<const tlAnimCollDSGChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlAnimCollDSGChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlAnimCollDSGChunk(ch);
    return dtCh;
}

void
tlAnimCollDSGChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutLong(version);
        f->PutLong(hasalpha);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlAnimCollDSGChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlAnimCollDSGChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name     = \"%s\"\n",indent,"",name);
    printf("%*s  Version  = %ld\n",indent,"",version);
    printf("%*s  HasAlpha = %ld\n",indent,"",hasalpha);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlAnimCollDSGChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name    \n",indent,"",name);
    printf("%*s  int   %12ld # Version \n",indent,"",version);
    printf("%*s  int   %12ld # HasAlpha\n",indent,"",hasalpha);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlAnimCollDSGChunk::GetType() const
{
    return "tlAnimCollDSGChunk";
}

int 
tlAnimCollDSGChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlAnimCollDSGChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlAnimCollDSGChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlAnimCollDSGChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "ULONG";

        case 2:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlAnimCollDSGChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Version";

        case 2:
            return "HasAlpha";

        default:
            return 0;
    }
}

bool 
tlAnimCollDSGChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", hasalpha);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlAnimCollDSGChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlAnimCollDSGChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlAnimCollDSGChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                hasalpha = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlAnimCollDSGChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlAnimCollDSGChunk::Copy()
{
    return new tlAnimCollDSGChunk(*this);
}

tlDataChunk*
tlAnimCollDSGChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case P3D_COMPOSITE_DRAWABLE:
        dch = new tlCompositeDrawableChunk16(&ch);
        break;
    case Pure3D::Animation::FrameControllerData::FRAME_CONTROLLER:
        dch = new tlFrameControllerChunk(&ch);
        break;
    case P3D_MULTI_CONTROLLER:
        dch = new tlMultiControllerChunk16(&ch);
        break;
    case Simulation::Collision::OBJECT:
        dch = new tlCollisionObjectChunk(&ch);
        break;
    case Pure3D::BillboardObject::QUAD_GROUP:
        dch = new tlBillboardQuadGroupChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlAnimCollDSGChunk

const char* 
tlAnimCollDSGChunk::Name()
{
    return name;
}

void 
tlAnimCollDSGChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned long 
tlAnimCollDSGChunk::Version()
{
    return version;
}

void 
tlAnimCollDSGChunk::SetVersion(unsigned long val)
{
    version = val;
}

unsigned long 
tlAnimCollDSGChunk::HasAlpha()
{
    return hasalpha;
}

void 
tlAnimCollDSGChunk::SetHasAlpha(unsigned long val)
{
    hasalpha = val;
}

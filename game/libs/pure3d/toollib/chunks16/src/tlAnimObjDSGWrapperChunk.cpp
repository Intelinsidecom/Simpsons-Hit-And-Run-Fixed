// Automatically generated by Jeeves, don't edit.  Edit tlAnimObjDSGWrapperChunk.sc instead

#include "tlAnimObjDSGWrapperChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlAnimObjDSGWrapperChunk.sc


// tlAnimObjDSGWrapperChunk

int tlAnimObjDSGWrapperChunk::sortPriority = -1;

tlAnimObjDSGWrapperChunk::tlAnimObjDSGWrapperChunk()
{
    Init();
}

tlAnimObjDSGWrapperChunk::tlAnimObjDSGWrapperChunk(const tlAnimObjDSGWrapperChunk& ch)
{
    Init();
    *this = ch;
}

tlAnimObjDSGWrapperChunk::~tlAnimObjDSGWrapperChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlAnimObjDSGWrapperChunk::tlAnimObjDSGWrapperChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::ANIM_OBJ_DSG_WRAPPER);
    ident = SRR2::ChunkID::ANIM_OBJ_DSG_WRAPPER;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    version = f->GetChar();
    hasalpha = f->GetChar();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::ANIM_OBJ_DSG_WRAPPER);
    }
}

void
tlAnimObjDSGWrapperChunk::Init()
{
    ident = SRR2::ChunkID::ANIM_OBJ_DSG_WRAPPER;
    name = NULL;
    version = 0;
    hasalpha = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::ANIM_OBJ_DSG_WRAPPER);
    }
}

long 
tlAnimObjDSGWrapperChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    s += sizeof( version );
    s += sizeof( hasalpha );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlAnimObjDSGWrapperChunk&
tlAnimObjDSGWrapperChunk::operator=(const tlAnimObjDSGWrapperChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);
    version = a.version;
    hasalpha = a.hasalpha;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlAnimObjDSGWrapperChunk::operator==(const tlDataChunk& a)
{

    const tlAnimObjDSGWrapperChunk* aptr = dynamic_cast<const tlAnimObjDSGWrapperChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    if(!(version == aptr->version))
    {
        return false;
    }
    if(!(hasalpha == aptr->hasalpha))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlAnimObjDSGWrapperChunk::NameCompare(const tlDataChunk& a)
{

    const tlAnimObjDSGWrapperChunk* aptr = dynamic_cast<const tlAnimObjDSGWrapperChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlAnimObjDSGWrapperChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlAnimObjDSGWrapperChunk(ch);
    return dtCh;
}

void
tlAnimObjDSGWrapperChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);
        f->PutChar(version);
        f->PutChar(hasalpha);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlAnimObjDSGWrapperChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlAnimObjDSGWrapperChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name     = \"%s\"\n",indent,"",name);
    printf("%*s  Version  = %d\n",indent,"",version);
    printf("%*s  HasAlpha = %d\n",indent,"",hasalpha);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlAnimObjDSGWrapperChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name    \n",indent,"",name);
    printf("%*s  byte  %12d # Version \n",indent,"",version);
    printf("%*s  byte  %12d # HasAlpha\n",indent,"",hasalpha);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlAnimObjDSGWrapperChunk::GetType() const
{
    return "tlAnimObjDSGWrapperChunk";
}

int 
tlAnimObjDSGWrapperChunk::GetFieldCount() const
{
    return 3;
}

bool 
tlAnimObjDSGWrapperChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlAnimObjDSGWrapperChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlAnimObjDSGWrapperChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "UBYTE";

        case 2:
            return "UBYTE";

        default:
            return 0;
    }
}

const char* 
tlAnimObjDSGWrapperChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        case 1:
            return "Version";

        case 2:
            return "HasAlpha";

        default:
            return 0;
    }
}

bool 
tlAnimObjDSGWrapperChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%d", version);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%d", hasalpha);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlAnimObjDSGWrapperChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlAnimObjDSGWrapperChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        default:
            return false;
    }
}

bool 
tlAnimObjDSGWrapperChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            unsigned char tmp;
            tmp = (unsigned char)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned char tmp;
            tmp = (unsigned char)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                hasalpha = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlAnimObjDSGWrapperChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlAnimObjDSGWrapperChunk::Copy()
{
    return new tlAnimObjDSGWrapperChunk(*this);
}

tlDataChunk*
tlAnimObjDSGWrapperChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case StateProp::STATEPROP:
        dch = new tlStatePropChunk(&ch);
        break;
    case Pure3D::Animation::AnimationData::ANIMATION:
        dch = new tlAnimationChunk(&ch);
        break;
    case P3D_COMPOSITE_DRAWABLE:
        dch = new tlCompositeDrawableChunk16(&ch);
        break;
    case Pure3D::Animation::FrameControllerData::FRAME_CONTROLLER:
        dch = new tlFrameControllerChunk(&ch);
        break;
    case P3D_MULTI_CONTROLLER:
        dch = new tlMultiControllerChunk16(&ch);
        break;
    case Simulation::Collision::OBJECT:
        dch = new tlCollisionObjectChunk(&ch);
        break;
    case Pure3D::BillboardObject::QUAD_GROUP:
        dch = new tlBillboardQuadGroupChunk(&ch);
        break;
    case Pure3D::Mesh::MESH:
        dch = new tlMeshChunk(&ch);
        break;
    case Simulation::Physics::OBJECT:
        dch = new tlPhysicsObjectChunk(&ch);
        break;
    case SRR2::ChunkID::OBJECT_ATTRIBUTES:
        dch = new tlObjectAttributeChunk(&ch);
        break;
    case P3D_SKELETON:
        dch = new tlSkeletonChunk16(&ch);
        break;
    case Pure3D::AnimatedObject::FACTORY:
        dch = new tlAnimatedObjectFactoryChunk(&ch);
        break;
    case Pure3D::AnimatedObject::OBJECT:
        dch = new tlAnimatedObjectChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlAnimObjDSGWrapperChunk

const char* 
tlAnimObjDSGWrapperChunk::Name()
{
    return name;
}

void 
tlAnimObjDSGWrapperChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

unsigned char 
tlAnimObjDSGWrapperChunk::Version()
{
    return version;
}

void 
tlAnimObjDSGWrapperChunk::SetVersion(unsigned char val)
{
    version = val;
}

unsigned char 
tlAnimObjDSGWrapperChunk::HasAlpha()
{
    return hasalpha;
}

void 
tlAnimObjDSGWrapperChunk::SetHasAlpha(unsigned char val)
{
    hasalpha = val;
}

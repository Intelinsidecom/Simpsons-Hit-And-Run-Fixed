// Automatically generated by Jeeves, don't edit.  Edit tlInstParticleSystemChunk.sc instead

#include "tlInstParticleSystemChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlInstParticleSystemChunk.sc


// tlInstParticleSystemChunk

int tlInstParticleSystemChunk::sortPriority = -1;

tlInstParticleSystemChunk::tlInstParticleSystemChunk()
{
    Init();
}

tlInstParticleSystemChunk::tlInstParticleSystemChunk(const tlInstParticleSystemChunk& ch)
{
    Init();
    *this = ch;
}

tlInstParticleSystemChunk::~tlInstParticleSystemChunk()
{

}

tlInstParticleSystemChunk::tlInstParticleSystemChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::INST_PARTICLE_SYSTEM);
    ident = SRR2::ChunkID::INST_PARTICLE_SYSTEM;

    tlFile* f = ch->GetFile();

    particletype = f->GetLong();
    maxinstances = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::INST_PARTICLE_SYSTEM);
    }
}

void
tlInstParticleSystemChunk::Init()
{
    ident = SRR2::ChunkID::INST_PARTICLE_SYSTEM;
    particletype = 0;
    maxinstances = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::INST_PARTICLE_SYSTEM);
    }
}

long 
tlInstParticleSystemChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += sizeof( particletype );
    s += sizeof( maxinstances );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlInstParticleSystemChunk&
tlInstParticleSystemChunk::operator=(const tlInstParticleSystemChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    particletype = a.particletype;
    maxinstances = a.maxinstances;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlInstParticleSystemChunk::operator==(const tlDataChunk& a)
{

    const tlInstParticleSystemChunk* aptr = dynamic_cast<const tlInstParticleSystemChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(!(particletype == aptr->particletype))
    {
        return false;
    }
    if(!(maxinstances == aptr->maxinstances))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlInstParticleSystemChunk::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlInstParticleSystemChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlInstParticleSystemChunk(ch);
    return dtCh;
}

void
tlInstParticleSystemChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutLong(particletype);
        f->PutLong(maxinstances);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlInstParticleSystemChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlInstParticleSystemChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  ParticleType = %ld\n",indent,"",particletype);
    printf("%*s  MaxInstances = %ld\n",indent,"",maxinstances);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlInstParticleSystemChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  int   %12ld # ParticleType\n",indent,"",particletype);
    printf("%*s  int   %12ld # MaxInstances\n",indent,"",maxinstances);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlInstParticleSystemChunk::GetType() const
{
    return "tlInstParticleSystemChunk";
}

int 
tlInstParticleSystemChunk::GetFieldCount() const
{
    return 2;
}

bool 
tlInstParticleSystemChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlInstParticleSystemChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlInstParticleSystemChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ULONG";

        case 1:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlInstParticleSystemChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "ParticleType";

        case 1:
            return "MaxInstances";

        default:
            return 0;
    }
}

bool 
tlInstParticleSystemChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%ld", particletype);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%ld", maxinstances);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlInstParticleSystemChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlInstParticleSystemChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        default:
            return false;
    }
}

bool 
tlInstParticleSystemChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                particletype = tmp;
                return true;
            }
            return false;
        }
        case 1:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                maxinstances = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlInstParticleSystemChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlInstParticleSystemChunk::Copy()
{
    return new tlInstParticleSystemChunk(*this);
}

tlDataChunk*
tlInstParticleSystemChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case Pure3D::ParticleSystem::SYSTEM_FACTORY:
        dch = new tlParticleSystemFactoryChunk(&ch);
        break;
    case Pure3D::Animation::FrameControllerData::FRAME_CONTROLLER:
        dch = new tlFrameControllerChunk(&ch);
        break;
    case Pure3D::Shader::SHADER:
        dch = new tlShaderChunk(&ch);
        break;
    case Pure3D::Animation::AnimationData::ANIMATION:
        dch = new tlAnimationChunk(&ch);
        break;
    case Pure3D::Texture::TEXTURE:
        dch = new tlTextureChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlInstParticleSystemChunk

unsigned long 
tlInstParticleSystemChunk::ParticleType()
{
    return particletype;
}

void 
tlInstParticleSystemChunk::SetParticleType(unsigned long val)
{
    particletype = val;
}

unsigned long 
tlInstParticleSystemChunk::MaxInstances()
{
    return maxinstances;
}

void 
tlInstParticleSystemChunk::SetMaxInstances(unsigned long val)
{
    maxinstances = val;
}

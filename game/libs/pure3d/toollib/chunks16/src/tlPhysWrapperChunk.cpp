// Automatically generated by Jeeves, don't edit.  Edit tlPhysWrapperChunk.sc instead

#include "tlPhysWrapperChunk.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlPhysWrapperChunk.sc


// tlPhysWrapperChunk

int tlPhysWrapperChunk::sortPriority = -1;

tlPhysWrapperChunk::tlPhysWrapperChunk()
{
    Init();
}

tlPhysWrapperChunk::tlPhysWrapperChunk(const tlPhysWrapperChunk& ch)
{
    Init();
    *this = ch;
}

tlPhysWrapperChunk::~tlPhysWrapperChunk()
{

    if(name != NULL){
        strdelete(name);
    }
}

tlPhysWrapperChunk::tlPhysWrapperChunk(tlReadChunk16* ch)
{
    assert(ch->GetID() == SRR2::ChunkID::PHYS_WRAPPER);
    ident = SRR2::ChunkID::PHYS_WRAPPER;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    name = strnew(buf);
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::PHYS_WRAPPER);
    }
}

void
tlPhysWrapperChunk::Init()
{
    ident = SRR2::ChunkID::PHYS_WRAPPER;
    name = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(SRR2::ChunkID::PHYS_WRAPPER);
    }
}

long 
tlPhysWrapperChunk::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( name );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlPhysWrapperChunk&
tlPhysWrapperChunk::operator=(const tlPhysWrapperChunk& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    name = strnew(a.name);

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlPhysWrapperChunk::operator==(const tlDataChunk& a)
{

    const tlPhysWrapperChunk* aptr = dynamic_cast<const tlPhysWrapperChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlPhysWrapperChunk::NameCompare(const tlDataChunk& a)
{

    const tlPhysWrapperChunk* aptr = dynamic_cast<const tlPhysWrapperChunk*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }

    if(strcmp(name, aptr->name) != 0)
    {
        return false;
    }
    return true;
}


tlDataChunk*
tlPhysWrapperChunk::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlPhysWrapperChunk(ch);
    return dtCh;
}

void
tlPhysWrapperChunk::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(name);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlPhysWrapperChunk::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlPhysWrapperChunk (%4x)\n",indent,"",(int) ident);
    printf("%*s  Name = \"%s\"\n",indent,"",name);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlPhysWrapperChunk::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Name\n",indent,"",name);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlPhysWrapperChunk::GetType() const
{
    return "tlPhysWrapperChunk";
}

int 
tlPhysWrapperChunk::GetFieldCount() const
{
    return 1;
}

bool 
tlPhysWrapperChunk::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlPhysWrapperChunk::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlPhysWrapperChunk::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        default:
            return 0;
    }
}

const char* 
tlPhysWrapperChunk::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Name";

        default:
            return 0;
    }
}

bool 
tlPhysWrapperChunk::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", name);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlPhysWrapperChunk::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlPhysWrapperChunk::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        default:
            return false;
    }
}

bool 
tlPhysWrapperChunk::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(name);
            name = strnew(new_val);
            return true;
        default:
            return false;
    }
}

bool 
tlPhysWrapperChunk::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlPhysWrapperChunk::Copy()
{
    return new tlPhysWrapperChunk(*this);
}

tlDataChunk*
tlPhysWrapperChunk::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case SRR2::ChunkID::OBJECT_ATTRIBUTES:
        dch = new tlObjectAttributeChunk(&ch);
        break;
    case Simulation::Collision::OBJECT:
        dch = new tlCollisionObjectChunk(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlPhysWrapperChunk

const char* 
tlPhysWrapperChunk::Name()
{
    return name;
}

void 
tlPhysWrapperChunk::SetName(const char* val)
{
    strdelete(name);
    name = strnew(val);
}

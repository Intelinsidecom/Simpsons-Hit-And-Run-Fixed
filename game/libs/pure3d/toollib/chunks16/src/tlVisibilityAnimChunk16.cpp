// Automatically generated by Jeeves, don't edit.  Edit tlVisibilityAnimChunk16.sc instead

#include "tlVisibilityAnimChunk16.hpp"
#include "tlString.hpp"
#include "tlFile.hpp"
#include "tlChunk16.hpp"

#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <memory.h>
#include <string.h>


// things included by tlVisibilityAnimChunk16.sc


// tlVisibilityAnimChunk16

int tlVisibilityAnimChunk16::sortPriority = -1;

tlVisibilityAnimChunk16::tlVisibilityAnimChunk16()
{
    Init();
}

tlVisibilityAnimChunk16::tlVisibilityAnimChunk16(const tlVisibilityAnimChunk16& ch)
{
    Init();
    *this = ch;
}

tlVisibilityAnimChunk16::~tlVisibilityAnimChunk16()
{

    if(animname != NULL){
        strdelete(animname);
    }
    if(scenename != NULL){
        strdelete(scenename);
    }
}

tlVisibilityAnimChunk16::tlVisibilityAnimChunk16(tlReadChunk16* ch)
{
    assert(ch->GetID() == P3D_VISIBILITY_ANIM);
    ident = P3D_VISIBILITY_ANIM;

    tlFile* f = ch->GetFile();
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    animname = strnew(buf);
    f->GetPString(buf);
    scenename = strnew(buf);
    version = f->GetLong();
    numframes = f->GetLong();
    framerate = f->GetFloat();
    numchannels = f->GetLong();
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_VISIBILITY_ANIM);
    }
}

void
tlVisibilityAnimChunk16::Init()
{
    ident = P3D_VISIBILITY_ANIM;
    animname = NULL;
    scenename = NULL;
    version = 0;
    numframes = 0;
    framerate = 0.0f;
    numchannels = 0;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_VISIBILITY_ANIM);
    }
}

long 
tlVisibilityAnimChunk16::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( animname );
    s += strlen( scenename );
    s += sizeof( version );
    s += sizeof( numframes );
    s += sizeof( framerate );
    s += sizeof( numchannels );
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlVisibilityAnimChunk16&
tlVisibilityAnimChunk16::operator=(const tlVisibilityAnimChunk16& a)
{

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    animname = strnew(a.animname);
    scenename = strnew(a.scenename);
    version = a.version;
    numframes = a.numframes;
    framerate = a.framerate;
    numchannels = a.numchannels;

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlVisibilityAnimChunk16::operator==(const tlDataChunk& a)
{

    const tlVisibilityAnimChunk16* aptr = dynamic_cast<const tlVisibilityAnimChunk16*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(animname, aptr->animname) != 0)
    {
        return false;
    }
    if(strcmp(scenename, aptr->scenename) != 0)
    {
        return false;
    }
    if(!(version == aptr->version))
    {
        return false;
    }
    if(!(numframes == aptr->numframes))
    {
        return false;
    }
    if(!(framerate == aptr->framerate))
    {
        return false;
    }
    if(!(numchannels == aptr->numchannels))
    {
        return false;
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlVisibilityAnimChunk16::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlVisibilityAnimChunk16::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlVisibilityAnimChunk16(ch);
    return dtCh;
}

void
tlVisibilityAnimChunk16::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

        f->PutPString(animname);
        f->PutPString(scenename);
        f->PutLong(version);
        f->PutLong(numframes);
        f->PutFloat(framerate);
        f->PutLong(numchannels);

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlVisibilityAnimChunk16::Print(int indent, unsigned int printdepth, unsigned int typemask)
{

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlVisibilityAnimChunk16 (%4x)\n",indent,"",(int) ident);
    printf("%*s  AnimName    = \"%s\"\n",indent,"",animname);
    printf("%*s  SceneName   = \"%s\"\n",indent,"",scenename);
    printf("%*s  Version     = %ld\n",indent,"",version);
    printf("%*s  NumFrames   = %ld\n",indent,"",numframes);
    printf("%*s  FrameRate   = %f\n",indent,"",framerate);
    printf("%*s  NumChannels = %ld\n",indent,"",numchannels);

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlVisibilityAnimChunk16::PrintFormatted(int indent)
{
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # AnimName   \n",indent,"",animname);
    printf("%*s  pstring \"%s\" # SceneName  \n",indent,"",scenename);
    printf("%*s  int   %12ld # Version    \n",indent,"",version);
    printf("%*s  int   %12ld # NumFrames  \n",indent,"",numframes);
    printf("%*s  float %12f # FrameRate  \n",indent,"",framerate);
    printf("%*s  int   %12ld # NumChannels\n",indent,"",numchannels);
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlVisibilityAnimChunk16::GetType() const
{
    return "tlVisibilityAnimChunk16";
}

int 
tlVisibilityAnimChunk16::GetFieldCount() const
{
    return 6;
}

bool 
tlVisibilityAnimChunk16::GetFieldIsArray(int fieldIndex) const
{

    return false;
}

int 
tlVisibilityAnimChunk16::GetFieldArrayCount(int fieldIndex) const
{
    return 0;
}

const char* 
tlVisibilityAnimChunk16::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "string";

        case 2:
            return "ULONG";

        case 3:
            return "ULONG";

        case 4:
            return "float";

        case 5:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlVisibilityAnimChunk16::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "AnimName";

        case 1:
            return "SceneName";

        case 2:
            return "Version";

        case 3:
            return "NumFrames";

        case 4:
            return "FrameRate";

        case 5:
            return "NumChannels";

        default:
            return 0;
    }
}

bool 
tlVisibilityAnimChunk16::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", animname);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%s", scenename);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", version);
            strncpy(val, buf, len);
            return true;

        case 3:
            sprintf(buf, "%ld", numframes);
            strncpy(val, buf, len);
            return true;

        case 4:
            sprintf(buf, "%f", framerate);
            strncpy(val, buf, len);
            return true;

        case 5:
            sprintf(buf, "%ld", numchannels);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlVisibilityAnimChunk16::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    return false;
}

bool 
tlVisibilityAnimChunk16::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        case 4:     
            return true;

        case 5:     
            return true;

        default:
            return false;
    }
}

bool 
tlVisibilityAnimChunk16::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(animname);
            animname = strnew(new_val);
            return true;
        case 1:
            strdelete(scenename);
            scenename = strnew(new_val);
            return true;
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                version = tmp;
                return true;
            }
            return false;
        }
        case 3:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numframes = tmp;
                return true;
            }
            return false;
        }
        case 4:
        {
            char* end_ptr;
            float tmp;
            tmp = (float)strtod(new_val, &end_ptr);
            if (*end_ptr == 0)
            {
                framerate = tmp;
                return true;
            }
            return false;
        }
        case 5:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numchannels = tmp;
                return true;
            }
            return false;
        }
        default:
            return false;
    }
}

bool 
tlVisibilityAnimChunk16::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    return false;
}

tlDataChunk*
tlVisibilityAnimChunk16::Copy()
{
    return new tlVisibilityAnimChunk16(*this);
}

tlDataChunk*
tlVisibilityAnimChunk16::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    switch(id)
    {
    case P3D_VISIBILITY_ANIM_CHANNEL:
        dch = new tlVisibilityAnimChannelChunk16(&ch);
        break;
    default:
        dch = new tlDataChunk(id);
        int size = ch.GetDataSize();
        char* buf = new char[size];
        f->GetBytes(buf, size);
        dch->SetData(buf, size);
        delete buf;
    }
    ch.Skip();
    return dch;
}

// Accessor Methods for tlVisibilityAnimChunk16

const char* 
tlVisibilityAnimChunk16::AnimName()
{
    return animname;
}

void 
tlVisibilityAnimChunk16::SetAnimName(const char* val)
{
    strdelete(animname);
    animname = strnew(val);
}

const char* 
tlVisibilityAnimChunk16::SceneName()
{
    return scenename;
}

void 
tlVisibilityAnimChunk16::SetSceneName(const char* val)
{
    strdelete(scenename);
    scenename = strnew(val);
}

unsigned long 
tlVisibilityAnimChunk16::Version()
{
    return version;
}

void 
tlVisibilityAnimChunk16::SetVersion(unsigned long val)
{
    version = val;
}

unsigned long 
tlVisibilityAnimChunk16::NumFrames()
{
    return numframes;
}

void 
tlVisibilityAnimChunk16::SetNumFrames(unsigned long val)
{
    numframes = val;
}

float 
tlVisibilityAnimChunk16::FrameRate()
{
    return framerate;
}

void 
tlVisibilityAnimChunk16::SetFrameRate(float val)
{
    framerate = val;
}

unsigned long 
tlVisibilityAnimChunk16::NumChannels()
{
    return numchannels;
}

void 
tlVisibilityAnimChunk16::SetNumChannels(unsigned long val)
{
    numchannels = val;
}
// tlVisibilityAnimChannelChunk16

int tlVisibilityAnimChannelChunk16::sortPriority = -1;

tlVisibilityAnimChannelChunk16::tlVisibilityAnimChannelChunk16()
{
    Init();
}

tlVisibilityAnimChannelChunk16::tlVisibilityAnimChannelChunk16(const tlVisibilityAnimChannelChunk16& ch)
{
    Init();
    *this = ch;
}

tlVisibilityAnimChannelChunk16::~tlVisibilityAnimChannelChunk16()
{

    if(nodename != NULL){
        strdelete(nodename);
    }
    if(frames != NULL){
        delete[] frames;
    }
}

tlVisibilityAnimChannelChunk16::tlVisibilityAnimChannelChunk16(tlReadChunk16* ch)
{
    assert(ch->GetID() == P3D_VISIBILITY_ANIM_CHANNEL);
    ident = P3D_VISIBILITY_ANIM_CHANNEL;

    tlFile* f = ch->GetFile();
    unsigned int i;
    char buf[P3DMAXNAME];

    f->GetPString(buf);
    nodename = strnew(buf);
    bstartstate = f->GetWord();
    numframes = f->GetLong();
    frames = new unsigned long[numframes];
    for(i = 0; i < numframes; i++)
    {
        frames[i] = f->GetLong();
    }
    while (!ch->EndOfChunk())
    {
        AppendSubChunk(LoadSubChunk(f));
    }
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_VISIBILITY_ANIM_CHANNEL);
    }
}

void
tlVisibilityAnimChannelChunk16::Init()
{
    ident = P3D_VISIBILITY_ANIM_CHANNEL;
    nodename = NULL;
    bstartstate = 0;
    numframes = 0;
    frames = NULL;
               
    if(sortPriority == -1)
    {
        sortPriority = FindPriority(P3D_VISIBILITY_ANIM_CHANNEL);
    }
}

long 
tlVisibilityAnimChannelChunk16::Size( )
{

    long s = 2*sizeof( long );

    int i;

    s += strlen( nodename );
    s += sizeof( bstartstate );
    s += sizeof( numframes );
    for(i = 0; i < (int)numframes; i++)
    {
        s += sizeof( frames[i ] );
    }
    for( i=0; i < SubChunkCount( ); i++){
        s += GetSubChunk(i)->Size();
    }

    return s;
}

tlVisibilityAnimChannelChunk16&
tlVisibilityAnimChannelChunk16::operator=(const tlVisibilityAnimChannelChunk16& a)
{
    unsigned int i;

    if(ident != a.ident)
    {
        assert(ident != a.ident);
        return *this;
    }

    nodename = strnew(a.nodename);
    bstartstate = a.bstartstate;
    numframes = a.numframes;
    if(frames)
    {
        delete frames;
    }
    frames = new unsigned long[numframes];
    for( i = 0; i < numframes ; i++ )
    {
        frames[i] = a.frames[i];
    }

    int subch;

    for(subch = 0; subch < subcount; subch++)
    {
        RemoveSubChunk(subch);
    }

    for(subch = 0; subch < a.subcount; subch++)
    {
        AppendSubChunk(a.GetSubChunk(subch)->Copy());
    }
    
    return *this;
}

bool
tlVisibilityAnimChannelChunk16::operator==(const tlDataChunk& a)
{
    unsigned int i;

    const tlVisibilityAnimChannelChunk16* aptr = dynamic_cast<const tlVisibilityAnimChannelChunk16*>(&a);

    if(aptr == NULL)
    {
        return false;
    }

    if(ident != aptr->ident)
    {
        return false;
    }


    if(strcmp(nodename, aptr->nodename) != 0)
    {
        return false;
    }
    if(!(bstartstate == aptr->bstartstate))
    {
        return false;
    }
    if(!(numframes == aptr->numframes))
    {
        return false;
    }
    for(i = 0; i < numframes; i++)
    {
        if(!(frames[i] == aptr->frames[i]))
        {
            return false;
        }
    }

    // check the subchunks
    if(subcount != aptr->subcount)
    {
        return false;
    }

    for(int subch = 0; subch < subcount; subch++)
    {
        if(!((*GetSubChunk(subch)) == (*aptr->GetSubChunk(subch))))
        {
            return false;
        }
    }
    return true;
}

bool
tlVisibilityAnimChannelChunk16::NameCompare(const tlDataChunk& a)
{

    // This chunk has no name, so we must compare the bits
    return operator==(a);
}


tlDataChunk*
tlVisibilityAnimChannelChunk16::Create(tlReadChunk16* ch)
{
    tlDataChunk* dtCh = new tlVisibilityAnimChannelChunk16(ch);
    return dtCh;
}

void
tlVisibilityAnimChannelChunk16::Write(tlFile* f)
{
    tlWriteChunk16 ch(f, ident);

    unsigned int i;
        f->PutPString(nodename);
        f->PutWord(bstartstate);
        f->PutLong(numframes);
    for(i = 0; i < numframes ; i++)
    {
        f->PutLong(frames[i]);
    }

    ch.ContentFinish();
    for(int subch = 0; subch < subcount; subch++)
    {
        GetSubChunk(subch)->Write(f);
    }
}

void
tlVisibilityAnimChannelChunk16::Print(int indent, unsigned int printdepth, unsigned int typemask)
{
    unsigned int i;

    if ( printdepth == 0 )
    {
        return;
    }

    printf("%*stlVisibilityAnimChannelChunk16 (%4x)\n",indent,"",(int) ident);
    printf("%*s  Nodename    = \"%s\"\n",indent,"",nodename);
    printf("%*s  bStartState = %d\n",indent,"",bstartstate);
    printf("%*s  NumFrames   = %ld\n",indent,"",numframes);
    printf("%*s  Frames:\n",indent,"");
    for( i=0; i < numframes; i++)
    {
         printf("%*s    %d %ld\n",indent,"",i,frames[i]);
    }

    if(subcount)
    {
        printf("%*s  SubChunks (%d):\n",indent,"",subcount);
        for(int subch = 0; subch < subcount; subch++)
        {
            GetSubChunk(subch)->Print(indent+4, printdepth-1, typemask);
        }
    }
}

void
tlVisibilityAnimChannelChunk16::PrintFormatted(int indent)
{
    unsigned int i;
    printf("%*schunk 0x%04x  \n",indent,"",(int) ident);
    printf("%*s  pstring \"%s\" # Nodename   \n",indent,"",nodename);
    printf("%*s  short %12d # bStartState\n",indent,"",bstartstate);
    printf("%*s  int   %12ld # NumFrames  \n",indent,"",numframes);
    printf("%*s  repeat 1 int   # Frames     \n",indent,"");
    for( i=0; i < numframes; i++)
    {
         printf("%*s    %12ld\n",indent,"",frames[i]);
    }
    printf("%*s  endrepeat\n",indent,"");
    if(subcount)
    {
        for(int i=0;i<subcount;i++)
        {
            GetSubChunk(i)->PrintFormatted(indent+2);
        }
    }
    printf("%*sendchunk\n",indent,"");
}

const char* 
tlVisibilityAnimChannelChunk16::GetType() const
{
    return "tlVisibilityAnimChannelChunk16";
}

int 
tlVisibilityAnimChannelChunk16::GetFieldCount() const
{
    return 4;
}

bool 
tlVisibilityAnimChannelChunk16::GetFieldIsArray(int fieldIndex) const
{

    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return false;

        case 2:
            return false;

        case 3:
            return true;

        default:
            return false;
    }
}

int 
tlVisibilityAnimChannelChunk16::GetFieldArrayCount(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 3:
            return numframes;

        default:
            return 0;
    }
}

const char* 
tlVisibilityAnimChannelChunk16::GetFieldType(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "string";

        case 1:
            return "WORD";

        case 2:
            return "ULONG";

        case 3:
            return "ULONG";

        default:
            return 0;
    }
}

const char* 
tlVisibilityAnimChannelChunk16::GetFieldName(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:
            return "Nodename";

        case 1:
            return "bStartState";

        case 2:
            return "NumFrames";

        case 3:
            return "Frames";

        default:
            return 0;
    }
}

bool 
tlVisibilityAnimChannelChunk16::GetFieldValue(int fieldIndex, char* val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:
            sprintf(buf, "%s", nodename);
            strncpy(val, buf, len);
            return true;

        case 1:
            sprintf(buf, "%d", bstartstate);
            strncpy(val, buf, len);
            return true;

        case 2:
            sprintf(buf, "%ld", numframes);
            strncpy(val, buf, len);
            return true;

        case 3:
            return false;

        default:
            return false;
    }
}

bool 
tlVisibilityAnimChannelChunk16::GetFieldArrayValue(int fieldIndex, int arrayIndex, char *val, int len) const
{
    char buf[65536];

    switch(fieldIndex)
    {
        case 0:        
            return false;

        case 1:        
            return false;

        case 2:        
            return false;

        case 3:        


            sprintf(buf, "%ld", frames[arrayIndex]);
            strncpy(val, buf, len);
            return true;

        default:
            return false;
    }
}

bool 
tlVisibilityAnimChannelChunk16::GetFieldUpdatable(int fieldIndex) const
{
    switch(fieldIndex)
    {
        case 0:     
            return true;

        case 1:     
            return true;

        case 2:     
            return true;

        case 3:     
            return true;

        default:
            return false;
    }
}

bool 
tlVisibilityAnimChannelChunk16::SetFieldValue(int fieldIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            strdelete(nodename);
            nodename = strnew(new_val);
            return true;
        case 1:
        {
            char* end_ptr;
            short tmp;
            tmp = (short)strtol(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                bstartstate = tmp;
                return true;
            }
            return false;
        }
        case 2:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                numframes = tmp;
                return true;
            }
            return false;
        }
        case 3:
            return false;
        default:
            return false;
    }
}

bool 
tlVisibilityAnimChannelChunk16::SetFieldArrayValue(int fieldIndex, int arrayIndex, const char* new_val)
{
    switch(fieldIndex)
    {
        case 0:
            return false;

        case 1:
            return false;

        case 2:
            return false;

        case 3:
        {
            char* end_ptr;
            unsigned long tmp;
            tmp = (unsigned long)strtoul(new_val, &end_ptr, 0);
            if (*end_ptr == 0)
            {
                frames[arrayIndex] = tmp;
                return true;
            }
            return false;
        }

        default:
            return false;
    }
}

tlDataChunk*
tlVisibilityAnimChannelChunk16::Copy()
{
    return new tlVisibilityAnimChannelChunk16(*this);
}

tlDataChunk*
tlVisibilityAnimChannelChunk16::LoadSubChunk(tlFile* f)
{
    tlDataChunk *dch;
    tlReadChunk16  ch(f);
    ch.ReadNext();
    unsigned int id = ch.GetID();
 
    dch = new tlDataChunk(id);
    int size = ch.GetDataSize();
    char* buf = new char[size];
    f->GetBytes(buf, size);
    dch->SetData(buf, size);
    delete buf;
    ch.Skip();
    return dch;
}

// Accessor Methods for tlVisibilityAnimChannelChunk16

const char* 
tlVisibilityAnimChannelChunk16::Nodename()
{
    return nodename;
}

void 
tlVisibilityAnimChannelChunk16::SetNodename(const char* val)
{
    strdelete(nodename);
    nodename = strnew(val);
}

short 
tlVisibilityAnimChannelChunk16::bStartState()
{
    return bstartstate;
}

void 
tlVisibilityAnimChannelChunk16::SetbStartState(short val)
{
    bstartstate = val;
}

unsigned long 
tlVisibilityAnimChannelChunk16::NumFrames()
{
    return numframes;
}

void 
tlVisibilityAnimChannelChunk16::SetNumFrames(unsigned long val)
{
    numframes = val;
}

unsigned long* 
tlVisibilityAnimChannelChunk16::Frames()
{
    return frames;
}

void 
tlVisibilityAnimChannelChunk16::SetFrames(unsigned long* val, int count)
{
    if(frames)
    {
        delete[] frames;
    }      
    unsigned long* newval = new unsigned long[count];
    for(int i = 0; i < count ; i++)
    {
        newval[i] = val[i];
    }
    frames = newval;
}
